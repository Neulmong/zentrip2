# checklist.md — 최종 검증 체크리스트

> **문서 버전 2.4** (2026-08-10). `spec.md` 2.4 기준. 개정 이력: [CHANGELOG.md](./CHANGELOG.md)
> 2.3 → 2.4 개정: **AA 섹션(동시성·원자성)·AB 섹션(삭제·정리) 신설**, A 섹션에 AI 호출 계약·시작 조건·`reason` 검증 추가, G·H 섹션에 `data` 키 표 대조 추가, 카운터 3종 → **4종**, `verdict` 저장값 영어, `abnormality_flags.attempt_no`, 길이 계약 **생성 4종 / 편집 6종** 분리, `gallery` 슬롯 삭제 반영, T-07 중복 해소.

근거: `spec.md` 2.4, `workflow.md` 2.4, `.claude/agents/`(5개), `.claude/skills/`(15개 = 워커 14 + `product-orchestrator` 1).

## 사용 방법

- 모든 항목은 **예/아니오**로만 판정한다. "부분 통과"는 없다. 한 항목 안에 조건이 여러 개면 **전부 충족해야 `예`**다.
- 판정 단위는 **`products.id` 1건**이다. 로그 항목은 해당 상품의 `execution_id` 기준이다.
- `[필수]` 항목이 하나라도 `아니오`면 **최종 불합격**이며, §Z의 게이트에 지정된 단계로 되돌려 재실행한다.
- `[조건]` 항목은 조건이 성립할 때만 판정하고, 성립하지 않으면 `N/A`로 표기한다.
- 근거 열 표기: `spec §n` = spec.md 2.4의 절 번호, 그 외는 skill/agent 문서명.

### 검증 대상 데이터

| 대상 | 위치 | 생성 주체 | 비고 |
|---|---|---|---|
| **원본 폼 입력** | `products.form_input` | 웹 폼 | **모든 검증의 기준값**. 구조는 spec §7.4 |
| 확정 데이터표 | `products.confirmed_data` | intake-agent | 일차 분해 포함 |
| 소개서(간단버전) | `products.brochure_content` | content-writer-agent | 스키마 spec §8.7 |
| 상품 페이지 콘텐츠 | `products.page_content` | web-builder-agent | 스키마 spec §9.3. `theme` 단일 출처 |
| 검증 스냅샷 | `products.validation_snapshot` | validator-agent | 4축 + `items`(spec §11.3) |
| 이미지 | `product_images` + Storage | 웹 폼 | 슬롯 4종 |
| 실행 로그 | `execution_logs` | log-monitor-agent | `attempt_no`·`retry_index` |
| 이상 플래그 | `abnormality_flags` | log-monitor-agent | 5종 + `attempt_no` |
| 편집 이력 | `edit_history` | 편집기 | |
| 신청 내역 | `applications` | 공개 신청 폼 | |

---

## A. 실행 구조 — 1요청 1AI호출 · AI 호출 계약 (§4.2·§4.3)

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| A-01 | §14.4 표의 **모든 엔드포인트가 AI를 최대 1회 호출**한다(AI 열이 0 또는 1) | [필수] | spec §4.2, §14.4 |
| A-02 | 소개서 생성이 **4개 요청**(등록 → 분해 → 소개서 → 1차검증)으로 나뉘어 순차 실행된다 | [필수] | spec §8.5 |
| A-03 | 상품 페이지 생성이 **3개 요청**(페이지 → 2차검증 → 3차검증)으로 나뉘어 순차 실행된다 | [필수] | spec §9.6 |
| A-04 | 단일 요청이 60초를 초과해 강제 종료된 건이 **0건**이다 | [필수] | spec §4.2 |
| A-05 | 재시도가 서버 내부 루프가 아니라 **클라이언트의 같은 API 재호출**로 이루어진다 | [필수] | spec §4.2 |
| A-06 | 재시도 카운터가 `products.retry_counts`에 보관되고 서버가 이를 근거로 판정한다 | [필수] | spec §4.2, §5.1 |
| A-07 | **상태 폴링을 사용하지 않고**, 각 요청의 응답으로 진행 표시가 갱신된다 | [필수] | spec §4.2, §18.2 |
| A-08 | AI 호출 1회 타임아웃 25초, `maxDuration` 60초가 설정되어 있다 | [필수] | spec §4.2, §4.3 |
| A-09 | 백그라운드 워커·큐·Cron을 도입한 건이 0건이다(API 분할로 대체) | [필수] | spec §18.2 |
| A-10 | 이메일 발송이 `after()` 안에서 실행되고, `after()`를 시간 한도 확장 수단으로 오용한 건이 0건이다 | [필수] | spec §4.2, §13.2 |
| **A-11** | 모델이 **`claude-opus-5`**이고 날짜 접미사가 붙지 않았다 | [필수] | spec §4.3 |
| **A-12** | **금지 파라미터가 0건**이다 — `temperature`·`top_p`·`top_k`·`budget_tokens`·마지막 assistant 턴 prefill·최상위 `output_format` | [필수] | spec §4.3 |
| **A-13** | `thinking`이 `{type:"adaptive"}`이고, `output_config.effort`가 **생성 3종 `medium` / 검증 3종 `low`**로 설정되어 있다 | [필수] | spec §4.3 |
| **A-14** | **SDK 재시도가 `maxRetries: 0`**이고, 요청 타임아웃이 요청 단위로 25초로 덮어써져 있다 | [필수] | spec §4.3 |
| **A-15** | **모든 AI 호출이 `output_config.format`으로 JSON 스키마를 강제**하고, 프롬프트로 "JSON만 출력하라"고 지시한 건이 0건이다 | [필수] | spec §4.3 |
| **A-16** | 스키마의 모든 객체에 `additionalProperties: false`와 `required`가 있고, 지원되지 않는 제약(재귀·수치·문자열 길이)을 스키마에 넣은 건이 0건이다 | [필수] | spec §4.3 |
| **A-17** | **`stop_reason`을 확인한 뒤 `content`를 읽는다.** 무조건 인덱싱해 예외가 발생한 건이 0건이다 | [필수] | spec §4.3 |
| **A-18** | 시스템 프롬프트에 `cache_control`이 배치되고, **날짜·`product_id`·`execution_id`·타임스탬프가 들어간 건이 0건**이다 | [필수] | spec §4.3 |
| **A-19** | `usage.cache_read_input_tokens`가 로그 `output`에 기록되고, 반복 호출에서 0이 아니다 | [필수] | spec §4.3 |
| **A-20** | **생성 라우트 2개(#3·#5)가 실패 시 `409 {reason:"retry"}`를 반환**하고 해당 카운터를 올린다. AI 실패 6종(타임아웃·SDK 오류·`max_tokens`·`refusal`·스키마·서버 검사) 전부가 이 경로를 탄다 | [필수] | spec §4.3, §8.3, §9.5 |
| **A-21** | **모든 라우트의 시작 조건이 §14.5 표와 일치**하며, 조건을 `current_step`의 특정 값으로 거는 라우트가 0건이다 | [필수] | spec §14.5 |
| **A-22** | **§11.6의 재시도 6경로와 §15.3의 재생성 3경로가 실제로 재실행된다.** 시작 조건 미충족으로 거부된 건이 0건이다 | [필수] | spec §11.6, §14.5, §15.3 |
| **A-23** | **모든 `409` 응답에 `reason`이 포함**되고, `reason = precondition`·`stale`을 받은 클라이언트가 재호출하지 않는다 | [필수] | spec §14.6 |
| **A-24** | `reason = retry`의 `retry_from` 값이 §11.6의 복귀 대상 표와 일치하고, 본문에 `items`가 함께 담긴다 | [필수] | spec §14.6, §11.3 |

---

## B. 데이터 저장 무결성

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| B-01 | `products` 1행에 `form_input`·`confirmed_data`·`brochure_content`·`page_content`·`validation_snapshot`이 모두 채워져 있고 전부 유효한 JSON으로 파싱된다 | [필수] | spec §5.1 |
| B-02 | 같은 상품에 대해 `products` 행이 중복 생성되지 않았다(`execution_id` UNIQUE 위반 0건) | [필수] | spec §5.1 |
| B-03 | `form_input`·`confirmed_data`가 **같은 `attempt_no` 안에서** 변경된 흔적이 0건이다 | [필수] | spec §16.2 |
| B-04 | **`products`에 `theme` 컬럼이 없고**, 테마 값이 `page_content.theme`에만 존재한다 | [필수] | spec §5.1 |
| B-05 | `attempt_no`·`current_step`·`retry_counts` 컬럼이 존재하고 갱신된다 | [필수] | spec §5.1 |
| B-06 | 로컬 파일 산출물(`outputs/run-*/`, `brochure.html`, `web/index.html`, `log.html`)을 생성한 코드·흔적이 0건이다 | [필수] | CHANGELOG 부록 A |
| B-07 | `slug`가 다른 상품과 중복되지 않으며, 페이지 생성 이전에는 NULL이다 | [필수] | spec §5.1, §12.1 |
| B-08 | 모든 쓰기 작업에서 `updated_at`이 갱신된다 | [필수] | spec §5.1 |
| **B-09** | `retry_counts`가 **4종 키**(`normalization`·`brochure`·`page`·`consistency`)를 가진다 | [필수] | spec §5.1, §11.6 |
| **B-10** | `abnormality_flags`에 **`attempt_no`·`product_id` 컬럼이 존재**한다 | [필수] | spec §5.5 |
| **B-11** | `execution_logs`·`abnormality_flags`의 `product_id`가 nullable이고, 상품 삭제 시 `NULL`로 설정된다 | [필수] | spec §5.4, §5.5, §12.4 |

---

## C. Step 1 — 웹 폼 입력 · form_input 구조

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| C-01 | **필수 폼 그룹 6개**(행사정보·일정·숙박·상점·가격·식사)의 필드 중 **어느 하나라도** 비어 있으면 프론트엔드에서 제출이 차단되고 해당 필드에 오류가 표시된다 | [필수] | spec §7.1 |
| C-02 | 프론트엔드를 우회해 API를 직접 호출해도 서버가 동일 규칙으로 **400을 반환하고 상품 행을 만들지 않는다** | [필수] | spec §8.1, §16.2 |
| C-03 | **일정 원문 필드가 존재**하고, 일차 구분 예시 placeholder가 표시되며 20~2000자 검증이 적용된다 | [필수] | spec §7.1 |
| C-04 | 행사명이 **40자**를 초과하면 입력이 차단된다(헤드라인 길이 계약과 정합) | [필수] | spec §7.1, §17.1 |
| C-05 | 여행기간이 `시작일 ≤ 종료일`, **최대 15일** 규칙으로 검증된다 | [필수] | spec §7.1 |
| C-06 | **§6.0 용어 구분이 문서·구현에서 일관되게 쓰인다** — "필수 폼 그룹 6"과 "데이터 키 6"이 혼용되지 않는다 | [필수] | spec §6.0 |
| C-07 | `confirmed_data` 최상위 **데이터 키가 6개**(행사정보·숙박·상점·가격·식사·항공편)이며, `일정`이 별도 최상위 키로 만들어진 건이 0건이다 | [필수] | spec §6.0, §6.1 |
| C-08 | `confirmed_data`의 모든 하위 값이 **문자열 또는 문자열 배열**이며 숫자·불리언 타입이 0건이다 | [필수] | spec §6.1 |
| C-09 | `confirmed_data`의 모든 값이 `form_input`에 존재하는 값이거나 §6.3의 분해 결과다(AI 임의 생성 0건) | [필수] | spec §16.1 |
| C-10 | 사용자가 입력하지 않은 필드가 `confirmed_data`에 임의로 추가되어 있지 않다 | [필수] | spec §16.1 |
| C-11 | 정규화 3종이 **규칙별 적용 대상에만** 적용됐다 — 날짜는 `여행기간_시작`·`여행기간_종료` 2개 필드만, 금액은 `가격.성인`·`가격.아동` 2개 필드만, 공백은 전 문자열 필드 | [필수] | spec §6.2 |
| C-12 | **정규화 3종 + 결합 1종 외의 값 변형이 0건**이다 | [필수] | spec §6.2, §6.2.1, §16.1 |
| C-13 | "아동 요금 미운영" 체크 시 `가격.아동`이 문자열 `해당 없음`으로 저장되고, `0`으로 저장되어 "0원"으로 표시되는 건이 0건이다 | [조건] | spec §6.1 |
| **C-14** | **`form_input`이 §7.4 구조를 따른다** — 최상위 키 6개, 점 표기(`가격.성인`), 전부 문자열, 미입력 선택 항목은 빈 문자열, 여행기간은 시작·종료 **두 필드** | [필수] | spec §7.4 |
| **C-15** | `일정원문`·`상점정보`·`식사정보`·`가격.기타`에 **공백 규칙만** 적용됐다. 안의 날짜·금액처럼 보이는 문자열을 손댄 건이 0건이다 | [필수] | spec §6.2 |
| **C-16** | **금액 단위가 `원` 하나로 고정**되어 있다. `USD`·`달러`·`만원`·`₩`가 저장된 건이 0건이고, 폼이 숫자 입력 + 고정 접미사 형태다 | [필수] | spec §6.2 |
| **C-17** | **여행기간 결합**이 `{시작} ~ {종료}`(물결표 앞뒤 공백 1칸) 형식이고, 0차 검증에서 위반으로 판정된 건이 0건이다 | [필수] | spec §6.2.1 |
| **C-18** | **일수 계산이 양끝 포함**(`종료 − 시작 + 1`)이고 `1 ≤ 일수 ≤ 15`이며, 일수를 DB에 저장한 건이 0건이다 | [필수] | spec §6.2.1 |
| **C-19** | `추후 추가 예정` 채움이 **`confirmed_data`에서만** 일어난다. `form_input`에 이 문자열이 들어간 건이 0건이다 | [필수] | spec §7.4, §6.1 |

---

## D. 선택 항목 처리

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| D-01 | 선택 4개(타겟층·여행스타일·숙박일정·항공편)가 입력됐으면 원문대로, 미입력이면 문자열 `추후 추가 예정`으로 채워져 있다 | [필수] | spec §6.1 |
| D-02 | `추후 추가 예정`이 소개서와 상품 페이지 **양쪽에 동일하게** 표기되어 있다(한쪽만 공란·삭제 0건) | [필수] | spec §11.1 |
| D-03 | `추후 추가 예정` 값이 들어간 섹션이 AI 생성 단계에서 삭제되지 않고 유지되어 있다 | [필수] | spec §16.1 |
| D-04 | 선택 항목 미입력을 이유로 파이프라인이 중단·실패 처리되지 않았다 | [필수] | spec §6.1 |
| D-05 | `숙박일정` 미입력 시 여행기간에서 파생 계산하지 않고 `추후 추가 예정`으로 두었다 | [필수] | spec §6.4 |
| D-06 | `여행스타일` select에서 선택한 옵션의 표시 문자열이 그대로 확정 데이터표에 들어갔다 | [필수] | spec §7.2 |

---

## E. 일정 일차 분해 · 숫자 출처 (§6.3·§6.3.1)

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| E-01 | `행사정보.일정원문`에 사용자 입력 원본이 그대로 보존되어 있다(공백 규칙 외 변형 0건) | [필수] | spec §6.3 |
| E-02 | `행사정보.일정` 배열 길이가 **여행기간 일수와 정확히 일치**한다(초과·미달 0일) | [필수] | spec §6.3 |
| E-03 | 각 일차의 `원문근거`가 `일정원문`의 **부분 문자열**이다(공백 정규화 후). 요약·재작성·의역된 건이 0건이다 | [필수] | spec §6.3 |
| E-04 | 각 일차 `내용`에 `원문근거` 밖의 **장소·활동·이동 수단이 0건**이다 | [필수] | spec §6.3, §16.1 |
| E-05 | **판정 3단계가 모두 실행된다**: ① 기계 부분문자열 검사 → ② 기계 명사구 후보 추출·포함 검사 → ③ AI 확인. AI 단독 판정에만 의존한 건이 0건이다 | [필수] | spec §6.3 |
| E-06 | 원문 일차가 여행기간보다 **적을 때** 부족한 일차가 `추후 추가 예정`으로 채워지고 `itinerary_partial` 플래그가 기록되었다 | [조건] | spec §6.3 |
| E-07 | 원문 일차가 여행기간보다 **많을 때** `status = input_error` + `failure_reason`이 기록되고 폼에 사유가 표시되었다 | [조건] | spec §6.3 |
| E-08 | 일차 구분 **6종**(`n일`·`n일차`·`n일 차`·`첫째 날`·`Day n`·`DAY n`) 중 하나도 없는 원문을 **임의 배분하지 않고** `input_error` + 안내를 표시했다. 인식 범위를 임의로 넓히거나 좁힌 건이 0건이다 | [조건] | spec §6.3 |
| E-09 | `itinerary_decomposed`·`normalization_validated` 로그가 `category = pipeline`으로 기록되어 있다 | [필수] | spec §5.4, §8.2 |
| **E-10** | **숫자 출처 판정 4단계가 실행된다** — 기계 토큰 추출 → 허용 출처 4종 포함 검사 → 위반 후보 표시 → AI 확인 | [필수] | spec §6.3.1 |
| **E-11** | 허용 출처 4종(`form_input` 전체 · 같은 일차 `원문근거` · **일차 번호** · **여행기간 일수와 일수−1**)이 정확히 적용됐다. 일차 번호나 `3박4일`을 위반으로 판정한 건이 0건이다 | [필수] | spec §6.3.1 |
| **E-12** | 판정 대상이 **서술 필드**(`핵심일정`·`days[].text`·`안내문구`·삽입 블록 `본문`)로 한정된다. 값 필드를 이 검사로 판정한 건이 0건이다 | [필수] | spec §6.3.1 |

---

## F. 이미지

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| F-01 | 업로드된 이미지가 **사용자가 지정한 슬롯**에 그대로 저장되어 있고, AI가 슬롯을 재배치한 건이 0건이다 | [필수] | spec §7.3, §16.1 |
| F-02 | **슬롯이 4종**이고 개수 상한(`hero` 1 / `accommodation` 3 / `itinerary_day_{n}` 일차별 1 / `shop` 2)이 강제되며, 장당 5MB 제한이 적용된다. **`gallery` 슬롯이 존재하지 않는다** | [필수] | spec §7.3 |
| F-03 | `itinerary_day_{n}` 슬롯이 **여행 일수만큼만** 생성되고, 여행기간 축소 시 사라진 슬롯의 이미지 삭제를 사용자에게 확인한다(확인 없는 삭제 0건) | [필수] | spec §7.3 |
| F-04 | 허용 포맷(JPG/PNG/WebP) 외 파일 업로드가 거부된다 | [필수] | spec §7.3 |
| F-05 | 모든 이미지에 `alt`가 비어 있지 않다(미입력 시 자동 채움) | [필수] | spec §7.3, §17.2 |
| F-06 | **이미지 업로드가 `/new` 폼에서만 가능**하고, 편집기에서 새 업로드를 수행한 건이 0건이다 | [필수] | spec §7.3, §10.2 |
| F-07 | `hero` 미업로드 시 테마 그라디언트로 폴백하고, 그 외 슬롯은 이미지 영역만 생략하며 **섹션 자체는 유지**한다 | [조건] | spec §7.3, §9.3 |
| F-08 | 375px에서 이미지 가로 넘침·비율 깨짐이 0건이다(고정 종횡비 + `object-fit: cover` + `max-width: 100%`) | [필수] | spec §7.3, §17.1 |
| F-09 | `hero`가 우선 로딩(`priority`)이고 **나머지 전 이미지가 지연 로딩(`loading="lazy"`)**이다 | [필수] | spec §7.3, §17.2 |
| F-10 | 이미지 업로드 폼에 저작권·초상권 고지 문구가 표시된다 | [필수] | spec §7.3 |
| F-11 | Storage 경로가 `{product_id}/{uuid}.{ext}` 형식이고 추측 가능한 순번 경로가 0건이다 | [필수] | spec §5.2, §7.3 |
| **F-12** | 절대 상한이 **21장**(15일 여행 전 슬롯 만재 `1+3+15+2`)으로 계산된다 | [필수] | spec §7.3 |
| **F-13** | Storage 업로드 실패 시 **트랜잭션을 커밋하지 않고 400을 반환**한다. 행만 만들어지고 이미지가 없는 상품이 0건이다 | [조건] | spec §16.1.1 |

---

## G. Step 2 — 소개서(간단버전)

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| G-01 | `brochure_content`에 **8개 섹션이 §8.7 표의 순서·`id`·`type`대로** 존재하고 임의 추가 섹션이 0건이다 | [필수] | spec §8.7 |
| **G-01b** | **각 섹션의 `data` 키가 §8.7 표와 정확히 일치**한다. 키를 임의로 추가·변경·누락한 건이 0건이다 | [필수] | spec §8.7 |
| **G-01c** | 소개서 섹션에 `visible`·`locked`·`order`·이미지·테마·신청 폼 필드가 들어간 건이 **0건**이다 | [필수] | spec §8.7 |
| G-02 | **필수 폼 그룹 6개**의 값이 소개서에 **전부** 반영되어 있고, 필수 요소를 제외한 섹션 구성이 0건이다 | [필수] | spec §8.7 |
| G-03 | 입력된 선택 항목이 소개서에 반영되어 있고, 미입력 항목은 섹션을 유지한 채 `추후 추가 예정`으로 표기된다 | [필수] | spec §6.1, §8.7 |
| G-04 | 각 필드에 `source` 경로가 기록되어 있고, 출처 없는 연결 문구(`핵심일정`)는 `"generated"`로 표기되어 있다 | [필수] | spec §8.7, §8.8 |
| G-05 | 미치환 중괄호 토큰(`{{...}}`)·조사 파이프 기호가 **0개**이고, 다른 키의 값이 들어간 오치환이 0건이다 | [필수] | spec §11.2 |
| **G-05b** | **사실정보 값 필드에 조사가 붙은 건이 0건**이다(`디럭스룸` → `디럭스룸으로` 금지). 조사 적용은 `source = "generated"` 서술 필드에만 나타난다 | [필수] | spec §8.8 |
| G-06 | 전체가 존댓말로 통일되고, 어투 조정으로 사실 정보가 변경된 건이 **0건**이다 | [필수] | tonal-manner-apply |
| G-07 | 일정 섹션이 **일차별 1~2문장**이고 일차 수가 여행기간과 정확히 일치한다 | [필수] | spec §8.6, §8.7 |
| G-08 | 소개서에 **이미지·테마가 적용되지 않고** 단일 컬럼 텍스트로 렌더링된다 | [필수] | spec §8.6 |
| G-09 | 소개서를 HTML 파일로 생성하지 않고 `brochure_content` jsonb + 고정 컴포넌트로 렌더링한다 | [필수] | spec §8.8 |
| G-10 | 검토 화면(`/admin/products/{id}`)에 1차 검증 결과와 **실패 항목 전체**가 표시된다 | [필수] | spec §8.9, §14.1 |
| G-11 | 1차 통과 시 `status = brochure_ready` + `axis_1 = pass`가 되고, 관리 목록의 `소개서 완료` 필터로 조회된다 | [필수] | spec §8.4, §14.1 |
| G-12 | **1차 재시도 소진 시 `input_error`가 아니라 `brochure_ready` + `axis_1 = fail`**이 되고, 검토 화면에 진입해 실패 항목을 볼 수 있다 | [조건] | spec §8.4, §15.1 |
| G-13 | `axis_1 = fail`일 때 **[상품 생성]이 잠기고** [다시 생성]·[입력 수정]이 제공된다 | [조건] | spec §8.9, §15.1 |
| **G-14** | **소개서 생성 실패 시 `409 {reason:"retry", retry_from:3}`**을 반환하고 `retry_counts.brochure`를 올린다. 소진 시 `axis_1 = fail` + `items` 1건(`검증영역: "생성"`)이 기록된다 | [조건] | spec §8.3 |
| **G-15** | 생성 후 **서버 검사**(섹션 8개·순서, `source` 누락 0건, 미치환 토큰 0건, 길이 계약 4종)가 실행된다 | [필수] | spec §8.3 |

---

## H. Step 3 — 상품 페이지 콘텐츠

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| H-01 | `page_content`에 **9개 섹션이 §9.3 표의 `order`·`id`·`type`대로** 존재한다 | [필수] | spec §9.3 |
| **H-01b** | **각 섹션의 `data` 키가 §9.3 표와 정확히 일치**한다. 키를 임의로 추가·변경·누락한 건이 0건이다 | [필수] | spec §9.3 |
| **H-01c** | 모든 섹션이 `{id, type, order, visible, locked, data, source}` **7개 최상위 필드**를 가진다 | [필수] | spec §9.2, §9.3 |
| H-02 | 각 섹션에 §9.3 표의 필수 내용이 반영되어 있고, 항공편 미입력 시 `flight` 섹션이 유지되며 `추후 추가 예정`으로 표기된다 | [필수] | spec §9.3 |
| H-03 | 모든 사실정보 필드에 `source` 경로가 기록되어 있다 | [필수] | spec §9.3 |
| **H-03b** | `image_slot`·`image_slots`에 `source`를 붙인 건이 **0건**이다(사실정보가 아니라 이미지 참조) | [필수] | spec §9.3 |
| H-04 | **`itinerary` 섹션이 `data.days[].image_slot`으로 일차 이미지를 참조**하고, 그 값이 업로드 시 지정 슬롯과 일치한다 | [필수] | spec §9.3, §11.2 |
| **H-04b** | **`accommodation`·`shop`이 `image_slots`(문자열 배열)를 쓰고**, `hero`·`itinerary.days[]`가 `image_slot`(문자열)을 쓴다. 단수·복수를 혼용한 건이 0건이다 | [필수] | spec §9.3 |
| H-05 | `hero`와 `apply`가 `locked: true`로 설정되어 있고, 나머지 7개는 `false`다 | [필수] | spec §9.3 |
| H-06 | AI가 HTML을 생성하지 않고 **`page_content` JSON만 생성**했으며 렌더링은 고정 React 컴포넌트가 담당한다 | [필수] | spec §9.1 |
| H-07 | `theme`이 §9.4 매핑표(6종 + `default`)대로 결정되고, 값이 비었거나 `추후 추가 예정`이면 `default`이며, **편집기에서 변경할 수 없다** | [필수] | spec §9.4, §10.2 |
| H-08 | 테마 적용 범위가 **컬러 스킴·헤드라인 톤·강조 포인트 3가지로 한정**되고, 테마로 인한 섹션·문구·사실 정보 변경이 0건이다 | [필수] | spec §9.4 |
| H-09 | `hero.headline`이 40자 이내이고 **행사명을 잘라낸 건이 0건**이다 | [필수] | spec §17.1, §16.1 |
| H-10 | 미치환 토큰·조사 파이프 기호가 **0개**다 | [필수] | spec §11.2 |
| H-11 | **생성 단계에서 길이 계약 4종**(헤드라인 40 / 부제 80 / 일차 서술 200 / 섹션 제목 30)이 강제된다. `free_text`·`notice`를 생성 시점에 검사한 건이 **0건**이다 | [필수] | spec §17.1 |
| H-12 | slug가 §9.5 ①(페이지 생성) 시점에 발급되고, **이미 있으면 재발급하지 않는다** | [필수] | spec §12.1 |
| **H-13** | `sec_apply`의 **신청 폼 필드 구성(이름·이메일·연락처·인원수·동의)이 `data`에 들어간 건이 0건**이다 | [필수] | spec §9.3, §13.1 |
| **H-14** | **페이지 생성 실패 소진 시 `draft`가 아니라 `brochure_ready`로 되돌린다.** `page_content` 없는 `draft`가 0건이다 | [조건] | spec §9.5 |
| **H-15** | 생성 후 **서버 검사**(섹션 9개·`order` 1~9, `source` 누락 0건, 미치환 토큰 0건, 길이 계약 4종, `image_slot`·`image_slots`가 `product_images`에 존재하는 슬롯인지)가 실행된다 | [필수] | spec §9.5 |
| **H-16** | **[상품 생성] 버튼이 `page`·`consistency` 카운터를 0으로 초기화**한다. 소진 상태에서 다시 눌러 즉시 실패한 건이 0건이다 | [필수] | spec §9.5, §11.6 |

---

## I. 검증 4축 (§11)

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| I-01 | **0차**(`form_input` vs `confirmed_data`)가 실행되어 정규화 3종 + 결합 1종 외 변형 0건과 일차 분해 근거를 판정했다 | [필수] | spec §11.1 |
| I-02 | 0차 검증이 **추가 AI 호출 없이** §8.2 ② 요청 안에서 처리되었다(3단계 판정의 AI 부분은 `itinerary-decomposition`의 호출 1회 안) | [필수] | spec §11.1, §4.2 |
| I-03 | **1차**(`form_input` vs `brochure_content`)가 소개서 8개 섹션 전 필드를 대조했다 | [필수] | spec §11.1 |
| I-04 | **2차**(`form_input` vs `page_content`)가 페이지 9개 섹션 전 필드를 대조했다 | [필수] | spec §11.1 |
| I-05 | **1·2차의 기준값이 `confirmed_data`가 아니라 `form_input`**이다 | [필수] | spec §11.1 |
| I-06 | **2차 검증이 실패한 경우 3차 검증 API가 호출되지 않았다** | [조건] | spec §11.1, §9.5 |
| I-07 | **3차**가 §11.1 대응표 기준으로 **`apply`를 제외한 전 섹션**을 대조하고 `apply`만 `skipped`로 기록했다 | [필수] | spec §11.1 |
| I-08 | 3차의 일정 행 대조 대상이 **일차 수와 사실정보 값**이며, **`원문근거`를 대조한 건이 0건**이다 | [필수] | spec §11.1 |
| I-09 | **서술 분량·어순·문장 수 차이만을 이유로** 3차를 실패 판정한 건이 **0건**이다 | [필수] | spec §11.1 |
| I-10 | **정규화 3종에 의한 표기 차이**(콤마 제거·날짜 형식)가 실패로 판정된 건이 0건이다 | [필수] | spec §11.2 |
| I-11 | 허용 차이가 §11.2의 **5종**(공백·HTML 이스케이프·정규화 표기·**결합 필드 차이**·서술 문장 차이)으로 한정되고, 값의 임의 재표기·요약·윤문·부분 삭제·어순 변경·약칭·영문 변환은 실패로 판정된다 | [필수] | spec §11.2 |
| I-12 | 선택 항목 미입력 시 양쪽 모두 `추후 추가 예정`이며, 한쪽만 공란·섹션 삭제인 경우 실패로 판정했다 | [필수] | spec §11.1 |
| I-13 | `apply` 섹션 내부의 가격·행사 정보가 `price`·`hero`와 일치한다(2차가 판정) | [필수] | spec §11.1, §11.2 |
| I-14 | 서술 필드의 숫자 토큰이 **허용 출처 4종 중 하나에 존재**하고, 입력에 없는 지명·시설·경유지·관광지·부대시설 추가가 **0건**이다 | [필수] | spec §6.3.1, §11.2, §16.1 |
| I-15 | `validation_snapshot`에 `attempt_no`·`verdict`·`validated_at`·`content_hash`·`axes`(**4축**)가 기록되고, 미실행 축이 `null`이며 `verdict` 계산에서 제외된다 | [필수] | spec §11.3 |
| I-16 | 최상위 `verdict`가 **완료된 전 축 통과 시에만** `pass`이고, 한 축이라도 `fail`이면 `fail`이다 | [필수] | spec §11.3 |
| I-17 | 각 축 결과가 같은 `attempt_no` 안에서 변경되지 않고, **편집으로 변경되지 않으며**, `attempt_no` 상승 시에만 갱신된다 | [필수] | spec §11.4 |
| I-18 | validator가 재시도 여부를 판단하지 않고 판정·`items`만 반환했다 | [필수] | validator-agent |
| I-19 | 이미지-텍스트 **내용** 정합성을 검증 실패 사유로 판정한 건이 0건이다(검증 대상 아님) | [필수] | spec §7.3, §18.2 |
| **I-20** | **`items`가 §11.3의 6필드**(`검증영역`·`source경로`·`기준값`·`발견값`·`사유`·`위치`)를 가진다 | [필수] | spec §11.3 |
| **I-21** | **실패 항목을 전부 담는다.** 첫 실패에서 멈추거나 개수 상한으로 잘라낸 건이 0건이다 | [필수] | spec §11.3 |
| **I-22** | `content_hash`가 **키 사전순 정렬 + 공백 없는 직렬화** 후 SHA-256으로 계산되고, `sha256:` 접두사를 가진다 | [필수] | spec §11.3 |
| **I-23** | `content_hash`가 현재 해시와 다를 때 관리 화면 상세에 **"검증 이후 편집됨"**이 표시된다 | [조건] | spec §11.3, §14.1 |

---

## J. Step 4 — 편집기

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| J-01 | 섹션 **수정·삭제·삽입·순서변경**이 모두 동작하고 저장 후 미리보기에 반영된다 | [필수] | spec §10.2 |
| J-02 | `hero`·`apply` 섹션 삭제 시도가 차단된다 | [필수] | spec §10.2 |
| J-03 | **`apply` 섹션의 신청 폼 필드 구성을 편집할 수 없고**, 안내 문구·제목만 편집된다 | [필수] | spec §10.2, §13.1 |
| J-04 | 삭제가 `visible: false` 전환으로 처리되어 데이터가 보존된다 | [필수] | spec §10.2 |
| J-05 | 삽입 가능 블록이 `free_text`·`image`·`notice` 3종으로 제한된다 | [필수] | spec §10.2 |
| **J-05b** | 삽입 블록이 **§10.2 스키마를 따른다** — `blk_` 접두사, `data` 키, `locked: false`, `source`(`image`는 없음). `image` 블록이 **`image_id`로 `product_images.id`를 직접 참조**한다 | [필수] | spec §10.2 |
| **J-05c** | 삽입 블록이 **`hero`와 `apply` 사이에만** 삽입되고, 저장 시 전 섹션 `order`가 1부터 다시 번호 매겨진다 | [필수] | spec §10.2 |
| J-06 | **`image` 블록이 이미 업로드된 이미지 중에서만 선택**되고, 새 업로드 경로가 0건이다 | [필수] | spec §10.2, §7.3 |
| J-07 | 순서변경 시 `hero`가 항상 첫 번째, `apply`가 항상 마지막으로 유지된다 | [필수] | spec §10.2 |
| J-08 | **테마를 편집기에서 변경할 수 없다** | [필수] | spec §10.2, §9.4 |
| J-09 | **편집 저장에서 길이 계약 6종 전부**를 검사하고 초과 시 400으로 거부한다(`free_text` 500 · `notice` 300 포함) | [필수] | spec §10.2, §17.1 |
| J-10 | 저장 시 `human_edited = true`가 되고, `draft`였으면 `reviewing`으로 전이하며 `published`/`unpublished`/`reviewing`이면 유지된다 | [필수] | spec §10.3 |
| J-11 | 변경된 섹션마다 `edit_history`에 `{action, section_id, before, after}`가 기록된다 | [필수] | spec §10.3 |
| J-12 | `content_edited` 로그가 `category = lifecycle`로 기록된다 | [필수] | spec §5.4, §10.3 |
| J-13 | 편집으로 `form_input`·`confirmed_data`·`validation_snapshot`·**`attempt_no`**가 변경되지 않았다 | [필수] | spec §10.3 |
| J-14 | 편집 후 **AI 재검증이 실행되지 않는다**(사람 책임 분리) | [필수] | spec §10.4 |
| J-15 | **검증 배지와 편집 배지가 2축으로 독립 표시**되어, `AI 검증 실패` + `사람 편집됨`이 동시에 보인다 | [필수] | spec §10.4 |
| J-16 | 검증 배지 3종이 상호 배타적이고 §10.4의 판정 순서를 따른다(조건 중첩 0건) | [필수] | spec §10.4 |
| J-17 | **`verdict = fail`인 상품도 편집기에 진입할 수 있다** | [필수] | spec §11.5, §15.1 |
| J-18 | 미리보기에서 **375 / 768 / 1280** 뷰포트 전환이 동작하고 세 폭 모두 가로 스크롤·요소 겹침·콘텐츠 잘림이 0건이다 | [필수] | spec §17.1 |
| J-19 | 임시저장 페이지 미리보기가 **편집기 안에서만** 가능하고, 인증 없이 접근 가능한 미리보기 경로가 0건이다 | [필수] | spec §4.1, §16.2 |
| J-20 | `draft`/`reviewing`에서 slug 편집 필드가 제공되고 허용 문자·중복 검사가 적용되며 `slug_changed` 로그가 남는다 | [필수] | spec §10.1, §12.1 |
| J-21 | `published` 상태 상품을 편집기에서 열면 "게시 중 — 저장 시 즉시 반영" 경고가 표시된다 | [조건] | spec §15.2 |
| **J-22** | 편집 라우트의 시작 조건이 **`page_content` 존재**를 요구한다. `page_content` 없는 상품이 편집기에 진입한 건이 0건이다 | [필수] | spec §14.5 |
| **J-23** | **삽입 블록이 2·3차 검증 대상에 포함된 건이 0건**이다(검증은 편집 이전 시점) | [필수] | spec §10.2 |

---

## K. Step 5 — 게시 및 URL

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| K-01 | 상품마다 서로 다른 고유 URL(`/p/{slug}`)을 가진다 | [필수] | spec §4.1, §12.1 |
| K-02 | slug가 **영문 소문자·숫자·하이픈으로만** 구성되고 한글·공백·특수문자가 0건이며 **percent-encoding이 발생하지 않는다** | [필수] | spec §12.1 |
| K-03 | 행사명에 한글이 포함되면 **로마자 변환을 시도하지 않고** `p-{6자 base36}`이 발급된다 | [필수] | spec §12.1, §16.1 |
| K-04 | 중복 시 `-2`·`-3` 접미사가 부여된다(자동 발급). 기획자 수동 입력이 중복이면 **`409 slug_conflict`**를 반환한다 | [필수] | spec §12.1, §14.6 |
| K-05 | 게시 후 **인증 없이 인터넷에서** 해당 URL에 접속해 페이지 전체를 볼 수 있다(실제 배포) | [필수] | spec §4.1 |
| K-06 | **`published`가 아닌 6개 상태 전부**(`generating`·`input_error`·`brochure_ready`·`draft`·`reviewing`·`unpublished`)의 `/p/{slug}`가 **404**를 반환하고, 인증 없이 임시저장 내용을 볼 수 있는 경로가 **0건**이다 | [필수] | spec §4.1, §15.1 |
| K-07 | `verdict = pass`이면 게시 버튼이 활성이고, `human_edited = true`인 경우 확인 모달에 "편집된 내용은 AI 검증 대상이 아닙니다"가 표시된다 | [조건] | spec §11.5 |
| K-08 | `verdict = fail`이면 게시 버튼이 **기본 비활성**이고, 실패 항목 전체 열람 + "책임하에 게시합니다" 체크 후에만 게시된다 | [조건] | spec §11.5 |
| K-09 | 책임 게시 시 `publish_override_at`과 `publish_override` 로그가 기록되고 `검증 실패 · 책임 게시됨` 배지가 표시된다 | [조건] | spec §11.5, §10.4 |
| K-10 | **`status = input_error`인 상품에는 게시 경로가 없다** | [필수] | spec §11.5, §15.1 |
| K-11 | slug 변경이 `draft`/`reviewing`에서만 가능하고, **게시 후에는 변경할 수 없다**(행사명을 고쳐도 URL 유지) | [필수] | spec §12.1 |
| K-12 | 게시 시 `published_at`이 최초 게시 시각으로만 기록되고 **재게시 시 덮어써지지 않는다** | [필수] | spec §12.2 |
| K-13 | 게시가 재빌드 대기 없이 즉시 반영된다 | [필수] | spec §4.1, §12.2 |
| K-14 | 게시 중단 시 URL이 즉시 404가 되고, **기존 신청 데이터가 삭제되지 않으며**, 신청 폼이 표시되지 않는다 | [필수] | spec §12.3 |
| K-15 | **사용자의 명시적 게시 버튼 조작 없이 자동으로 게시된 건이 0건**이다 | [필수] | CHANGELOG 부록 A |

---

## L. Step 6 — 신청

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| L-01 | 신청 폼에 이름·이메일·연락처·인원수 4개 필드와 동의 체크박스가 있고 §13.1 validation이 적용된다 | [필수] | spec §13.1 |
| L-02 | **동의 체크박스 미체크 시 제출 버튼이 비활성**되고, API를 직접 호출해도 서버가 거부한다 | [필수] | spec §13.1, §16.2 |
| L-03 | 동의 영역에 수집 항목·수집 목적·**보유 기간(신청 접수일로부터 1년)**이 표시된다 | [필수] | spec §13.1 |
| L-04 | 신청이 `applications`에 적재되고 `consent_at`이 NULL이 아니다 | [필수] | spec §13.2 |
| L-05 | `status != published`인 상품에 대한 신청이 **409 `{reason:"product_not_published"}`**로 거부된다 | [필수] | spec §13.2, §14.6 |
| L-06 | **`application_received` 로그가 이메일 발송보다 먼저 기록된다**(발송 실패 시에도 신청 기록이 남는다) | [필수] | spec §13.2 |
| L-07 | 신청 완료 응답이 **이메일 발송을 기다리지 않고** 즉시 반환된다 | [필수] | spec §13.2 |
| L-08 | `product_snapshot`에 **신청 시점** 상품 정보(행사명·여행기간·숙소·가격·상품 URL)가 저장된다 | [필수] | spec §13.2 |
| L-09 | 신청 버튼이 375px에서 겹침·잘림 없이 조작 가능하다 | [필수] | spec §17.1 |
| L-10 | 신청 동작이 상품 `status`를 변경하지 않는다 | [필수] | spec §15.2 |

---

## M. 자동 이메일

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| M-01 | 신청자 이메일 주소로 상품 정보 이메일이 **실제 수신**된다 | [필수] | spec §13.3 |
| M-02 | 발송이 **`after()`(또는 `waitUntil`) 안에서 실행**되어 응답 반환 후에도 실제로 발송된다 | [필수] | spec §13.2 |
| M-03 | 본문 값이 **`product_snapshot`** 기준이며 현재 상품 값을 다시 읽은 건이 0건이다 | [필수] | spec §13.3 |
| M-04 | 본문에 신청자명·상품명·여행지·여행기간·숙소·성인·아동 가격·신청 인원수·상품 URL·문의 안내가 모두 포함된다 | [필수] | spec §13.3 |
| M-05 | **총액 계산 표기(인원수 × 가격)가 0건**이다 | [필수] | spec §13.3, §16.1 |
| M-06 | 본문에 **상품 마감·중단 시 링크 안내 문구**가 포함된다 | [필수] | spec §13.3 |
| M-07 | 발송 성공 시 `email_status = 'sent'`, 실패 시 `'failed'` + `email_error`가 기록된다 | [필수] | spec §13.2 |
| M-08 | **이메일 발송 실패가 신청 접수를 실패시키지 않는다** | [필수] | spec §13.2, §16.2 |
| M-09 | 관리 페이지에서 **[재발송]**이 동작하고 `email_resent` 로그가 기록된다 | [조건] | spec §13.3 |
| **M-10** | 상품 URL이 **`{SITE_URL}/p/{slug}` 절대 URL**이고, 문의 안내가 환경 변수 `CONTACT_INFO`에서 온다 | [필수] | spec §4, §13.3 |

---

## N. 상태 · 화면 — §15.1 단일 기준표 대조

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| N-01 | **§15.1 표의 10개 행이 실제 화면·버튼과 정확히 일치**한다. 표에 없는 상태·버튼 조합이 0건이다 | [필수] | spec §15.1 |
| N-02 | 상태값이 **7종**이며 §15.2에 정의되지 않은 상태·전이가 0건이다 | [필수] | spec §5.1, §15.2 |
| N-03 | **`input_error`가 입력 문제(폼 재검증·일차 분해) 또는 0차 검증 소진에서만 발생**하고, **1·2·3차 검증 소진이나 생성 실패로 발생한 건이 0건**이다 | [필수] | spec §15.1, §15.2 |
| N-03b | 0차 검증 재시도 소진 시 `input_error` + `failure_reason` + `pipeline_aborted`가 기록되고 `/new?product_id=`로 복귀한다 | [조건] | spec §8.2, §15.2 |
| N-04 | `generating` 상태에서 진행 표시 화면이 나오고 **[이어서 진행]·[처음부터 다시] 2개 버튼**이 제공된다 | [필수] | spec §15.1, §15.1.1 |
| N-05 | `input_error`에서 **`/new?product_id={id}`가 입력값을 채운 채** 열리고 `failure_reason`이 표시되며 [입력 수정 후 재제출]이 제공된다 | [조건] | spec §14.1, §15.1 |
| N-06 | `brochure_ready` + `pass`에서 [상품 생성]·[다시 생성]·[입력 수정] 3개가 제공된다 | [필수] | spec §15.1 |
| N-07 | `draft` + `fail`에서 [편집]·[다시 생성]·[책임 게시]가 제공된다 | [조건] | spec §15.1 |
| N-08 | **`reviewing`·`published`·`unpublished`에서 [다시 생성]이 제공되지 않는다**(사람 편집 소실 방지) | [필수] | spec §15.1 |
| N-09 | 상태 전이가 §15.2 표대로 이루어졌다 | [필수] | spec §15.2 |
| N-10 | `input_error` 재제출 시 `attempt_no` +1, 카운터 4종 0, 축 4개 폐기, §8.2부터 재실행되었다 | [조건] | spec §15.2, §14.4 |
| **N-11** | **§15.1의 도달 화면 5종이 §14.1의 경로와 1:1로 대응**한다. 대응 경로가 없는 도달 화면이 0건이다 | [필수] | spec §14.1, §15.1 |
| **N-12** | **[이어서 진행]이 `current_step` → 다음 호출 대상 표(6행)대로** 재개 지점을 판단한다 | [조건] | spec §15.1.1 |
| **N-13** | [이어서 진행]이 `409 precondition`을 받으면 **[처음부터 다시]** 경로가 제공된다 | [조건] | spec §15.1.1 |
| **N-14** | **재제출 라우트 #17이 `form_input`을 교체할 수 있는 유일한 경로**다. 다른 라우트가 `form_input`을 수정한 건이 0건이다 | [필수] | spec §14.4 |
| **N-15** | #17이 `input_error`에서만 허용되고, 다른 상태에서 호출 시 `409 precondition`을 반환한다 | [조건] | spec §14.5 |
| **N-16** | **페이지 생성 소진 시 `brochure_ready` + `axis_2 = fail`**로 전이한다(§15.2 전이표) | [조건] | spec §9.5, §15.2 |

---

## O. [다시 생성] · [처음부터 다시] (§15.3)

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| O-01 | `POST /api/products/{id}/regenerate`가 존재하고 AI를 호출하지 않는다 | [필수] | spec §14.4, §15.3 |
| O-02 | [다시 생성]이 **`brochure_ready`·`draft`에서만**, [처음부터 다시]가 **`generating`에서만** 제공된다 | [필수] | spec §15.1, §15.3 |
| O-03 | 실행 시 `attempt_no` +1, `retry_counts` **4종 전부 0**, `status = generating`으로 전이하고 `regenerate_requested` 로그가 남는다 | [조건] | spec §15.3 |
| O-04 | 재실행 시작점이 `brochure_ready`에서는 §8.3 ③, `draft`에서는 §9.5 ①, `generating`에서는 축 통과 여부로 결정된다 | [조건] | spec §15.3 |
| O-05 | **`form_input`·`confirmed_data`·업로드 이미지가 보존**된다 | [조건] | spec §15.3 |
| O-06 | 이전 `attempt`의 폐기된 축이 `execution_logs`에는 남아 추적 가능하다 | [조건] | spec §15.3, §11.4 |
| O-07 | `human_edited = true`인 상품에 [다시 생성]을 누르면 **"편집한 내용이 사라집니다" 확인**을 받는다 | [조건] | spec §15.3 |
| **O-08** | **`current_step`이 §15.3 되돌림 표의 값으로 되돌려진다**(`brochure_ready` → `normalization_validated`, `draft` → `validation_1_completed`) | [조건] | spec §15.3 |
| **O-09** | **시작점 이전 축이 보존된다.** `validation_snapshot`을 통째로 비워 시작 조건을 충족하지 못한 건이 0건이다 | [조건] | spec §15.3 |
| **O-10** | **`slug`가 재발급·폐기되지 않는다.** [다시 생성]·검증 재시도에서 slug가 바뀐 건이 0건이다 | [조건] | spec §12.1, §15.3 |
| **O-11** | 폐기한 축이 `null`로 설정되고 최상위 `verdict` 계산에서 제외된다 | [조건] | spec §11.3, §15.3 |

---

## P. 관리 페이지 및 인증

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| P-01 | 비밀번호 없이 `/admin/*`·`/new`에 접근하면 로그인으로 리다이렉트된다 | [필수] | spec §14.2 |
| P-02 | **`POST /api/applications`를 제외한 모든 API**가 인증 없이 호출되면 거부된다 | [필수] | spec §14.2, §14.4 |
| P-03 | 세션 쿠키가 `httpOnly` + `Secure` + `SameSite=Lax`이고 HMAC 서명되어 있다 | [필수] | spec §14.2 |
| P-04 | 비밀번호 비교가 상수 시간으로 이루어지고 IP당 분당 5회 제한이 적용된다 | [필수] | spec §14.2 |
| P-05 | **관리 페이지 1곳**에서 **7개 상태 전부**를 필터로 조회할 수 있고 상태·URL·검증 배지·상태별 버튼을 확인할 수 있다 | [필수] | spec §14.1 |
| P-06 | 신청 내역과 이메일 발송 상태를 관리 페이지에서 확인할 수 있다 | [필수] | spec §14.1 |
| P-07 | 신청 내역의 연락처가 기본 마스킹되고 명시적 조작 시에만 전체가 표시된다 | [필수] | spec §13.1 |
| P-08 | **환경 변수 8개**(`ANTHROPIC_API_KEY`·`SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY`·`RESEND_API_KEY`·`ADMIN_PASSWORD`·`SESSION_SECRET`·`SITE_URL`·`CONTACT_INFO`)가 클라이언트 번들에 노출되지 않고, `NEXT_PUBLIC_` 변수가 0건이다 | [필수] | spec §4, §16.3 |
| P-09 | §14.4 표의 **19개 엔드포인트**가 표에 명시된 메서드·인증·AI 호출 수·응답 코드대로 동작한다 | [필수] | spec §14.4 |

---

## Q. 실행 로그

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| Q-01 | 로그가 `execution_logs`에 단계당 1행씩 append되고 기존 행을 덮어쓴 건이 0건이다 | [필수] | spec §14.3 |
| Q-02 | `/admin/logs/{execution_id}` **단일 화면**에서 전체 이력과 이상 플래그를 모두 확인할 수 있다(단계별 화면 분리 0건) | [필수] | spec §14.3 |
| Q-03 | 화면이 `category` 탭(파이프라인 / 상태변경 / 신청·메일)으로 구분되고 **파이프라인 탭에 신청 로그가 섞이지 않는다** | [필수] | spec §5.4, §14.3 |
| Q-04 | 표 컬럼 순서가 `타임스탬프 → 시도 → 재시도 → 단계명 → 판정 → 상태 → 입력 → 출력`이며 컬럼 추가·누락이 0건이다 | [필수] | spec §14.3 |
| Q-05 | **`attempt_no`와 `retry_index`가 모두 표시되어 시도 회차와 재시도 회차가 구분된다** | [필수] | spec §5.4, §14.3 |
| Q-06 | §5.4 표의 `pipeline` **11개 단계**가 표에 명시된 기록 시점에 **성공·실패 모두** 기록된다 | [필수] | spec §5.4 |
| Q-07 | `lifecycle` 6종(`content_edited`·`slug_changed`·`published`·`unpublished`·`publish_override`·`product_deleted`)과 `application` 4종이 발생 시 기록된다 | [조건] | spec §5.4 |
| Q-08 | 입력·출력이 원본 JSON으로 표시되고, 타임스탬프는 UTC ISO 8601, 빈 값은 `-`이며 기록값이 실제 산출물과 일치한다 | [필수] | spec §14.3 |
| **Q-09** | **`verdict` 저장값이 `pass`/`fail`/`-` 영어**이고, 화면 표시만 `통과`/`반려`/`-`다. 표시 문자열을 DB에 저장한 건이 0건이다 | [필수] | spec §5.4, §14.3 |
| **Q-10** | **`category = application` 행의 `input`·`output`에 이름·이메일·연락처 원본이 노출된 건이 0건**이다(저장 시점 마스킹) | [필수] | spec §5.4 |
| **Q-11** | **`draft`에 도달한 모든 경로에서 `draft_registered`가 기록된다** — Step 07 통과·소진, 그리고 **2차 소진 시 Step 06**이 담당하는 경로 | [필수] | spec §9.5 |
| **Q-12** | AI 호출 단계의 `output`에 `{stop_reason, usage, error_type}`이 포함되고 `usage.cache_read_input_tokens`가 기록된다 | [필수] | spec §4.3 |
| **Q-13** | **로그 기록 실패가 본 동작을 실패시키지 않는다.** 로그 실패로 게시·신청이 되돌려진 건이 0건이다 | [필수] | spec §16.1.1 |
| **Q-14** | `form_input_resubmitted` 로그가 재제출 시 기록된다 | [조건] | spec §5.4, §14.4 |

---

## R. 이상 플래그

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| R-01 | 플래그가 `abnormality_flags`에 기록되고 로그 화면 하단에 함께 표시된다 | [필수] | spec §5.5, §14.3 |
| R-02 | `abnormality-detection`이 `execution-log-collection` **다음에** 실행되었다(순서 역전 0건) | [필수] | abnormality-detection |
| R-03 | **감지된 경우에만** 플래그가 추가되어 있고 "이상 없음" 류 항목이 0건이며, 여러 이상이 감지되면 유형별로 각각 1행이다 | [필수] | spec §5.5 |
| R-04 | **§5.5 감지 조건표의 5종이 정의된 조건대로** 기록된다 — `retry_accumulated`(카운터 2 도달) · `pipeline_aborted`(재시도 소진 확정) · `validation_repeated_failure`(같은 항목 2회 실패) · `processing_delayed`(20초 초과) · `itinerary_partial`(일차 부족) | [조건] | spec §5.5 |
| R-05 | 플래그를 근거로 산출물이 수정된 건이 0건이고, 로그 기록 외 알림 발송이 0건이다 | [필수] | abnormality-detection / log-monitor-agent |
| **R-06** | **중복 기록 범위가 `(execution_id, attempt_no, step, type)` 조합당 1행**이다. `attempt_no`가 올라간 뒤 같은 단계·같은 유형이 재발했는데 기록되지 않은 건이 0건이다 | [필수] | spec §5.5 |
| **R-07** | `validation_repeated_failure`가 `attempt_no`별로 집계된다(이전 시도의 실패를 누적하지 않는다) | [조건] | spec §5.5 |

---

## S. 재시도 및 중단

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| S-01 | 재시도 시 `form_input`이 유지되고 문장·구성·렌더 결과만 다시 만들어졌다 | [필수] | spec §11.6 |
| S-02 | 동일 단계 재시도가 **2회를 초과하지 않았다**(총 3회 시도 이내) | [필수] | spec §11.6 |
| S-03 | 재시도 카운터가 **4종(`normalization`·`brochure`·`page`·`consistency`)**이며 **카운터끼리 예산을 공유한 건이 0건**이다 | [필수] | spec §11.6 |
| S-04 | **폼 validation·일차 분해 실패에 재시도 카운터가 적용되지 않았다**(사용자 재입력이므로) | [필수] | spec §11.6 |
| S-05 | 실패 지점별 복귀 대상이 §11.6 표(**8행**)와 정확히 일치한다 | [필수] | spec §11.6 |
| S-06 | 재시도 소진 시 `pipeline_aborted` 플래그가 기록되고, 상태가 §15.2대로 전이했다 | [조건] | spec §5.5, §15.2 |
| **S-07** | **카운터와 축의 대응이 1:1**이고, 소진 시 상태가 §11.6 표대로다 | [필수] | spec §11.6 |
| **S-08** | **카운터 초기화가 사람의 조작 시점에만** 일어난다 — 폼 제출·재제출·[다시 생성]은 4종 전부, **[상품 생성]은 `page`·`consistency`**. 시스템 자동 재호출이 초기화한 건이 0건이다 | [필수] | spec §11.6 |
| **S-09** | **생성 실패와 검증 실패가 같은 카운터를 공유**하고, 원인 구분이 `execution_logs.output`에 남는다 | [필수] | spec §11.6, §4.3 |

---

## T. 에이전트 권한 및 실행 순서

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| T-01 | 호출 순서가 `intake(분해·정규화) → content-writer → validator(1차) → web-builder → validator(2차) → validator(3차)`이고, 각 단계 종료 직후 성공·실패 모두 log-monitor가 호출되었다 | [필수] | product-orchestrator / workflow.md |
| T-02 | **0차 검증은 intake-agent가 담당**하고(기계 검사 + `itinerary-decomposition`의 AI 판정), validator-agent는 1·2차에 `fact-check`, 3차에 `consistency-check`를 사용하며 재시도 판단 없이 판정·`items`만 반환했다 | [필수] | spec §11.1 / validator-agent |
| T-03 | 권한 침범이 0건이다(오케스트레이터 외 에이전트 간 호출 / 오케스트레이터의 자체 산출물 생성 / log-monitor의 산출물 수정 / web-builder의 `confirmed_data` 변경 / content-writer의 페이지 콘텐츠 작성) | [필수] | product-orchestrator / workflow.md |
| T-04 | 실행 컨텍스트(`execution_id`·`attempt_no`·`retry_counts`·`current_step`)가 DB에 보관되어 매 요청마다 전달·갱신되었다 | [필수] | spec §5.1, §4.2 |
| T-05 | 일차 분해가 `itinerary-decomposition` 스킬로 intake 단계에서 수행되고, 그 결과가 `confirmed_data`에 담겨 후속 에이전트에 전달된다 | [필수] | spec §6.3, §2.3 |
| T-06 | **스킬 15개(워커 14 + 오케스트레이터 1)가 모두 실제로 하는 일이 있다.** 문서에만 있고 호출되지 않거나, 호출되지만 하는 일이 없는 스킬이 0건이다 | [필수] | workflow §8.2 |
| T-07 | 통합·삭제된 3개(`required-field-check`·`rejection-reason-writer`·`natural-language-extraction`)를 참조하는 코드·문서가 0건이고, 그 역할이 `input-guard`·`itinerary-decomposition`에 반영되어 있다 | [필수] | CHANGELOG 부록 D |
| T-08 | 각 Step의 AI 호출 배정이 workflow §8.2 표와 일치한다(Step 02는 `itinerary-decomposition`, 03은 `intro-content-fill`, 05는 `content-structuring`, 04·06은 `fact-check`, 07은 `consistency-check`가 AI 1회를 쓴다) | [필수] | workflow §8.2, spec §4.2 |
| **T-09** | **에이전트 1회 호출이 AI 호출 1회에 대응**하며, 한 서버 라우트에서 여러 에이전트를 연쇄 호출한 건이 0건이다 | [필수] | spec §4.2 |
| **T-10** | **`input-guard`의 선택 항목 채움이 `data-normalization`보다 먼저** 실행되고, 정규화가 그 채움 결과를 입력으로 받는다 | [필수] | workflow §9 Step 02 |
| **T-11** | **draft 등록을 web-builder-agent가 하지 않는다.** Step 07(또는 2차 소진 시 Step 06)에서 서버 라우트가 `draft-registration`을 실행한다 | [필수] | spec §9.5, workflow §8.1 |

---

## U. 보안 및 개인정보 (RLS)

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| U-01 | **익명 키로 `applications`를 `SELECT`하려는 시도가 차단된다**(신청자 명단 노출 0건) | [필수] | spec §16.3 |
| U-02 | 익명 키로 `applications`에 `UPDATE`·`DELETE`가 차단되고 `INSERT`만 허용된다 | [필수] | spec §16.3 |
| U-03 | 익명 키의 `products` `SELECT`가 `status = 'published'` 조건으로 제한된다 | [필수] | spec §16.3 |
| U-04 | 익명 키로 `execution_logs`·`abnormality_flags`·`edit_history` 접근이 전면 거부된다 | [필수] | spec §16.3 |
| U-05 | `product_images` 익명 `SELECT`가 게시된 상품의 것으로 제한되고 업로드는 service role만 가능하다 | [필수] | spec §16.3 |
| U-06 | service role 키가 서버 라우트에서만 사용되고 클라이언트에 노출되지 않는다 | [필수] | spec §16.3 |
| U-07 | 개인정보 수집 동의 없이 접수된 신청 레코드가 0건이다(`consent_at IS NULL` 0건) | [필수] | spec §16.2 |
| U-08 | Storage 읽기 공개로 인한 임시저장 **이미지 파일** 접근 가능성이 §16.2에 한계로 명시되어 있고, 페이지 콘텐츠·가격·일정은 노출되지 않는다 | [필수] | spec §7.3, §16.2 |
| **U-09** | **보유 기간(1년) 경과 신청을 삭제할 수 있는 경로가 존재**한다(`DELETE /api/applications/{id}`) | [필수] | spec §12.4, §13.1 |

---

## V. 반응형 및 성능

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| V-01 | **페이지 9종 섹션 컴포넌트 + 삽입 블록 3종**에 대한 375 / 768 / 1280px 사전 검증 결과가 문서화되어 있다 | [필수] | spec §17.1 |
| V-02 | 공개 페이지 본문에 가로 스크롤이 발생하지 않고, 표·긴 텍스트는 자체 `overflow-x: auto` 컨테이너 안에서만 스크롤한다 | [필수] | spec §17.1 |
| V-03 | 375 / 768 / 1280px에서 요소 겹침·콘텐츠 잘림이 0건이다 | [필수] | spec §17.1 |
| V-04 | 콘텐츠 길이 계약이 **생성 4종 / 편집 6종**으로 각 단계에서 강제된다 | [필수] | spec §17.1 |
| V-05 | 실제 검사 없이 "반응형 검사 통과"를 기록만 한 건이 **0건**이다 | [필수] | spec §17.1 |
| V-06 | 폼 필드에 `label`이 있고 본문 대비가 4.5:1 이상이다(테마 6종 전부) | [필수] | spec §17.2, §9.4 |
| V-07 | 공개 페이지 LCP가 2.5초 이내이고, `next/image` + `hero` priority + 나머지 lazy가 적용되어 있다 | [필수] | spec §17.2 |
| V-08 | 단계별 진행 표시가 §8.5·§9.6대로 나오고, 실패 시 사유와 다음 조작 버튼이 제공된다 | [필수] | spec §17.2, §15.1 |

---

## W. 데이터 무결성 — 고유명사·수치 (AI 생성분 기준)

> 판정 기준 시점은 **`validation_snapshot.content_hash` 시점의 AI 생성분**이다. 기획자가 편집기에서 추가·변경한 내용은 이 섹션의 판정 대상이 아니다(J-14, spec §10.4).

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| W-01 | 소개서·상품 페이지의 **모든 고유명사(지명·숙소·상점·공항·항공편)가 `form_input`과 일치**하며 입력에 없는 고유명사가 **0건**이다 | [필수] | spec §16.1 |
| W-02 | 실재하지 않는 장소명·호텔명을 새로 만들거나 유사한 다른 명칭으로 바꿔 쓴 건이 0건이다 | [필수] | spec §16.1 |
| W-03 | 지명·시설명 표기 임의 변경(약칭·영문 변환·어순 변경)이 0건이다. **slug는 이 규칙의 대상이 아니다** | [필수] | spec §16.1, §12.1 |
| W-04 | 소개서와 상품 페이지의 고유명사 표기가 서로 동일하다 | [필수] | spec §11.1 |
| W-05 | **일차별 서술에 `원문근거` 밖의 장소·활동·이동이 0건**이다 | [필수] | spec §6.3, §16.1 |
| W-06 | 가격이 입력값 그대로이고 계산·할인·환산·합계가 0건이며, 남아 있는 모든 아라비아 숫자가 **허용 출처 4종**에 근거를 가진다 | [필수] | spec §6.3.1, §16.1 |
| W-07 | 규칙을 적용할 수 없어 원본을 유지한 값이 사유와 함께 기록되어 있다 | [조건] | data-normalization |

---

## AA. 동시성 · 원자성 (§16.1.1)

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| **AA-01** | **모든 쓰기 라우트가 `updated_at`을 조건으로 갱신**하고, 영향 행이 0이면 **409 `{reason:"stale"}`**를 반환한다 | [필수] | spec §16.1.1 |
| **AA-02** | 적용 대상이 상태·산출물을 바꾸는 전 라우트(#2~8·10~13·17~19)이고, `POST /api/products`·`POST /api/applications`·조회는 제외된다 | [필수] | spec §16.1.1 |
| **AA-03** | `409 stale`을 받은 클라이언트가 **자동 재시도하지 않고** 재조회 후 사용자에게 알린다 | [필수] | spec §16.1.1, §14.6 |
| **AA-04** | 편집기 동시 편집 시 잠금 없이 마지막 저장이 이기되, `updated_at` 불일치 시 **"다른 사람이 먼저 저장했습니다"**를 표시하고 재조회한다. 저장 전 내용이 화면에 남는다 | [조건] | spec §16.1.1 |
| **AA-05** | slug UNIQUE 충돌 시 접미사를 올려 **최대 5회 재시도**하고, 모두 실패하면 `409 slug_conflict`를 반환한다 | [조건] | spec §16.1.1 |
| **AA-06** | **트랜잭션 경계가 §16.1.1 표대로**다 — Step 01(행+이미지), 08(콘텐츠+`human_edited`+전이+이력), 09(상태+시각), 10(INSERT), 13(삭제+CASCADE) | [필수] | spec §16.1.1 |
| **AA-07** | Storage 업로드·삭제와 로그 기록이 **트랜잭션 밖**에서 실행된다 | [필수] | spec §16.1.1 |
| **AA-08** | 다단계 쓰기가 중간에 실패했을 때 부분 반영된 상태로 남은 건이 0건이다 | [필수] | spec §16.1.1 |

---

## AB. 삭제 · 정리 (§12.4)

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| **AB-01** | **`DELETE /api/products/{id}`가 존재**하고 인증을 요구한다 | [필수] | spec §12.4, §14.4 |
| **AB-02** | **`published` 상품의 삭제가 차단**되고 `409 precondition` + 사유를 반환한다 | [조건] | spec §12.4 |
| **AB-03** | **`applications`가 1건 이상 있는 상품의 삭제가 차단**된다(고아 신청 0건) | [조건] | spec §12.4 |
| **AB-04** | `generating` 상태 상품을 삭제할 수 있다(진행 주체가 사라진 상품 정리) | [조건] | spec §12.4, §15.1.1 |
| **AB-05** | 확인 모달에 행사명이 표시되고 명시적 확인을 받는다 | [조건] | spec §12.4 |
| **AB-06** | 삭제 시 `product_images`·`edit_history`가 CASCADE로 함께 삭제된다 | [조건] | spec §12.4 |
| **AB-07** | **`execution_logs`·`abnormality_flags`가 삭제되지 않고** `product_id`만 `NULL`로 설정된다 | [조건] | spec §12.4 |
| **AB-08** | Storage 파일이 `{product_id}/` 접두사로 일괄 삭제되고, 실패해도 요청은 성공으로 확정된다 | [조건] | spec §12.4 |
| **AB-09** | `DELETE /api/applications/{id}`가 존재하고 `application_deleted` 로그를 남긴다 | [필수] | spec §12.4 |
| **AB-10** | `product_deleted` 로그가 `category = lifecycle`로 기록된다 | [조건] | spec §5.4, §12.4 |

---

## X. 승인 준비 상태

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| X-01 | AI 생성분에 오탈자·비문·표기 불일치로 수정이 필요한 항목이 **0건**이다 | [필수] | spec §1 |
| X-02 | 빈 섹션·플레이스홀더 문자열(`TODO`·`준비 중` 등)이 남아 있지 않다(`추후 추가 예정`·`해당 없음`은 정상 값) | [필수] | spec §16.1 |
| X-03 | 소개서·상품 페이지 어디에도 중괄호 토큰·조사 파이프 기호 등 템플릿 흔적이 없다 | [필수] | G-05, H-10 |
| X-04 | 기획자가 **코드·디자인 작업 0건**으로 입력부터 게시까지 완료할 수 있다 | [필수] | spec §1 |
| X-05 | 기획자 1명이 입력부터 공개 URL 배포까지 **10분 이내**에 완료할 수 있다 | [필수] | spec §1 |

---

## Y. 범위 위반 확인

| ID | 항목 | 판정 | 근거 |
|---|---|---|---|
| Y-01 | 결제 기능을 구현한 건이 0건이다 | [필수] | spec §18.2 |
| Y-02 | 가격 산정·계산·환산·합계를 수행한 건이 0건이다 | [필수] | spec §18.2 |
| Y-03 | 상품 기획 자체(입력 전 단계)를 AI가 대신 수행한 건이 0건이다 | [필수] | spec §18.2 |
| Y-04 | 이미지를 AI가 생성·보정한 건이 0건이고, 편집기에서 새 업로드한 건이 0건이다 | [필수] | spec §18.2 |
| Y-05 | **사용자 승인(게시 버튼) 전에 고객에게 노출된 건이 0건**이다 | [필수] | spec §16.2 |
| Y-06 | 편집 후 AI 재검증을 실행한 건이 0건이다 | [필수] | spec §18.2 |
| Y-07 | 헤드리스 브라우저 반응형 자동 검사를 구현한 건이 0건이다 | [필수] | spec §18.2 |
| Y-08 | 다중 계정·역할 분리·게시본/작업본 분리를 구현한 건이 0건이다 | [필수] | spec §18.2 |
| Y-09 | **일차 구분 없는 일정 원문을 자동 배분한 건이 0건**이다 | [필수] | spec §18.2, §6.3 |
| Y-10 | **백그라운드 워커·큐·Cron·상태 폴링·로마자 자동 변환 slug**를 구현한 건이 0건이다 | [필수] | spec §18.2 |
| **Y-11** | **`gallery` 슬롯을 구현한 건이 0건**이다 | [필수] | spec §18.2, §7.3 |
| **Y-12** | 다중 통화를 허용한 건이 0건이다(단위 `원` 고정) | [필수] | spec §6.2, §18.2 |
| **Y-13** | 개인정보 자동 삭제 배치를 구현한 건이 0건이다(수동 삭제 경로만) | [필수] | spec §13.1, §18.2 |

---

## Z. 최종 판정 게이트

아래 7개 게이트를 순서대로 통과해야 최종 합격이다. 앞 게이트가 실패하면 뒤 게이트는 판정하지 않는다.

| 게이트 | 통과 조건 | 대응 spec 절 | 실패 시 복귀 |
|---|---|---|---|
| **G0. 실행 구조** | A 섹션의 모든 `[필수]` 항목이 `예` | §4.2 · §4.3 · §14.4~14.6 | API 분할·AI 계약 재설계 |
| **G1. 입력·데이터 무결성** | B · C · D · E · F · W 섹션의 모든 `[필수]` 항목이 `예` | §5 · §6 · §7 · §16.1 | 폼 입력 화면 / intake-agent |
| **G2. 소개서 정합성** | G 섹션 + I-01~I-03 | §8 · §11.1(0·1차) | content-writer-agent (카운터 `brochure`) |
| **G3. 페이지 정합성** | H 섹션 + I-04~I-23 | §9 · §11.1~11.3 | web-builder-agent (카운터 `page`/`consistency`) |
| **G4. 상태·편집·게시** | J · K · N · O · V 섹션의 모든 `[필수]` 항목이 `예` | §10 · §12 · §15 · §17 | §15.1 표 대조 후 해당 화면 수정 |
| **G5. 신청·이메일·보안** | L · M · U 섹션의 모든 `[필수]` 항목이 `예` | §13 · §16.3 | 신청 API / RLS 정책 |
| **G6. 기록·운영 완결성** | P · Q · R · S · T · **AA · AB** · X · Y 섹션의 모든 `[필수]` 항목이 `예` | §5.4 · §5.5 · §11.6 · §12.4 · §14.1~14.3 · §16.1.1 | 해당 단계 재실행 |

### 최종 판정 기록

| 항목 | 값 |
|---|---|
| `products.id` | |
| `execution_id` / `attempt_no` | |
| `slug` / 공개 URL | |
| 검증 일시(UTC) | |
| `[필수]` 항목 총 개수 / `예` 개수 | |
| `N/A` 항목 개수 | |
| G0 / G1 / G2 / G3 / G4 / G5 / G6 | |
| **최종 판정** | 합격 / 불합격 |
| 불합격 시 복귀 단계 | |
| 불합격 시 재시도 카운터 | |

---

## 부록. 폐기된 1.0 항목

1.0 checklist의 아래 항목은 **그대로 두면 이 제품이 무조건 불합격 처리된다.** 폐기·개정 내역은 [CHANGELOG.md](./CHANGELOG.md)에 있다.

| 1.0 ID | 내용 | 처리 |
|---|---|---|
| V-01 | "정식 게시를 수행한 건이 0건이다" | **폐기.** 게시가 핵심 기능이 되었다. K-15("자동 게시 0건")로 대체 |
| A-01~A-05 | `outputs/run-{id}/` 파일 존재·개수 검사 | **폐기.** B 섹션(DB 기준)으로 대체 |
| M-01~M-05 | `log.html` 단일 파일 규정 | **폐기.** Q 섹션으로 대체 |
| H-04 | 반응형 검사 결과 3건 기록 필수 | **폐기.** V-01·V-05로 대체 |
| C-05 | 선택 항목 미입력 시 AI 확인 질문 이력 | **폐기.** 웹 폼 UI가 대체 |
| E-01~E-04 | 필수 항목 누락 시 AI 반려 경로 | **폐기.** C-01·C-02로 대체 |
| O-02 | 최종 상태값 `draft` 유지 | K-15로 개정 |
| T-05 | 신청이 `draft` 상태를 변경하지 않음 | L-10으로 확장 |
| J-02 | 소개서 vs 웹 전 항목 문장 문자열 일치 | I-07~I-09로 개정 |
| P-02 | 재시도 3회 이내 | S-02로 개정(2회) |
| P-04 | 재시도 카운터 6종 | S-03으로 개정(**4종**) |
