# spec.md — zentrip: 여행 상품 페이지 자동 생성·배포 플랫폼

> **문서 버전 2.2** (2026-08-10).
> 2.1 → 2.2 개정: 파이프라인을 **단계별 API로 분할**(§8·§9·§14.4), 검증 기준값을 확정 데이터표 → **`form_input`으로 교체**하고 정규화 검증 축 신설(§11.1), 3차 검증 대조 대상 수정, 실패를 **두 갈래로 단순화**하고 **상태 × 화면 × 버튼 표** 신설(§15.1), 그 외 14건.
> 개정 내역: [부록 A](#부록-a-10-대비-개정-내역)(1.0 대비) · [부록 B](#부록-b-20-대비-개정-내역)(2.0 대비) · [부록 C](#부록-c-21-대비-개정-내역)(2.1 대비)

---

## 1. 목표

기획이 끝난 여행 상품 정보를 웹 폼으로 입력하면, AI가 여행 상품 소개서(간단버전)와 고객용 반응형 상품 페이지를 생성하고, 기획자가 편집기에서 검토·수정한 뒤 버튼 하나로 상품별 고유 URL에 실제 배포하며, 고객의 신청을 데이터베이스에 적재하고 상품 정보 이메일을 자동 발송하는 것까지를 하나의 흐름으로 완결한다.

**측정 기준**: 기획자 1명이 상품 정보 입력부터 공개 URL 배포까지 **10분 이내**, 코드·디자인 작업 **0건**으로 완료할 수 있다.

---

## 2. 맥락

### 2.1 대상

| 역할 | 설명 | 접근 범위 |
|---|---|---|
| 기획자 | 여행 상품을 기획·등록하는 담당자 | 관리 페이지 전체(비밀번호 인증) |
| 고객 | 상품 페이지를 열람하고 신청하는 일반 이용자 | 게시된 상품 페이지, 신청 폼 |

### 2.2 사용 목적

기획이 끝난 여행 상품을 페이지 제작·디자인·배포 등 여러 과정을 거치지 않고 자동화하여, 시간적 자원과 인적 자원을 효율적으로 사용한다.

### 2.3 AI의 기여 범위 (명시적 정의)

입력이 웹 폼으로 구조화되었으므로, AI가 담당하는 일과 담당하지 않는 일을 아래와 같이 확정한다. 이 경계는 심사·검증 시 판정 기준으로 사용한다.

| AI가 하는 일 | AI가 하지 않는 일 |
|---|---|
| 폼 값의 표기 정규화 및 확정 데이터표 구성 | 값의 생성·추정·보완 |
| **일정 원문의 일차 분해·구조화**(§6.1) | 원문에 없는 일차·장소·활동·이동 생성 |
| 자유 서술 필드(일정·식사·상점정보) 내부의 요소 추출·분류 | 필수값 누락 판정(프론트엔드 validation이 담당) |
| 섹션별 문장 작성(개요·일차별 서술 문장·안내 문구) | 사실정보 변경, 요약·윤문에 의한 값 변형 |
| 여행스타일 기반 테마(컬러·헤드라인 톤·강조점) 결정 | 레이아웃·컴포넌트 구조 결정(고정 컴포넌트가 담당) |
| 폼 입력 대조 검증(사실정보·정합성) 판정 | 재시도 여부 판단(클라이언트·기획자가 담당) |
| 이미지의 지정 슬롯 배치 | 이미지 슬롯 재배치, 이미지 생성·보정 |

---

## 3. 최종 산출물 — 사용자 여정

```text
[기획자]                                              [고객]
   │
   │ ① 비밀번호 로그인 → /new
   ▼
웹 폼 입력 (필수 폼그룹 6 · 선택 4) + 이미지 업로드
   │
   │ ② [소개서 생성] 버튼 — 3개 요청을 순차 실행
   │      ├─ 일차 분해 + 확정 데이터표 + 0차 검증
   │      ├─ 소개서 생성
   │      └─ 1차 검증                    status = brochure_ready
   ▼
소개서 검토 화면 (8개 섹션 · 입력 전 항목 반영)
   │
   │ ③ [상품 생성] 버튼 — 3개 요청을 순차 실행
   │      ├─ page_content 생성
   │      ├─ 2차 검증
   │      └─ 3차 검증                    status = draft
   ▼
임시저장 목록 (/admin — 기획자만)
   │
   │ ④ [편집] 버튼 (편집 없이 바로 ⑤로 갈 수도 있다)
   ▼
편집기: 섹션 수정 · 삭제 · 삽입 · 순서변경 + 실시간 미리보기
   │                              status = reviewing, human_edited = true
   │ ⑤ [게시] 버튼
   ▼
status = published → 공개 URL 배포 ─────────────────────▶ /p/{slug}
                                                            │
                                                            │ ⑥ 신청
                                                            ▼
                                              Supabase applications 적재
                                                            │
                                                            ▼
                                              Resend 상품 정보 이메일 자동 발송
                                                            │
관리 페이지 신청 내역 ◀──────────────────────────────────────┘
```

---

## 4. 시스템 구성 (확정 사항)

| 구분 | 선택 | 근거 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | 서버 라우트·동적 라우트·Vercel 네이티브 |
| 배포 | Vercel 단일 프로젝트, 상시 배포 | 게시마다 재빌드하지 않으므로 게시 지연 0초, 게시 취소도 즉시 |
| 데이터베이스 | Supabase Postgres | 상품·신청·로그·편집이력 |
| 이미지 저장소 | Supabase Storage | 업로드 이미지 |
| AI | Claude API (Messages API) 직접 호출 | 서버리스 환경에서 응답 지연·배포 복잡도 최소 |
| 이메일 | Resend | Vercel 서버 라우트에서 직접 발송 |
| 인증 | 단일 공유 비밀번호 + 서명 쿠키 | 기획자 전용 게이트, 시연 단순성 |

### 4.1 게시 = 공개 노출 전환 (배포 방식 확정)

게시는 **DB 상태값 전환**으로 구현하며, 공개 상품 페이지는 Vercel에 상시 배포된 앱의 동적 라우트 `/p/{slug}`가 서빙한다.

- **상품마다 별도 URL**: `/p/{slug}` — 상품별 고유 slug로 각각 다른 URL을 가진다.
- **실제 배포**: 게시된 페이지는 인증 없이 인터넷에서 접근 가능한 실제 공개 웹페이지다.
- **`published` 외 전 상태는 404**: `/p/{slug}`는 `status = published`일 때만 200을 반환한다. 그 외 6개 상태는 예외 없이 404다(§15.1 표 최우측 열).
- **draft는 배포하지 않는다**: 임시저장 페이지는 DB에만 존재하고, 편집기 내부 미리보기(`/admin/products/{id}/edit`)로만 렌더링된다.

> 게시마다 재빌드(Deploy Hook)하는 방식을 채택하지 않은 이유: 게시당 1~3분 지연이 발생하고, 빌드 실패 시 시연이 중단되며, draft 파일을 Vercel에 올리는 순간 URL을 아는 사람이 열람할 수 있어 "기획자만 확인 가능" 요건을 무료 플랜에서 만족시킬 수 없다.

### 4.2 실행 시간 제약과 1요청 1AI호출 원칙

**제약**: Vercel 서버리스 함수는 요청 1건을 정해진 시간 안에 끝내야 하고, 초과하면 강제 종료된다. 무료(Hobby) 요금제의 상한은 **60초**이며 설정으로 늘릴 수 없다. 응답을 먼저 반환하는 `after()` / `waitUntil`은 **실행 순서를 바꿔줄 뿐 시간 한도를 늘려주지 않는다.**

> 요금제별 상한은 서비스 정책에 따라 바뀔 수 있다. 배포 전 현재 문서를 확인한다.

**원칙**: 서버 라우트 1건은 **AI를 최대 1회만 호출한다.**

| 항목 | 규정 |
|---|---|
| 요청당 AI 호출 | 최대 1회 |
| AI 호출 1회 타임아웃 | 25초 |
| 서버 라우트 `maxDuration` | 60초 |
| 여러 AI 호출이 필요한 작업 | **여러 API로 분할하고 클라이언트가 순차 호출**한다 |
| 재시도 | 서버 내부 루프가 아니라 **클라이언트가 같은 API를 재호출**한다. 카운터는 `products.retry_counts`에 보관 |
| 진행 표시 | 클라이언트가 현재 호출 중인 단계를 화면에 표시한다. 폴링은 사용하지 않는다 |

이 원칙에 따라 소개서 생성은 3개 요청, 상품 페이지 생성은 3개 요청으로 나뉜다(§8.5·§9.6).

---

## 5. 데이터 모델

### 5.1 `products`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid PK | 상품 식별자 |
| `execution_id` | text UNIQUE | 실행 ID(로그 추적 단위). 재시도·재제출에도 동일 값을 유지한다 |
| `attempt_no` | int | 생성 시도 회차. 최초 1. **[다시 생성]·재제출 시 +1** |
| `slug` | text UNIQUE NULL | 공개 URL 경로. 페이지 생성 시 발급되며 그 전에는 NULL. 게시 후 불변 |
| `status` | text | `generating` / `input_error` / `brochure_ready` / `draft` / `reviewing` / `published` / `unpublished` |
| `current_step` | text | 마지막으로 완료한 파이프라인 단계. 클라이언트가 다음 호출 대상을 판단한다 |
| `form_input` | jsonb | 사용자 원본 폼 입력. **불변**. **모든 검증의 기준값**(§11.1) |
| `confirmed_data` | jsonb | 확정 데이터표(§6). 같은 `attempt_no` 안에서 불변 |
| `brochure_content` | jsonb | 소개서(간단버전) 콘텐츠 모델(§8) |
| `page_content` | jsonb | 상품 페이지 콘텐츠 모델(§9). **`theme`을 이 안에만 보관한다** |
| `validation_snapshot` | jsonb | 검증 결과(§11.3). **편집으로 변경되지 않으며, `attempt_no`가 올라갈 때만 갱신** |
| `retry_counts` | jsonb | `{brochure, page, consistency}` 단계별 재시도 횟수 |
| `human_edited` | boolean | 편집기에서 1회 이상 저장되었는지 |
| `publish_override_at` | timestamptz | 검증 실패 상태에서 기획자가 책임 확인 후 게시한 시각(§11.5) |
| `failure_reason` | text | `input_error` 상태의 사유 |
| `published_at` | timestamptz | 최초 게시 시각 |
| `created_at` / `updated_at` | timestamptz | 모든 쓰기 작업에서 `updated_at`을 갱신한다 |

> `theme` 컬럼은 2.1에서 제거했다. `page_content.theme` 단일 출처로만 보관해 이중 저장·동기화 문제를 없앤다.

### 5.2 `product_images`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid PK | |
| `product_id` | uuid FK | |
| `slot` | text | `hero` / `accommodation` / `itinerary_day_{n}` / `shop` / `gallery` |
| `storage_path` | text | Supabase Storage 경로. `{product_id}/{uuid}.{ext}` |
| `alt` | text | 대체 텍스트 |
| `sort_order` | int | 같은 슬롯 내 순서 |
| `width` / `height` / `bytes` | int | 업로드 시 추출 |

### 5.3 `applications`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid PK | |
| `product_id` | uuid FK | |
| `name` / `email` / `phone` | text | 신청자 정보 |
| `headcount` | int | 인원수 (1~20) |
| `consent_at` | timestamptz NOT NULL | 개인정보 수집·이용 동의 시각 |
| `product_snapshot` | jsonb | **신청 시점** 상품 정보 스냅샷 |
| `email_status` | text | `pending` / `sent` / `failed` |
| `email_error` | text | 발송 실패 사유 |
| `created_at` | timestamptz | |

### 5.4 `execution_logs`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | bigserial PK | |
| `execution_id` | text | |
| `product_id` | uuid FK nullable | |
| `category` | text | **`pipeline`** / `lifecycle` / `application` — 로그 화면 탭 기준(§14.3) |
| `step` | text | 아래 표 참조 |
| `attempt_no` | int | 기록 시점 시도 회차 |
| `retry_index` | int | 해당 시도 안에서의 재시도 회차. 0 = 최초 |
| `verdict` | text | `통과` / `반려` / `-` |
| `status` | text | 기록 시점 상품 상태 |
| `input` / `output` | jsonb | 가공·요약 없는 원본 |
| `created_at` | timestamptz | UTC |

| `category` | `step` 값 | 기록 시점 |
|---|---|---|
| `pipeline` | `pipeline_started` | 폼 제출 + 서버 validation 통과(§8.1) |
| | `itinerary_decomposed` | 일차 분해 완료(§8.2) |
| | `normalization_validated` | 0차 검증 완료(§8.2) |
| | `brochure_generated` | 소개서 생성 완료(§8.3) |
| | `validation_1_completed` | 1차 검증 완료(§8.4) |
| | `page_generated` | `page_content` 생성 완료(§9.5) |
| | `validation_2_completed` | 2차 검증 완료(§9.5) |
| | `validation_3_completed` | 3차 검증 완료(§9.5) |
| | `draft_registered` | `status = draft` 전이 직후(§9.5) |
| | `regenerate_requested` | [다시 생성] 조작 시(§15.3) |
| `lifecycle` | `content_edited` · `slug_changed` · `published` · `unpublished` · `publish_override` | 각 조작 직후 |
| `application` | `application_received` · `email_sent` · `email_resent` | §13.2 |

각 단계는 **성공·실패 모두** 1행을 기록한다. 재시도는 `retry_index`를 올려 새 행으로 누적하고 기존 행을 덮어쓰지 않는다.

> `category`를 둔 이유: 신청이 100건 누적되면 `application` 로그가 실행 로그 화면을 덮어 "단일 화면에서 파이프라인 이력 확인"이 무의미해진다.

### 5.5 `abnormality_flags`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | bigserial PK | |
| `execution_id` | text | |
| `type` | text | 아래 표 |
| `step` | text | 감지 단계 |
| `detail` | text | 감지 내용 |
| `detected_at` | timestamptz | |

**감지 조건** (2.1에서 소실되었던 정의를 복원·확장)

| `type` | 감지 조건 |
|---|---|
| `retry_accumulated` | 한 단계의 `retry_counts` 값이 **2에 도달**한 시점(= 마지막 재시도 진입) |
| `pipeline_aborted` | 한 단계의 재시도가 **소진**되어 `input_error` 또는 `verdict = fail`로 확정된 시점 |
| `validation_repeated_failure` | **같은 검증 항목**이 같은 `attempt_no` 안에서 2회 이상 실패 |
| `processing_delayed` | 한 요청의 소요 시간이 **AI 호출 타임아웃(25초)의 80%를 초과**(= 20초 이상) |
| `itinerary_partial` | 일정 원문의 일차 수가 여행기간보다 적어 `추후 추가 예정`으로 채운 경우(§6.1) |

감지된 경우에만 기록한다. "이상 없음" 류 항목은 남기지 않는다.

### 5.6 `edit_history`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | bigserial PK | |
| `product_id` | uuid FK | |
| `action` | text | `update` / `delete` / `insert` / `reorder` |
| `section_id` | text | 대상 섹션 |
| `before` / `after` | jsonb | 변경 전·후 값 |
| `edited_at` | timestamptz | |

---

## 6. 확정 데이터표 (`confirmed_data`)

### 6.0 용어 구분 — "폼 그룹"과 "데이터 키"

2.1에서 "6개"가 두 의미로 쓰여 혼동을 일으켰다. 아래와 같이 용어를 분리한다.

| 용어 | 개수 | 구성 |
|---|---:|---|
| **필수 폼 그룹** (입력 화면 단위, §7.1) | 6 | 행사정보 · **일정** · 숙박 · 상점 · 가격 · 식사 |
| **데이터 키** (확정 데이터표 최상위, §6) | 6 | 행사정보 · 숙박 · 상점 · 가격 · 식사 · **항공편** (이 중 필수 5, 항공편은 선택) |

`일정`은 **데이터 키가 아니라 `행사정보`의 하위 필드**다. 폼에서는 별도 그룹으로 보이지만 저장 구조상 `행사정보.일정원문`·`행사정보.일정`에 들어간다.

### 6.1 구조

데이터 키는 6개다. 1~5는 필수, 6은 선택이다. **모든 하위 값은 문자열 또는 문자열 배열이며, 숫자·불리언 타입을 사용하지 않는다.**

| 순번 | 데이터 키 | 필수 | 하위 필드 |
|---:|---|---|---|
| 1 | 행사정보 | 필수 | `행사명`, `여행지`, `여행기간`, `일정원문`, `일정`, `여행스타일`*, `타겟층`* |
| 2 | 숙박 | 필수 | `숙소명`, `객실타입`, `위치`, `숙박일정`* |
| 3 | 상점 | 필수 | `상점명`, `상점정보` |
| 4 | 가격 | 필수 | `성인`, `아동`, `기타` |
| 5 | 식사 | 필수 | `식사정보` |
| 6 | 항공편 | 선택 | `공항`, `항공사`, `편명`, `출발시간`, `도착시간` |

\* 선택 필드. 데이터 키는 필수이지만 표시된 하위 필드는 미입력을 허용한다.

**선택 항목 미입력 처리**: 웹 폼에서 선택 항목이 비어 있으면 문자열 `추후 추가 예정`으로 채운다. 소개서·상품 페이지 양쪽에 동일하게 반영되고, 해당 섹션은 삭제되지 않는다.

**`가격.아동` 미운영 처리**: 아동 요금을 운영하지 않는 상품은 폼의 "아동 요금 미운영" 체크로 문자열 `해당 없음`을 저장한다. `0`을 넣어 "0원"으로 표시되는 것을 방지한다.

### 6.2 정규화 규칙 (허용 3종)

| 대상 | 규칙 |
|---|---|
| 날짜 | `YYYY-MM-DD` 형식으로 통일 |
| 금액 | **천 단위 콤마를 제거**하고 숫자와 단위만 남긴다. `120,000원` → `120000원` |
| 공백 | 앞뒤 공백 제거. 문자열 내부 연속 공백은 1칸으로 축약 |

이 3종 외의 값 변형은 금지한다. **이 3종에 의한 표기 차이는 §11.2의 허용 차이에 포함된다** — `form_input`을 기준으로 검증하므로 명시가 필요하다.

### 6.3 일정 원문의 일차 분해

폼의 일정 필드는 자유 텍스트 1개다(§7.1). AI가 이를 일차 단위로 분해하되, **분해 결과가 원문에 근거함을 검증할 수 있어야 한다.**

```json
"행사정보": {
  "일정원문": "1일은 김해공항에서 출발해 올레 7코스 걷기, 중식·석식 제공. 2일은 성산일출봉과 해녀박물관 관람...",
  "일정": [
    { "day": "1", "원문근거": "1일은 김해공항에서 출발해 올레 7코스 걷기, 중식·석식 제공", "내용": "..." },
    { "day": "2", "원문근거": "2일은 성산일출봉과 해녀박물관 관람", "내용": "..." }
  ]
}
```

| 규칙 | 내용 |
|---|---|
| `일정원문` | 사용자 입력 원본. **불변** |
| `원문근거` | `일정원문`에서 **발췌한 부분 문자열**이어야 한다(공백 정규화 후 부분 문자열 일치). 요약·재작성·의역 금지 |
| `내용` | `원문근거`에 등장하는 요소만 사용해 작성한 서술 문장. 새 장소·활동·이동을 추가할 수 없다 |
| 일차 수 | `일정` 배열 길이는 여행기간 일수와 **정확히 일치**한다 |
| 원문 일차 < 여행기간 | 부족한 일차는 `{"day": "n", "원문근거": "", "내용": "추후 추가 예정"}`으로 채우고 이상 플래그 `itinerary_partial`을 남긴다 |
| 원문 일차 > 여행기간 | **실패**. `status = input_error`, 사유를 폼에 표시하고 재입력을 요청한다 |
| 일차 구분 불가 | 원문에서 일차 구분(`1일`·`n일차`·`첫째 날`·`Day n`)을 식별할 수 없으면 **실패**. "일차 구분을 포함해 입력해 주세요"를 표시한다. 임의 배분하지 않는다 |

**`원문근거`·`내용`의 판정 방법** (2.1에서 판정 주체가 불명확했던 부분):

| 순서 | 검사 | 주체 |
|---:|---|---|
| 1 | `원문근거`가 `일정원문`의 부분 문자열인가 | **기계**(문자열 포함 검사) |
| 2 | `내용`에 등장하는 고유명사·명사구가 `원문근거` 또는 `confirmed_data`의 다른 값 안에 부분 문자열로 존재하는가 | **기계**(후보 추출 + 포함 검사). 미존재 후보는 위반 후보로 표시 |
| 3 | 2에서 표시된 후보가 실제 위반인지(조사·어미 변화 등 정상 변형 제외) | AI(`fact-check`) |

기계 검사가 1차 필터이므로, AI가 자기 생성물을 자기가 검사하는 구조에만 의존하지 않는다.

### 6.4 해소한 1.0 모순

1.0에서 `숙박일정`이 숙박의 필수 필드이면서 동시에 "선택 4개 항목"에 포함되어 있었다. **선택**으로 확정한다. 미입력 시 여행기간에서 파생 계산하지 않고 `추후 추가 예정`으로 둔다.

---

## 7. Step 1 — 웹 폼 입력 및 이미지 업로드

**경로**: `/new` (인증 필요)

### 7.1 필수 입력 (필수 폼 그룹 6개)

| 폼 그룹 | 필드 | 입력 형태 | validation | 저장 경로 |
|---|---|---|---|---|
| 행사정보 | 행사명 | 텍스트 | 2~40자 | `행사정보.행사명` |
| | 여행지 | 텍스트 | 2~60자 | `행사정보.여행지` |
| | 여행기간 | 시작일 + 종료일 | 시작일 ≤ 종료일, 최대 15일 | `행사정보.여행기간` |
| **일정** | 일정 원문 | 여러 줄 텍스트 | 20~2000자. 일차 구분 표기 필수 | `행사정보.일정원문` |
| 숙박 | 숙소명 | 텍스트 | 2~60자 | `숙박.숙소명` |
| | 객실타입 | 텍스트 | 1~40자 | `숙박.객실타입` |
| | 위치 | 텍스트 | 1~60자 | `숙박.위치` |
| 상점 | 상점명 | 텍스트 | 1~80자 | `상점.상점명` |
| | 상점정보 | 여러 줄 텍스트 | 1~500자 | `상점.상점정보` |
| 가격 | 성인 | 숫자 + 단위 | 0 이상 정수 | `가격.성인` |
| | 아동 | 숫자 + 단위 또는 "미운영" 체크 | 0 이상 정수 또는 `해당 없음` | `가격.아동` |
| | 기타 | 여러 줄 텍스트 | 0~300자 (포함/불포함 사항) | `가격.기타` |
| 식사 | 식사정보 | 여러 줄 텍스트 | 1~500자 | `식사.식사정보` |

**필수 필드 중 어느 하나라도 비어 있으면** 프론트엔드에서 제출을 차단하고 해당 필드에 오류를 표시한다. 서버는 동일 규칙으로 재검증하며, 위반 시 400을 반환하고 파이프라인을 시작하지 않는다.

일정 필드에는 아래 형식의 placeholder를 표시한다.

```text
1일: 김해공항 출발, 올레 7코스 걷기, 중식·석식 제공
2일: 성산일출봉, 해녀박물관 관람, 조식·중식 제공
...
```

**행사명 상한을 40자로 정한 근거**: `hero.headline`의 콘텐츠 길이 계약이 40자이고(§17.1), 값을 잘라내는 것은 §16.1 위반이므로 입력 단계에서 상한을 맞춘다.

**여행기간 상한을 15일로 정한 근거**: 일차별 이미지 슬롯이 총 이미지 상한과 충돌하지 않게 하기 위함이다(§7.3).

### 7.2 선택 입력 (4개)

| 필드 | 입력 형태 | 저장 경로 |
|---|---|---|
| 타겟층 | 텍스트 | `행사정보.타겟층` |
| 여행스타일 | select (테마 키와 1:1 대응. 선택한 옵션의 표시 문자열이 그대로 들어간다) | `행사정보.여행스타일` |
| 숙박일정 | 텍스트 (예: `3박4일`) | `숙박.숙박일정` |
| 항공편 | 공항 / 항공사 / 편명 / 출발시간 / 도착시간 | `항공편.*` |

### 7.3 이미지 업로드

| 항목 | 규정 |
|---|---|
| 슬롯 | `hero`(0~1) / `accommodation`(0~3) / `itinerary_day_{n}`(일차별 0~1, 일차 수만큼) / `shop`(0~2) / `gallery`(0~6) |
| 슬롯 지정 주체 | **사용자**. 업로드 시 슬롯을 반드시 선택한다. AI는 슬롯을 재배치하지 않는다 |
| 포맷 | JPG / PNG / WebP |
| 용량 | 장당 5MB 이하 |
| 총 장수 | **슬롯별 상한을 모두 준수**하며, 절대 상한은 27장(15일 여행에서 전 슬롯 만재 시 `1+3+15+2+6`). 실제 상한은 여행 일수에 따라 줄어든다 |
| 대체 텍스트 | 사용자 입력. 미입력 시 `{행사명} {슬롯 한글명}`으로 자동 채움 |
| 이미지 없을 때 | `hero` 미업로드 시 테마 컬러 그라디언트 배경으로 폴백. 그 외 슬롯은 이미지 영역을 렌더링하지 않으며, 섹션 자체는 유지한다 |
| 렌더링 계약 | 고정 종횡비 컨테이너 + `object-fit:cover` + `max-width:100%`. `hero`를 제외한 전 이미지는 지연 로딩(`loading="lazy"`), `hero`는 우선 로딩(§17.2) |
| 검증 범위 | 이미지-텍스트 내용 정합성(사진 속 장소가 여행지와 같은지)은 **검증 대상이 아니며 기획자 책임**이다. 검증은 슬롯 배치·개수·용량·대체 텍스트 존재 여부만 판정한다 |
| 권리 | 업로드 이미지의 저작권·초상권 확보는 기획자 책임이다. 폼에 고지 문구를 표시한다 |

`itinerary_day_{n}` 슬롯은 여행기간 입력 후 일차 수만큼 자동 생성된다. 여행기간을 줄여 일차가 사라지면 해당 슬롯의 이미지는 `gallery`로 이동시킬지 삭제할지 사용자에게 확인한다.

**이미지 업로드는 이 폼에서만 가능하다.** 편집기의 `image` 블록은 이미 업로드된 이미지 중에서만 선택한다(§10.2). 편집기에서 새 업로드를 허용하면 슬롯 체계와 총량 제한 밖으로 새어 나간다.

**Storage 접근에 대한 명시**: 버킷은 읽기 공개이고 경로에 UUID를 사용한다(§5.2). 따라서 **임시저장 상품의 이미지 파일은 정확한 URL을 아는 경우 접근이 가능하다.** 페이지 콘텐츠·가격·일정은 DB에 있어 노출되지 않으므로 "임시저장 페이지는 기획자만 확인" 요건은 유지되지만, 이미지 파일 자체는 완전 비공개가 아니다. 완전 비공개가 필요하면 signed URL 프록시로 전환해야 하며 이번 범위에서는 채택하지 않는다.

---

## 8. Step 2 — 소개서(간단버전) 생성

§4.2의 1요청 1AI호출 원칙에 따라 **4개 요청**으로 나눈다. ①은 AI를 쓰지 않으므로 즉시 끝나고, ②~④가 각각 AI 1회를 쓴다.

### 8.1 ① 상품 등록 — `POST /api/products`

AI 호출 없음. 폼 값을 검증하고 상품 행을 만든다.

1. §7.1·§7.2 규칙으로 서버 재검증. 위반 시 **400**을 반환하고 행을 만들지 않는다.
2. `products` 행 생성: `form_input` 저장, `execution_id` 발급, `attempt_no = 1`, `retry_counts` 전부 0, `status = generating`, `current_step = pipeline_started`.
3. 이미지를 Storage에 업로드하고 `product_images`에 슬롯별로 기록.
4. `execution_logs`에 `pipeline_started` 기록.
5. **200 + `{product_id}`** 반환.

### 8.2 ② 일차 분해 및 확정 데이터표 — `POST /api/products/{id}/decompose`

AI 1회. `form_input`을 §6.2 정규화와 §6.3 일차 분해로 `confirmed_data`로 만든다.

1. 정규화 3종 적용(기계 처리).
2. 일정 원문 일차 분해(AI 1회).
3. **0차 검증**(§11.1) 실행 — 기계 검사 중심이므로 추가 AI 호출을 하지 않는다.
4. `execution_logs`에 `itinerary_decomposed`, `normalization_validated` 기록.
5. 분해 실패(일차 초과·구분 불가) 시 `status = input_error` + `failure_reason`, `pipeline_aborted` 플래그, **422**를 반환한다.
6. 0차 검증 실패 시 — 재시도 여력이 있으면 `retry_counts.brochure += 1` 후 **409**(클라이언트가 이 단계를 재호출), 소진되면 `status = input_error` + `pipeline_aborted` 플래그, **422**.
7. 성공 시 `current_step = normalization_validated`, **200**.

### 8.3 ③ 소개서 생성 — `POST /api/products/{id}/brochure`

AI 1회. `confirmed_data`로 `brochure_content`(§8.6)를 만든다. `execution_logs`에 `brochure_generated` 기록, `current_step = brochure_generated`.

### 8.4 ④ 1차 검증 — `POST /api/products/{id}/validate-brochure`

AI 1회. `form_input` vs `brochure_content` 대조(§11.1 1차).

- 통과 → `status = brochure_ready`, `validation_snapshot.axes.axis_1 = pass`.
- 실패 & 재시도 여력 있음 → `retry_counts.brochure += 1`, **409 + 실패 항목**. 클라이언트가 ③부터 재호출한다.
- 실패 & 재시도 소진(`retry_counts.brochure = 2`) → **`status = brochure_ready`, `axis_1 = fail`**, `pipeline_aborted` 플래그, 200 + 실패 항목.

`execution_logs`에 `validation_1_completed`(통과·실패 모두) 기록.

> 재시도가 소진되어도 `input_error`가 아니라 `brochure_ready`가 되는 이유: 소개서는 이미 만들어졌고 사용자 입력에는 문제가 없다. 검토 화면에서 실패 항목을 보여주고 [다시 생성]·[입력 수정]을 제공하는 것이 맞다(§15.1·부록 C).

### 8.5 클라이언트 호출 순서

```text
[소개서 생성] 버튼
  │
  ├─ ① POST /api/products                        (AI 0회 · 즉시)     "저장 중…"
  ├─ ② POST /api/products/{id}/decompose         (AI 1회 · ~20초)   "일정 정리 중…"
  ├─ ③ POST /api/products/{id}/brochure          (AI 1회 · ~20초)   "소개서 작성 중…"
  └─ ④ POST /api/products/{id}/validate-brochure (AI 1회 · ~20초)   "검사 중…"
                                                  ─────────────────
                                        요청당 60초 예산 / 총 체감 ~60초
```

각 요청이 독립적으로 60초 예산을 가지므로 §4.2의 제약을 위반하지 않는다. 실패 시 클라이언트는 해당 단계부터 재호출한다.

### 8.6 소개서의 위치 정의

소개서(간단버전)는 **기획자가 상품 페이지를 생성할지 판단하기 위한 검토용 문서**다. 폼에서 입력받은 필수 폼 그룹 6개와 선택 4개 항목을 **전부 반영한다.**

**"간단버전"은 섹션을 줄인다는 뜻이 아니라 표현을 줄인다는 뜻이다.**

| 구분 | 소개서(간단버전) | 상품 페이지 |
|---|---|---|
| 반영 사실정보 | 입력된 전 항목 | 입력된 전 항목 (동일) |
| 서술 분량 | 압축 (개요 2~3문장, 일차별 1~2문장) | 확장 서술 |
| 이미지 | 없음 | 슬롯별 배치 |
| 테마·디자인 | 없음 (단일 컬럼 텍스트) | 여행스타일 테마 적용 |
| 신청 폼 | 없음 | 있음 |
| 용도 | 기획자 검토 | 고객 열람·신청 |

### 8.7 섹션 구성 (8개)

| 순서 | 섹션 | 내용 | 출처 |
|---:|---|---|---|
| 1 | 제목 | 행사명 | `행사정보.행사명` |
| 2 | 개요 | 여행지, 여행기간, 타겟층, 여행스타일, 핵심 일정 2~3문장 | `행사정보` |
| 3 | 일정 | 일차별 1~2문장 (이동·식사 포함) | `행사정보.일정` |
| 4 | 숙박 | 숙소명, 객실타입, 위치, 숙박일정 | `숙박` |
| 5 | 항공 | 공항, 항공사, 편명, 출발·도착 시간 | `항공편` |
| 6 | 식사 | 식사정보 | `식사` |
| 7 | 가격 | 성인, 아동, 기타 | `가격` |
| 8 | 제휴상점 | 상점명, 상점정보 | `상점` |

**필수 폼 그룹 6개의 값은 항상 위 8개 섹션 안에 전부 반영된다.** 필수 요소를 제외하는 구성은 허용하지 않는다.

**선택 항목 미입력 시**: 섹션을 삭제하지 않고 `추후 추가 예정`으로 표기한다. 상품 페이지와 동일한 표기이므로 §11의 검증에서 양쪽 섹션이 1:1로 대응한다.

**이미지를 포함하지 않는 근거**: 소개서는 사실정보 정확성을 빠르게 확인하는 문서이고, 이미지 배치 확인은 편집기 미리보기(§10.1)에서 수행한다.

### 8.8 산출 형태

`brochure_content` jsonb에 저장하고, 고정 React 컴포넌트가 렌더링한다. 별도 HTML 파일을 생성하지 않는다.

```json
{
  "schema_version": "1.0",
  "sections": [
    {
      "id": "b_title",
      "type": "title",
      "data": { "text": "제주 올레 바람 여행" },
      "source": { "text": "행사정보.행사명" }
    },
    {
      "id": "b_itinerary",
      "type": "itinerary",
      "data": { "days": [
        { "day": "1", "text": "김해공항에서 출발해 올레 7코스를 걷습니다. 중식과 석식이 제공됩니다." }
      ] },
      "source": { "days": "행사정보.일정" }
    },
    {
      "id": "b_flight",
      "type": "flight",
      "data": { "항공사": "추후 추가 예정", "편명": "추후 추가 예정" },
      "source": { "항공사": "항공편.항공사", "편명": "항공편.편명" }
    }
  ]
}
```

`source` 맵은 각 필드가 확정 데이터표의 어느 경로에서 왔는지를 기록하며, 검증의 기준이 된다. 출처가 없는 연결 문구는 `source`를 `"generated"`로 표기한다.

### 8.9 검토 화면 동작

- 소개서 8개 섹션을 읽기 전용으로 표시한다.
- 1차 검증 결과(통과/실패, 실패 항목 전체)를 함께 표시한다.
- 버튼 구성은 §15.1 표를 따른다. **`axis_1 = fail`이면 [상품 생성]이 잠기고 [다시 생성]·[입력 수정]이 제공된다.**

---

## 9. Step 3 — 상품 페이지 생성

§4.2 원칙에 따라 **3개 요청**으로 나눈다.

### 9.1 콘텐츠 모델이 산출물이다 (HTML 아님)

AI는 **`page_content` JSON만 생성**하고, 렌더링은 고정된 React 컴포넌트 세트가 담당한다.

| 이유 | 설명 |
|---|---|
| 편집기 성립 | 섹션 단위 수정·삭제·삽입은 안정적인 구조가 있어야 가능하다 |
| 반응형 보증 | 컴포넌트가 고정이므로 375/768/1280 동작이 사전에 보증된다 |
| 검증 가능 | `source` 맵으로 필드별 출처 대조가 가능하다 |
| 성능 | HTML 전문 생성 대비 출력 토큰과 지연이 크게 줄어든다 |

### 9.2 `page_content` 스키마

```json
{
  "schema_version": "1.0",
  "theme": "nature",
  "sections": [
    {
      "id": "sec_hero",
      "type": "hero",
      "order": 1,
      "visible": true,
      "locked": true,
      "data": {
        "headline": "제주 올레 바람 여행",
        "subcopy": "2026-03-14 ~ 2026-03-17",
        "image_slot": "hero"
      },
      "source": {
        "headline": "행사정보.행사명",
        "subcopy": "행사정보.여행기간"
      }
    },
    {
      "id": "sec_itinerary",
      "type": "itinerary",
      "order": 3,
      "visible": true,
      "locked": false,
      "data": { "days": [
        { "day": "1", "text": "...", "image_slot": "itinerary_day_1" }
      ] },
      "source": { "days": "행사정보.일정" }
    }
  ]
}
```

`theme`은 여기에만 보관한다(§5.1). 일차 이미지는 `data.days[].image_slot`으로 참조한다 — 2.1에서 이 필드가 없어 §11.2의 이미지 검증이 대상을 찾을 수 없었다.

### 9.3 섹션 타입

| 순서 | `type` | 필수 내용 | 출처 | 미입력 시 |
|---:|---|---|---|---|
| 1 | `hero` | 행사명, 핵심 문구, hero 이미지 | `행사정보.행사명`, `행사정보.여행기간` | 이미지 없으면 테마 그라디언트 |
| 2 | `summary` | 여행기간, 여행지, 타겟층, 여행스타일 | `행사정보` | `추후 추가 예정` 표기 |
| 3 | `itinerary` | 일차별 일정, 이동, 식사, 일차 이미지 | `행사정보.일정`, `식사` | — |
| 4 | `accommodation` | 숙소명, 객실타입, 위치, 숙박일정 | `숙박` | `추후 추가 예정` 표기 |
| 5 | `flight` | 공항, 항공사, 편명, 출·도착 시간 | `항공편` | 섹션 유지 + `추후 추가 예정` |
| 6 | `meal` | 식사정보 | `식사` | — |
| 7 | `price` | 성인, 아동, 기타 | `가격` | `해당 없음` 표기 |
| 8 | `shop` | 상점명, 상점정보 | `상점` | — |
| 9 | `apply` | 신청 폼, 가격 요약, 행사 정보 | `가격`, `행사정보` | — |

`hero`와 `apply`는 `locked: true`로, 편집기에서 삭제할 수 없다(§10.2).

### 9.4 테마

`행사정보.여행스타일` 값을 테마 키에 매핑한다. 값이 비었거나 `추후 추가 예정`이면 `default`를 사용한다.

테마 적용 범위는 **컬러 스킴 · 헤드라인 톤 · 강조 포인트 3가지로 한정**한다. 테마가 섹션 구성·문구·사실 정보를 바꾸지 않는다. 편집기에서 테마를 변경할 수 없다.

### 9.5 API 분할

| 순서 | 엔드포인트 | AI | 동작 |
|---:|---|---:|---|
| ① | `POST /api/products/{id}/page` | 1회 | `page_content` 생성, slug 발급(§12.1), 로그 `page_generated` |
| ② | `POST /api/products/{id}/validate-page` | 1회 | **2차 검증**(`form_input` vs `page_content`), 로그 `validation_2_completed` |
| ③ | `POST /api/products/{id}/validate-consistency` | 1회 | **3차 검증**(소개서 vs 페이지), 로그 `validation_3_completed` → `status = draft`, 로그 `draft_registered` |

**②가 실패하면 ③을 호출하지 않는다**(§11.1). 클라이언트는 ②의 응답을 보고 ③ 호출 여부를 결정한다.

각 검증의 실패 처리:
- 재시도 여력 있음 → 해당 카운터 +1, **409 + 실패 항목**. 클라이언트가 ①부터 재호출.
- 재시도 소진 → **`status = draft`, 해당 축 `fail`**, `pipeline_aborted` 플래그, 200 + 실패 항목. 편집·책임 게시 경로 유지(§11.5).

### 9.6 클라이언트 호출 순서

```text
[상품 생성] 버튼
  │
  ├─ ① POST /api/products/{id}/page                 (AI 1회) "페이지 구성 중…"
  ├─ ② POST /api/products/{id}/validate-page        (AI 1회) "사실 확인 중…"
  └─ ③ POST /api/products/{id}/validate-consistency (AI 1회) "소개서와 대조 중…"
        └─ ②가 fail이면 ③을 건너뛴다
```

### 9.7 실행 예산

| 항목 | 값 |
|---|---|
| 요청당 AI 호출 | **최대 1회** (§4.2) |
| AI 호출 1회 타임아웃 | 25초 |
| 서버 라우트 `maxDuration` | 60초 |
| 소개서 완료까지 체감 시간 (4요청 순차) | 목표 ≤ 60초 |
| 페이지 완료까지 체감 시간 (3요청 순차) | 목표 ≤ 60초 |
| 단계별 재시도 | 최대 2회. **클라이언트가 재호출**하며 카운터는 `products.retry_counts` |
| 폼 validation·일차 분해 실패 | 재시도가 아니라 사용자 재입력. 카운터 미적용 |

---

## 10. Step 4 — 편집기

**경로**: `/admin/products/{id}/edit` (인증 필요)

### 10.1 화면 구성

좌측 섹션 목록 + 중앙 편집 패널 + 우측 실시간 미리보기(375 / 768 / 1280 뷰포트 전환). 임시저장 페이지의 미리보기는 이 화면에서만 가능하며 공개 URL로 노출되지 않는다.

`draft` / `reviewing` 상태에서는 slug 편집 필드도 제공한다(§12.1).

### 10.2 편집 가능 범위

| 동작 | 허용 범위 |
|---|---|
| 수정 | 섹션의 `data` 필드 텍스트, 이미지 슬롯 교체, 대체 텍스트 |
| 삭제 | `locked: false`인 섹션. `visible: false`로 전환하며 데이터는 보존한다 |
| 삽입 | `free_text`, `image`, `notice` 3종 블록 |
| 순서변경 | `order` 값 변경. `hero`는 항상 첫 번째, `apply`는 항상 마지막 |
| 삭제 불가 | `hero`, `apply` (`locked: true`) |
| `apply` 예외 | 안내 문구·제목만 편집 가능. **신청 폼의 필드 구성(이름·이메일·연락처·인원수·동의)은 편집할 수 없다** — 신청 API 계약(§13.1)과 어긋나기 때문이다 |
| `image` 블록 | **이미 업로드된 이미지 중에서만 선택**한다. 편집기에서 새 업로드를 하지 않는다(§7.3) |
| 테마 | 변경 불가(§9.4) |
| 길이 계약 | §17.1 상한을 초과하는 입력은 저장 시 거부한다 |

### 10.3 저장 시 동작

`PATCH /api/products/{id}/content` 호출 시:

1. 길이 계약을 검사한다. 위반 시 400을 반환하고 저장하지 않는다.
2. `page_content`를 갱신한다.
3. `human_edited = true`로 설정한다.
4. `status`가 `draft`이면 `reviewing`으로 전이한다. `published` / `unpublished` / `reviewing`이면 유지한다.
5. 변경된 섹션마다 `edit_history`에 `{action, section_id, before, after}`를 기록한다.
6. `execution_logs`에 `content_edited`(`category = lifecycle`)를 기록한다.
7. **`form_input`, `confirmed_data`, `validation_snapshot`, `attempt_no`는 수정하지 않는다.**

### 10.4 편집분에 대한 책임 분리

AI 검증은 §11.4의 기준 시점에 고정되며, **편집 이후 재검증하지 않는다.** 편집분의 사실 정확성은 기획자 책임이다.

이를 명시하기 위해 관리 페이지와 편집기에 **배지 2종을 각각 독립적으로** 표시한다. 2.1에서는 배지 4종의 조건이 겹치고, 검증 실패 시 편집 여부가 사라지는 문제가 있었다.

| 배지 축 | 값 | 조건 |
|---|---|---|
| **검증** | `AI 검증 통과` | `validation_snapshot.verdict = pass` |
| | `AI 검증 실패` | `verdict = fail` 이고 `publish_override_at`이 없음 |
| | `검증 실패 · 책임 게시됨` | `verdict = fail` 이고 `publish_override_at`이 있음 |
| **편집** | (표시 없음) | `human_edited = false` |
| | `사람 편집됨` | `human_edited = true` |

두 축은 서로 독립이므로 `AI 검증 실패` + `사람 편집됨`이 동시에 표시될 수 있다. 검증 배지 3종은 상호 배타적이며 위에서 아래 순으로 판정한다.

---

## 11. 검증 정책

### 11.1 검증 4축 — 기준값은 `form_input`

2.1까지는 소개서와 상품 페이지를 **둘 다 `confirmed_data`에 대고** 검증했다. `confirmed_data`는 AI가 `form_input`을 정규화·분해해 만든 산물이므로, **그 단계에서 값이 변형되면 어느 축도 잡아내지 못했다.** 기준값을 사용자가 실제로 입력한 `form_input`으로 교체하고, `confirmed_data`의 생성 자체를 별도 축으로 검증한다.

| 축 | 시점 | 대상 | 스킬 | 판정 범위 |
|---|---|---|---|---|
| **0차** | §8.2 ② 직후 | `form_input` vs `confirmed_data` | `data-normalization` + 기계 검사 | 정규화 3종 외 변형 0건, 일차 분해의 원문 근거(§6.3) |
| 1차 | §8.4 ④ | **`form_input`** vs `brochure_content` | `fact-check` | 소개서 8개 섹션 전 필드 |
| 2차 | §9.5 ② | **`form_input`** vs `page_content` | `fact-check` | 페이지 9개 섹션 전 필드 (**주 검증**) |
| 3차 | §9.5 ③ (2차 통과 후) | `brochure_content` vs `page_content` | `consistency-check` | 대응표 기준, `apply` 제외 전 섹션 |

**0차를 둔 이유**: 기준값이 `form_input`이 되면서 "정규화가 규칙대로 됐는가"가 별도 판정 대상이 된다. 이 축은 문자열 비교 위주라 AI 호출 없이 ② 요청 안에서 처리한다.

**3차의 목적과 실행 조건**: 1·2차가 모두 통과하면 3차는 논리적으로 통과해야 한다. 따라서 3차는 **교차 검증·회귀 감지용**이다 — `source` 맵 누락, 두 생성 경로의 스키마 드리프트, 검증에서 빠진 필드를 잡아낸다. **2차 실패 시 3차는 실행하지 않는다.**

#### 3차 검증 섹션 대응표

| 소개서 섹션 | 상품 페이지 섹션 | 대조 대상 값 |
|---|---|---|
| 제목 | `hero` | 행사명 |
| 개요 | `summary` | 여행지, 여행기간, 타겟층, 여행스타일 |
| 일정 | `itinerary` | **일차 수, 각 일차의 사실정보 값(장소·활동·식사)** |
| 숙박 | `accommodation` | 숙소명, 객실타입, 위치, 숙박일정 |
| 항공 | `flight` | 공항, 항공사, 편명, 출발·도착 시간 |
| 식사 | `meal` | 식사정보 |
| 가격 | `price` | 성인, 아동, 기타 |
| 제휴상점 | `shop` | 상점명, 상점정보 |
| — | `apply` | 소개서에 대응 섹션이 없어 제외. 단 `apply` 내부의 가격·행사 정보는 `price`·`hero`와 일치해야 한다 |

> 2.1은 일정 행의 대조 대상을 `원문근거` 동일성으로 지정했으나, `원문근거`는 `confirmed_data`에만 있고 두 콘텐츠 모델에는 없어 실행 불가능한 규정이었다. `원문근거` 검증은 0차의 몫이다.

**대조 단위는 사실정보 값이다.** 소개서는 압축 서술, 상품 페이지는 확장 서술이므로 **문장 전체의 문자열 일치는 요구하지 않는다.** 값을 감싸는 서술 문장의 길이·어순·문장 수 차이는 실패 사유가 아니며, `source` 맵이 가리키는 **값 자체가 다르면** 실패다.

선택 항목 미입력 시 양쪽 모두 `추후 추가 예정`이어야 한다. 한쪽만 공란이거나 한쪽에서 섹션이 삭제된 경우 실패로 판정한다.

### 11.2 검증 항목과 허용 차이

| 영역 | 확인 내용 | 적용 축 |
|---|---|---|
| 정규화 범위 | `form_input` → `confirmed_data`가 허용 3종만 적용 | 0차 |
| 일정 근거 | `원문근거` ⊂ `일정원문`, `내용`이 §6.3 판정 3단계를 통과 | 0차 |
| 값 일치 | `source` 맵이 가리키는 경로의 값이 `form_input`의 대응 값과 일치 | 1·2차 |
| 치환 | 미치환 토큰(`{{...}}`)·조사 파이프 기호 0건, 오치환 0건 | 1·2차 |
| 출처 없는 수치 | `form_input`에 출처가 없는 아라비아 숫자 0건 | 1·2차 |
| 입력 외 고유명사 | 입력에 없는 지명·시설·경유지·관광지·부대시설 0건 | 1·2차 |
| 일정 일치 | 일차 수가 여행기간과 정확히 일치(초과·미달 0일) | 0·1·2·3차 |
| 선택 항목 | 미입력 항목이 `추후 추가 예정`으로 양쪽 동일 표기, 섹션 삭제 0건 | 1·2·3차 |
| 이미지 | `image_slot` 값이 업로드 시 지정 슬롯과 동일, 대체 텍스트 존재 | 2차 |
| 문서 간 일치 | 대응표 기준 사실정보 값 동일 | 3차 |

**허용 차이** (기준값이 `form_input`이 되면서 정규화 항목이 추가됐다)

| 허용 | 예 |
|---|---|
| 앞뒤 공백, 내부 연속 공백 축약 | `제주  올레` → `제주 올레` |
| HTML 이스케이프 | `&` → `&amp;` |
| **정규화 3종에 의한 표기 차이** | `120,000원` → `120000원`, `2026.03.14` → `2026-03-14` |
| 값을 둘러싼 서술 문장의 분량·어순·문장 수 | 소개서 압축 vs 페이지 확장 |

**실패로 판정하는 차이**: 값의 임의 재표기, 요약, 윤문, 부분 삭제, 어순 변경, 약칭·영문 변환.

### 11.3 `validation_snapshot` 구조

```json
{
  "attempt_no": 1,
  "verdict": "pass",
  "validated_at": "2026-08-10T04:12:33Z",
  "content_hash": "sha256:...",
  "axes": {
    "axis_0": { "verdict": "pass", "items": [] },
    "axis_1": { "verdict": "pass", "items": [] },
    "axis_2": { "verdict": "pass", "items": [] },
    "axis_3": { "verdict": "pass", "skipped": ["apply"], "items": [] }
  }
}
```

최상위 `verdict`는 **완료된 전 축이 통과했을 때만** `pass`다. 한 축이라도 `fail`이면 `fail`이다. 아직 실행되지 않은 축은 `null`로 두며 `verdict` 계산에 넣지 않는다.

`content_hash`는 검증 시점의 `page_content` 해시다. 현재 해시와 다르면 편집이 있었다는 뜻이다.

### 11.4 기준 시점 고정

- 각 축의 결과는 **그 축이 완료된 시점**에 기록되고, **같은 `attempt_no` 안에서 변경되지 않는다.**
- **편집은 `validation_snapshot`을 바꾸지 않는다**(§10.3 7항).
- **[다시 생성]으로 `attempt_no`가 올라갈 때만** 새 결과로 갱신된다. 이전 시도의 결과는 `execution_logs`에 남아 추적 가능하다.

### 11.5 게시 게이트

| `verdict` | 게시 버튼 | 절차 |
|---|---|---|
| `pass` | 활성 | `human_edited = true`면 확인 모달에 "편집된 내용은 AI 검증 대상이 아닙니다"를 표시하고 명시적 확인을 받는다 |
| `fail` | 기본 비활성 | **[검증 실패 항목 확인]** 모달에서 실패 항목 전체를 열람하고 "내용을 확인했으며 책임하에 게시합니다" 체크 시 활성된다. 게시 시 `publish_override_at` 기록 + `publish_override` 로그 |

`status = input_error`에는 게시 경로가 없다(§15.1).

> 책임 게시를 둔 이유: 검증 실패 한 번으로 상품을 영구히 게시 불가로 만들면 기획자가 직접 고쳐 쓸 수 있는 편집기의 의미가 사라진다. 대신 이력을 남겨 사후 추적이 가능하게 한다.

### 11.6 검증 실패 시 복귀

| 실패 지점 | 성격 | 복귀 대상 | 유지 항목 | 카운터 |
|---|---|---|---|---|
| 폼 검증(§8.1) | 입력 오류 | 폼 화면 | 없음(행 미생성) | 없음 |
| 일차 분해(§8.2) | 입력 오류 | 폼 화면 (값 유지) | `form_input` | 없음 |
| 0차 검증 | 생성 오류 | §8.2 ② 재호출 | `form_input` | `brochure`* |
| 1차 검증 | 생성 오류 | §8.3 ③부터 재호출 | `form_input` | `brochure` |
| 2차 검증 | 생성 오류 | §9.5 ① 부터 재호출 | `form_input`, 소개서 | `page` |
| 3차 검증 | 생성 오류 | §9.5 ① 부터 재호출 | `form_input`, 소개서 | `consistency` |

\* 0차 실패는 정규화·분해 자체의 문제이므로 `brochure` 카운터를 공유한다.

재시도 카운터는 **`brochure` · `page` · `consistency` 3종**이다. 재시도 시 `form_input`은 항상 유지하고 문장·구성·렌더 결과만 다시 만든다.

---

## 12. Step 5 — 게시

**API**: `POST /api/products/{id}/publish` / `POST /api/products/{id}/unpublish` (인증 필요)

### 12.1 slug 정책

| 항목 | 규정 |
|---|---|
| 허용 문자 | **영문 소문자·숫자·하이픈만.** 한글·공백·특수문자 불허 |
| 생성 시점 | §9.5 ① (페이지 생성) |
| 자동 생성 규칙 | 행사명이 **영문·숫자·공백만으로 구성된 경우에만** 그것을 변환해 사용한다(공백 → 하이픈, 소문자화). 한글이 한 글자라도 포함되면 **로마자 변환을 시도하지 않고** `p-{6자 base36}`(예: `p-k3m9x2`)을 발급한다 |
| 중복 처리 | 동일 slug가 있으면 `-2`, `-3` … 접미사 부여 |
| 수정 가능 | `draft` / `reviewing` 상태에서만 편집기에서 변경 가능. 허용 문자·중복 검사 적용. 로그 `slug_changed` |
| **게시 후 불변** | `published`로 전이한 시점부터 변경 불가. 이후 행사명을 수정해도 URL은 유지된다 |

**로마자 변환을 하지 않는 근거**: §16.1이 "지명·시설명의 영문 변환 금지"를 규정한다. slug는 표시값이 아닌 URL 식별자라 층위가 다르지만, 자동 로마자 변환은 표기 왜곡(`제주` → `jeju`/`jeju-do`/`cheju`)을 낳고 규칙이 모호해진다. **한글이 섞이면 무작위 slug를 발급하고, 원하는 주소는 기획자가 직접 입력**하게 한다.

**한글 slug를 허용하지 않는 근거**: `/p/%EC%A0%9C%EC%A3%BC-...`로 percent-encoding되어 링크 복사·QR·발표 자료에서 읽을 수 없는 형태로 노출된다.

### 12.2 게시 동작

1. 게시 게이트(§11.5)를 확인한다.
2. `status = published`, `published_at` 기록(**최초 게시 시에만**. 재게시 시 덮어쓰지 않는다).
3. `execution_logs`에 `published`(`category = lifecycle`) 기록.
4. `/p/{slug}`가 즉시 공개된다. 재빌드·배포 대기 시간은 없다.

### 12.3 게시 중단

`status = unpublished`로 전이한다. `/p/{slug}`는 404를 반환한다. 이미 접수된 신청 데이터는 삭제하지 않는다. 게시 중단된 상품의 신청 폼은 표시되지 않는다.

---

## 13. Step 6 — 신청 및 자동 이메일

**경로**: `/p/{slug}` 내 `apply` 섹션 / **API**: `POST /api/applications` (인증 불필요)

### 13.1 신청 폼

| 필드 | 형태 | validation |
|---|---|---|
| 이름 | 텍스트 | 2~30자 |
| 이메일 | 이메일 | RFC 형식 검사 |
| 연락처 | 텍스트 | 숫자·하이픈 9~15자 |
| 인원수 | 숫자 | 1~20 정수 |
| 개인정보 수집·이용 동의 | 체크박스 | **필수**. 미체크 시 제출 버튼 비활성 |

이 5개 필드 구성은 **고정 계약**이며 편집기에서 변경할 수 없다(§10.2).

동의 영역에는 수집 항목(이름·이메일·연락처·인원수), 수집 목적(여행 상품 신청 접수 및 안내), 보유 기간을 표시한다.

### 13.2 처리 순서

```text
POST /api/applications
  1. 서버 재검증 (필드 + 동의 여부)          위반 → 400
  2. product.status = published 확인          아니면 → 409
  3. product_snapshot 구성 (행사명·여행기간·숙소·가격·상품 URL)
  4. applications INSERT (email_status='pending', consent_at=now())
  5. execution_logs: application_received 기록   ← 메일보다 먼저 기록한다
  6. 사용자에게 완료 응답 ✅  (이메일 발송을 기다리지 않는다)
  7. after() 안에서 Resend 발송              ← 응답 후 실행을 보장하는 방법
       성공 → email_status='sent',   로그 email_sent(통과)
       실패 → email_status='failed', email_error 기록, 로그 email_sent(반려)
```

**5번을 메일보다 먼저 기록하는 이유**: 로그를 마지막에 쓰면 메일 발송 단계에서 예외·타임아웃이 발생했을 때 신청 접수 기록 자체가 남지 않는다.

**7번을 `after()`로 감싸는 이유**: 서버리스 함수는 응답을 반환하면 실행 컨텍스트가 종료될 수 있어, 응답 후에 그냥 이어 쓴 코드는 실행되지 않을 수 있다. 단 `after()`는 실행 순서를 보장할 뿐 **`maxDuration`을 늘려주지 않는다**(§4.2). 이메일 발송은 AI 호출이 없어 수 초 안에 끝나므로 60초 예산 안에서 안전하다.

### 13.3 이메일

| 항목 | 규정 |
|---|---|
| 발송 수단 | Resend |
| 수신자 | 신청자 이메일 |
| 본문 데이터 출처 | **`product_snapshot`(신청 시점 스냅샷)**. 현재 상품 값을 다시 읽지 않는다 |
| 본문 구성 | 신청자명, 상품명, 여행지, 여행기간, 숙소, 성인·아동 가격, 신청 인원수, 상품 페이지 URL, 문의 안내 |
| 금지 | **총액 계산 금지**. 인원수 × 가격을 계산해 표기하지 않는다 |
| URL 안내 문구 | "상품이 마감·중단된 경우 링크가 열리지 않을 수 있습니다. 문의처로 연락해 주세요." |
| 발신 도메인 | 자체 도메인 인증 전에는 Resend 기본 발신 주소를 사용한다 |
| 실패 처리 | 신청 자체는 성공으로 확정한다. 관리 페이지에서 **[재발송]**(`email_resent` 로그) |

**스냅샷을 쓰는 이유**: 게시 후 가격이 수정되면, 스냅샷 없이는 이미 발송한 메일과 현재 페이지 값이 불일치하고 분쟁의 근거가 된다.

### 13.4 범위 제외

정원·재고 관리, 초과 신청 차단, 중복 신청 방지, 결제는 포함하지 않는다. 인원수는 입력값을 그대로 저장한다.

---

## 14. 관리 페이지 및 인증

### 14.1 라우트

| 경로 | 인증 | 내용 |
|---|---|---|
| `/admin/login` | 불필요 | 비밀번호 입력 |
| `/admin` | 필요 | 상품 목록. 상태 필터: **전체 / 처리중 / 입력오류 / 소개서 완료 / 임시저장 / 검토중 / 게시됨 / 게시중단** (7개 상태 전부) |
| `/new` | 필요 | Step 1 입력 폼 |
| `/admin/products/{id}` | 필요 | 상품 상세 — 소개서, 검증 결과, 로그 요약, 신청 내역 요약 |
| `/admin/products/{id}/edit` | 필요 | Step 4 편집기 + 미리보기 |
| `/admin/applications` | 필요 | 전체 신청 내역, 이메일 발송 상태, 재발송 |
| `/admin/logs/{execution_id}` | 필요 | 실행 로그 단일 뷰 + 이상 플래그 |
| `/p/{slug}` | 불필요 | 공개 상품 페이지 (`published`만 200) |

### 14.2 인증 방식

| 항목 | 규정 |
|---|---|
| 방식 | 단일 공유 비밀번호(환경변수 `ADMIN_PASSWORD`) |
| 세션 | HMAC 서명된 `httpOnly` + `Secure` + `SameSite=Lax` 쿠키, 유효기간 7일 |
| 보호 대상 | `/admin/*`, `/new/*`, 그리고 `POST /api/applications`를 **제외한** 모든 API — Next.js middleware에서 차단 |
| 비교 | 타이밍 공격 방지를 위해 상수 시간 비교 |
| 시도 제한 | IP당 분당 5회 |
| 범위 제외 | 다중 계정, 역할 분리, 기획자별 상품 소유권 |

### 14.3 실행 로그 뷰

- 로그는 `execution_logs`에 단계당 1행씩 append한다. 재시도는 `retry_index`, 재생성·재제출은 `attempt_no`로 구분한다.
- `/admin/logs/{execution_id}` **단일 화면**에서 해당 실행의 전체 이력과 이상 플래그를 모두 확인할 수 있어야 한다. 단계별로 화면이 나뉘지 않는다.
- 화면은 `category` 탭으로 구분한다: **파이프라인**(기본) / 상태변경 / 신청·메일. 파이프라인 탭에는 `category = pipeline` 행만 표시한다.
- 표 컬럼 순서: `타임스탬프 → 시도 → 재시도 → 단계명 → 판정 → 상태 → 입력 → 출력`. 입력·출력은 가공·요약 없이 원본 JSON을 표시한다.
- 타임스탬프는 UTC ISO 8601, 빈 값은 `-`로 표기한다.
- 이상 플래그는 같은 화면 하단에 감지된 것만 나열한다.

### 14.4 API 엔드포인트

| # | 메서드 | 경로 | 인증 | AI | 동작 | 응답 |
|---:|---|---|---|---:|---|---|
| 1 | POST | `/api/products` | 필요 | 0 | 폼 검증 + 상품 등록 + 이미지 업로드 | 200 `{product_id}` / 400 |
| 2 | POST | `/api/products/{id}/decompose` | 필요 | 1 | 정규화 + 일차 분해 + 0차 검증 | 200 / 409(재시도) / 422(입력 오류) |
| 3 | POST | `/api/products/{id}/brochure` | 필요 | 1 | 소개서 생성 | 200 |
| 4 | POST | `/api/products/{id}/validate-brochure` | 필요 | 1 | 1차 검증 | 200 / 409(재시도 필요) |
| 5 | POST | `/api/products/{id}/page` | 필요 | 1 | 페이지 생성 + slug 발급 | 200 |
| 6 | POST | `/api/products/{id}/validate-page` | 필요 | 1 | 2차 검증 | 200 / 409 |
| 7 | POST | `/api/products/{id}/validate-consistency` | 필요 | 1 | 3차 검증 + `draft` 등록 | 200 / 409 |
| 8 | POST | `/api/products/{id}/regenerate` | 필요 | 0 | `attempt_no` +1, 카운터 초기화, 재생성 시작점 지정 | 200 |
| 9 | GET | `/api/products/{id}` | 필요 | 0 | 상태·단계·검증 결과 조회(새로고침 복귀용) | 200 |
| 10 | PATCH | `/api/products/{id}/content` | 필요 | 0 | 편집 저장 | 200 / 400 |
| 11 | PATCH | `/api/products/{id}/slug` | 필요 | 0 | slug 변경 (`draft`/`reviewing`만) | 200 / 409 |
| 12 | POST | `/api/products/{id}/publish` | 필요 | 0 | 게시 (게시 게이트 확인) | 200 / 403 |
| 13 | POST | `/api/products/{id}/unpublish` | 필요 | 0 | 게시 중단 | 200 |
| 14 | POST | `/api/applications` | **불필요** | 0 | 신청 접수 + 메일 발송 | 200 / 400 / 409 |
| 15 | GET | `/api/applications` | 필요 | 0 | 신청 내역 조회 | 200 |
| 16 | POST | `/api/applications/{id}/resend` | 필요 | 0 | 이메일 재발송 | 200 |

**AI 열의 값은 전부 0 또는 1이다** — §4.2 원칙의 준수 여부를 이 표에서 바로 확인할 수 있다.

---

## 15. 상태

### 15.1 상태 × 화면 × 버튼 (단일 기준표)

**이 표가 상태 관련 규정의 단일 출처다.** 다른 절에서 화면·버튼을 언급할 때는 이 표와 일치해야 한다.

2.0~2.1에서 "실제로는 도달할 수 없는 상태를 전제한 기능"이 **세 번** 발견됐다. 상태가 7개인데 상태별 도달 화면과 버튼을 모아둔 표가 없어, 각 절을 따로 쓸 때마다 어긋났기 때문이다.

| `status` | `verdict` | 표시 이름 | 도달 화면 | 제공 버튼 | `/p/{slug}` |
|---|---|---|---|---|---|
| `generating` | — | 처리중 | 진행 표시 | (없음) | 404 |
| `input_error` | — | 입력오류 | 폼 화면 (값 유지) + 사유 | [입력 수정 후 재제출] | 404 |
| `brochure_ready` | `pass` | 소개서 완료 | 소개서 검토 | [상품 생성] · [다시 생성] · [입력 수정] | 404 |
| `brochure_ready` | `fail` | 소개서 완료 | 소개서 검토 + 실패 항목 | [다시 생성] · [입력 수정]<br>**[상품 생성] 잠김** | 404 |
| `draft` | `pass` | 임시저장 | 편집기 / 상세 | [편집] · [게시] · [다시 생성] | 404 |
| `draft` | `fail` | 임시저장 | 편집기 + 실패 항목 | [편집] · [다시 생성] · [책임 게시] | 404 |
| `reviewing` | `pass` | 검토중 | 편집기 | [편집] · [게시] | 404 |
| `reviewing` | `fail` | 검토중 | 편집기 + 실패 항목 | [편집] · [책임 게시] | 404 |
| `published` | 무관 | 게시됨 | 편집기 (게시 중 경고) | [편집] · [게시 중단] | **200** |
| `unpublished` | 무관 | 게시중단 | 편집기 | [편집] · [게시] | 404 |

**규칙**

- `reviewing` · `published` · `unpublished`에는 **[다시 생성]을 제공하지 않는다.** 사람이 편집한 내용이 사라지기 때문이다.
- `brochure_ready` + `fail`에서 [상품 생성]을 잠그는 이유: 소개서가 입력과 어긋난 상태로 페이지를 만들면 오류가 증폭된다. 대신 [다시 생성]·[입력 수정]이 있어 막힌 길이 아니다.
- `input_error`는 **입력 문제 전용**이다. AI가 재시도를 소진한 경우는 `input_error`가 아니라 생성물을 남긴 채 `verdict = fail`이 된다.

### 15.2 상태 전이

| 현재 | 조건 | 다음 | 처리 |
|---|---|---|---|
| — | 폼 제출, 서버 validation 통과(§8.1) | `generating` | 행 생성, `attempt_no = 1`, 카운터 0 |
| `generating` | 일차 분해 실패(초과·구분 불가) | `input_error` | `failure_reason`, `pipeline_aborted` 플래그 |
| `generating` | **0차 검증 재시도 소진** | `input_error` | `failure_reason`, `pipeline_aborted` 플래그. 정규화·분해가 3회 실패하면 입력에 처리 불가 요소가 있다는 뜻이다 |
| `generating` | 소개서 생성 + 1차 검증 통과 | `brochure_ready` | `axis_1 = pass` |
| `generating` | **1차 검증 재시도 소진** | `brochure_ready` | `axis_1 = fail`, `pipeline_aborted` 플래그 |
| `brochure_ready` | [상품 생성] (`verdict = pass`일 때만) | `generating` | 페이지 생성 시작 |
| `generating` | 페이지 생성 + 2·3차 통과 | `draft` | `verdict = pass` 확정, `draft_registered` |
| `generating` | **2·3차 재시도 소진** | `draft` | 해당 축 `fail`, `pipeline_aborted` 플래그 |
| `draft` | 편집기에서 최초 저장 | `reviewing` | `human_edited = true`, 편집 이력 |
| `draft` / `reviewing` / `unpublished` | 게시 게이트 통과 + [게시] | `published` | `published_at` 기록(최초만) |
| `published` | [게시 중단] | `unpublished` | 공개 URL 404, 신청 폼 미표시 |
| `published` | 편집기 저장 | `published` | 게시 유지, 즉시 반영, 편집 이력 |
| `input_error` | [입력 수정 후 재제출] | `generating` | `attempt_no` +1, 카운터 0, §8.2부터 재실행 |
| `brochure_ready` / `draft` | **[다시 생성]** | `generating` | §15.3 |

> `published` 상태에서의 편집은 즉시 공개 페이지에 반영된다. 게시본/작업본 분리(스테이징)는 도입하지 않으며, 편집기 상단에 "이 상품은 현재 게시 중입니다 — 저장 시 즉시 반영됩니다" 경고를 표시한다.

### 15.3 [다시 생성] — `POST /api/products/{id}/regenerate`

입력은 그대로 두고 AI 작업만 다시 실행한다. 2.1에는 "실패 시 재시도 버튼 제공"이 있었으나 그에 대응하는 전이가 없었다.

| 항목 | 규정 |
|---|---|
| 제공 상태 | `brochure_ready`, `draft` (§15.1) |
| 동작 | `attempt_no` +1, `retry_counts` 전부 0, `status = generating`, 로그 `regenerate_requested` |
| 재실행 시작점 | `brochure_ready`에서는 §8.3 ③부터, `draft`에서는 §9.5 ①부터 |
| 보존 | `form_input`, `confirmed_data`, 업로드 이미지 |
| 폐기 | 해당 시작점 이후의 산출물과 이전 `attempt`의 `validation_snapshot`(로그에는 남는다) |
| 제한 | `human_edited = true`인 경우 확인 모달에 "편집한 내용이 사라집니다"를 표시하고 명시적 확인을 받는다 |

---

## 16. 제약

### 16.1 데이터 무결성 (AI 생성 구간)

- 여행지·숙소·상점·공항·항공편 등 고유명사는 사용자가 입력한 명칭을 그대로 사용한다. 존재하지 않는 장소명을 만들거나 유사한 다른 지명·호텔명으로 바꿔 쓰지 않는다.
- 지명·시설명의 표기를 임의로 변경하지 않는다(예: `롯데호텔 제주` → `롯데 제주 호텔` 금지, 약칭·영문 변환 금지). **slug는 표시값이 아니라 URL 식별자이므로 이 규칙의 대상이 아니며, 자동 로마자 변환도 하지 않는다**(§12.1).
- **입력에 없는 경유지·관광지·부대시설·이동 수단을 일정 설명에 추가하지 않는다.** 일차별 서술은 `원문근거`에 등장하는 요소만 사용한다(§6.3).
- 일정 원문에서 일차 구분을 식별할 수 없을 때 임의로 배분하지 않는다.
- 정규화 규칙(§6.2) 3종 외에 입력값을 변형하지 않는다.
- 가격은 입력값을 그대로 사용하고 계산·할인·환산·합계하지 않는다.
- 지정된 여행기간·숙박일정을 초과·미달하는 일정을 구성하지 않는다.
- 선택 항목 미입력 시 `추후 추가 예정`으로 표기하고 섹션을 삭제하지 않는다.
- `form_input`에 출처가 없는 아라비아 숫자를 생성하지 않는다.
- 이미지 슬롯을 재배치하지 않는다.

### 16.2 시스템 제약

- **서버 라우트 1건은 AI를 최대 1회 호출한다**(§4.2). 이를 어기면 `maxDuration`을 초과할 수 있다.
- `published` 외 모든 상태의 상품 페이지는 공개 라우트에서 404다(§15.1).
- `form_input`은 생성 후 불변이다. `confirmed_data`는 같은 `attempt_no` 안에서 불변이다.
- `validation_snapshot`은 편집으로 변경되지 않으며 `attempt_no`가 올라갈 때만 갱신된다.
- 게시된 상품의 slug는 변경할 수 없다.
- `applications`는 익명 사용자에게 `INSERT`만 허용한다.
- 개인정보 수집 동의 없이 신청을 접수하지 않는다.
- 이메일 발송 실패가 신청 접수를 실패시키지 않는다.
- 서버는 프론트엔드 validation을 신뢰하지 않고 동일 규칙으로 재검증한다.
- 신청 폼의 필드 구성과 테마는 편집기에서 변경할 수 없다.
- 이미지 업로드는 `/new` 폼에서만 가능하다.
- **알려진 한계**: Storage 버킷이 읽기 공개이므로 임시저장 상품의 **이미지 파일**은 정확한 URL을 아는 경우 접근 가능하다(§7.3). 페이지 콘텐츠·가격·일정은 노출되지 않는다.

### 16.3 보안 (RLS)

| 테이블 | anon | service role |
|---|---|---|
| `products` | `SELECT` (조건: `status = 'published'`) | 전체 |
| `product_images` | `SELECT` (게시된 상품의 것만) | 전체 |
| `applications` | **`INSERT`만** | 전체 |
| `execution_logs` | 전면 거부 | 전체 |
| `abnormality_flags` | 전면 거부 | 전체 |
| `edit_history` | 전면 거부 | 전체 |

Storage `product-images` 버킷은 읽기 공개, 업로드는 service role만 허용한다. service role 키는 서버 라우트에서만 사용하고 클라이언트에 노출하지 않는다.

---

## 17. 반응형 및 비기능 요건

### 17.1 반응형 보증 방식

서버리스 환경에서 헤드리스 브라우저 검사는 비현실적이므로 **보증 방식을 바꾼다.**

| 계층 | 방식 |
|---|---|
| 컴포넌트 사전 검증 | 9종 섹션 컴포넌트 + 삽입 블록 3종을 개발 단계에서 375 / 768 / 1280px에 대해 1회 검증하고 결과를 문서화한다 |
| 콘텐츠 길이 계약 | 아래 표. 생성·편집 양쪽에서 초과를 차단한다 |
| 이미지 계약 | 고정 종횡비 컨테이너 + `object-fit: cover` + `max-width: 100%` |
| 표·긴 텍스트 | 가로 스크롤이 필요한 요소는 자체 `overflow-x: auto` 컨테이너 안에서만 스크롤한다. 페이지 본문은 가로 스크롤이 발생하지 않는다 |
| 편집기 확인 | 편집기 미리보기에서 375 / 768 / 1280 뷰포트를 전환해 기획자가 직접 확인한다 |

**콘텐츠 길이 계약**

| 항목 | 상한 | 연동된 입력 상한 |
|---|---:|---|
| `hero.headline` | 40자 | 행사명 40자(§7.1) |
| `hero.subcopy` | 80자 | — |
| 일차별 서술 | 200자 | — |
| 섹션 제목 | 30자 | — |
| `free_text` 블록 | **500자** | — |
| `notice` 블록 | 300자 | — |

> AI가 매 실행마다 반응형 검사를 "통과했다고 기록"하는 방식은 실제 검사 없는 형식적 기록이 되므로 채택하지 않는다.

### 17.2 기타

| 항목 | 기준 |
|---|---|
| 접근성 | 모든 이미지에 대체 텍스트, 폼 필드에 label, 본문 대비 4.5:1 이상 |
| 공개 페이지 로딩 | LCP 2.5초 이내. `next/image` 사용, `hero`는 `priority`, 나머지 전 이미지는 `loading="lazy"` |
| 에러 UX | 단계별 진행 표시(§8.5·§9.6), 실패 시 사유와 [다시 생성] 제공 |
| 다크 모드 | 범위 제외. 라이트 테마 단일 |

---

## 18. 범위

### 18.1 포함

- 웹 폼 입력(필수 폼 그룹 6 · 선택 4) 및 이미지 업로드
- 일정 원문의 일차 분해(원문 근거 추적 + 기계 1차 검사 포함)
- 확정 데이터표 구성 및 정규화
- 소개서(간단버전, 8섹션 · 입력된 전 항목 반영) 생성 및 검토 화면
- 상품 페이지 콘텐츠 모델 생성 + 고정 컴포넌트 렌더링
- **검증 4축**(`form_input` 기준) 및 결과 스냅샷
- 섹션 단위 편집기(수정·삭제·삽입·순서변경) + 미리보기
- [다시 생성] 및 시도 회차 추적
- 비밀번호 인증 관리 페이지(상품 목록·상세·신청 내역·실행 로그)
- **게시 및 상품별 고유 URL 실제 공개 배포**, 게시 중단, 책임 게시
- 신청 접수(Supabase) 및 자동 이메일 발송(Resend), 재발송
- 실행 로그 및 이상 플래그 기록

### 18.2 제외

| 제외 항목 | 비고 |
|---|---|
| 결제 | |
| 정원·재고 관리, 초과·중복 신청 차단 | |
| 가격 산정·계산·환산 | 1.0에서 유지 |
| 상품 기획 자체(입력 전 단계) | 1.0에서 유지 |
| 다중 계정·역할 분리·상품 소유권 | 단일 공유 비밀번호로 대체 |
| 게시본/작업본 분리(스테이징) | 게시 중 편집은 즉시 반영 |
| 이미지 AI 생성·보정, 편집기 내 새 업로드 | 업로드는 `/new`에서만 |
| Storage signed URL 프록시 | §7.3의 한계를 감수 |
| 백그라운드 워커·큐·Cron | §4.2의 API 분할로 대체 |
| 상태 폴링 | 클라이언트 순차 호출로 대체 |
| 로마자 자동 변환 slug | §12.1 |
| 다국어, 고급 SEO, 다크 모드 | |
| 헤드리스 브라우저 반응형 자동 검사 | §17.1 방식으로 대체 |
| 이미지-텍스트 내용 정합성 검증 | 기획자 책임 |
| 편집 후 AI 재검증 | 기획자 책임(§10.4) |
| 일차 구분 없는 일정 원문의 자동 배분 | §6.3 — 창작 방지를 위해 실패 처리 |

---

## 19. 성공 기준

예/아니오로 판정 가능한 형태로 작성한다. 판정 단위는 `products.id` 1건이다.

### 실행 구조 (§4.2)

- [ ] §14.4 표의 모든 엔드포인트가 AI를 **최대 1회** 호출한다.
- [ ] 소개서 생성이 4개 요청, 상품 페이지 생성이 3개 요청으로 나뉘어 실행된다.
- [ ] 단일 요청이 60초를 초과해 강제 종료된 건이 0건이다.
- [ ] 재시도가 서버 내부 루프가 아니라 클라이언트 재호출로 이루어지고, 카운터가 `retry_counts`에 보관된다.
- [ ] 상태 폴링을 사용하지 않고, 각 요청의 응답으로 진행 표시가 갱신된다.

### Step 1 — 입력

- [ ] 필수 폼 그룹 6개의 필드 중 **어느 하나라도** 비어 있으면 제출이 차단되고, 서버도 400을 반환한다.
- [ ] 일정 원문 필드가 존재하고, 일차 구분 예시 placeholder가 표시된다.
- [ ] 행사명 40자·여행기간 15일 초과 입력이 차단된다.
- [ ] 선택 4개 항목 미입력 시 `추후 추가 예정`이 채워진다.
- [ ] 아동 요금 미운영 체크 시 `가격.아동`이 `해당 없음`으로 저장된다.
- [ ] 이미지가 지정 슬롯에 그대로 배치되고, 슬롯별 상한과 장당 5MB가 강제된다.
- [ ] `itinerary_day_{n}` 슬롯이 여행 일수만큼만 생성된다.
- [ ] 이미지 업로드가 `/new` 폼에서만 가능하다.
- [ ] `form_input`이 저장되고 이후 수정되지 않는다.
- [ ] `confirmed_data`의 모든 하위 값이 문자열 또는 문자열 배열이다.

### Step 2 — 일차 분해 · 0차 검증 · 소개서

- [ ] `행사정보.일정` 배열 길이가 여행기간 일수와 정확히 일치한다.
- [ ] 각 일차의 `원문근거`가 `일정원문`의 부분 문자열이다(기계 검사).
- [ ] `내용`의 명사구 후보가 §6.3의 3단계 판정을 거친다(기계 1차 필터 → AI 확인).
- [ ] 원문 일차가 적으면 `추후 추가 예정` + `itinerary_partial` 플래그가 기록된다.
- [ ] 원문 일차가 많거나 일차 구분이 없으면 `input_error` + 사유가 폼에 표시된다.
- [ ] **0차 검증**이 정규화 3종 외 변형 0건을 판정한다.
- [ ] 8개 섹션이 순서대로 생성되고 임의 추가 섹션이 0건이다.
- [ ] 필수 폼 그룹 6개의 값이 소개서에 전부 반영되어 있다.
- [ ] 1차 검증이 **`form_input` 기준**으로 8개 섹션 전 필드를 대조한다.
- [ ] 정규화로 인한 표기 차이(콤마·날짜 형식)가 실패로 판정되지 않는다.
- [ ] 미치환 토큰·조사 파이프 기호가 0개다.
- [ ] 소개서에 이미지·테마가 적용되지 않고 단일 컬럼 텍스트로 렌더링된다.
- [ ] 1차 통과 시 `status = brochure_ready` + `axis_1 = pass`가 된다.
- [ ] **1차 재시도 소진 시 `input_error`가 아니라 `brochure_ready` + `axis_1 = fail`이 되고, 검토 화면에서 실패 항목을 볼 수 있다.**
- [ ] `axis_1 = fail`일 때 [상품 생성]이 잠기고 [다시 생성]·[입력 수정]이 제공된다.

### Step 3 — 상품 페이지

- [ ] `page_content` 9개 섹션이 생성되고, 각 필드에 `source` 경로가 기록되어 있다.
- [ ] `theme`이 `page_content` 안에만 있고 `products`에 별도 컬럼이 없다.
- [ ] `itinerary` 섹션이 `data.days[].image_slot`으로 일차 이미지를 참조한다.
- [ ] 2차 검증이 **`form_input` 기준**으로 9개 섹션 전 필드를 대조한다.
- [ ] **2차 실패 시 3차 검증이 호출되지 않는다.**
- [ ] 3차 검증이 대응표 기준으로 `apply`를 제외한 전 섹션을 대조하고, 일정 행은 **일차 수와 사실정보 값**을 본다(`원문근거`를 대조하지 않는다).
- [ ] 서술 분량·어순 차이만을 이유로 3차가 실패 판정된 건이 0건이다.
- [ ] `hero.headline`이 40자 이내이고 행사명을 잘라낸 건이 0건이다.
- [ ] 통과 시 `draft` + `verdict = pass`, 재시도 소진 시 `draft` + `verdict = fail`이 된다.

### Step 4 — 편집기

- [ ] 섹션 수정·삭제·삽입·순서변경이 동작하고 저장 후 미리보기에 반영된다.
- [ ] `hero`·`apply` 삭제가 차단되고, `apply`의 신청 폼 필드 구성을 편집할 수 없다.
- [ ] `image` 블록이 이미 업로드된 이미지 중에서만 선택된다.
- [ ] 테마를 변경할 수 없다.
- [ ] 길이 계약(6종) 초과 저장이 400으로 거부된다.
- [ ] 저장 시 `human_edited = true`, `draft` → `reviewing` 전이, `edit_history` 기록이 이루어진다.
- [ ] 편집으로 `form_input`·`confirmed_data`·`validation_snapshot`·`attempt_no`가 변경되지 않는다.
- [ ] 검증 배지와 편집 배지가 **독립적으로** 표시되어, `AI 검증 실패` + `사람 편집됨`이 함께 보인다.
- [ ] 375 / 768 / 1280 미리보기 전환이 동작하고 세 폭 모두 가로 스크롤·겹침·잘림이 0건이다.
- [ ] `verdict = fail`인 상품도 편집기에 진입할 수 있다.

### 상태·재생성 (§15)

- [ ] §15.1 표의 10개 행이 실제 화면·버튼과 정확히 일치한다.
- [ ] `input_error`가 **입력 문제로만** 발생하고, AI 재시도 소진으로 발생한 건이 0건이다.
- [ ] `brochure_ready`·`draft`에서 [다시 생성]이 제공되고, `reviewing`·`published`·`unpublished`에서는 제공되지 않는다.
- [ ] [다시 생성] 시 `attempt_no`가 +1되고 `retry_counts`가 0으로 초기화되며 `regenerate_requested` 로그가 남는다.
- [ ] [다시 생성]이 `form_input`·`confirmed_data`·업로드 이미지를 보존한다.
- [ ] `human_edited = true`인 상품에 [다시 생성]을 누르면 소실 경고 확인을 받는다.
- [ ] §15.2에 정의되지 않은 상태값·전이가 0건이다.

### Step 5 — 게시

- [ ] 상품마다 서로 다른 고유 URL(`/p/{slug}`)을 가진다.
- [ ] slug가 영문 소문자·숫자·하이픈으로만 구성되고 percent-encoding이 발생하지 않는다.
- [ ] 행사명에 한글이 포함되면 로마자 변환을 시도하지 않고 `p-{base36}`이 발급된다.
- [ ] 게시 후 인증 없이 인터넷에서 해당 URL에 접속해 페이지를 볼 수 있다.
- [ ] **`published`가 아닌 6개 상태 전부**의 URL이 404를 반환하고, 인증 없이 내용을 볼 수 있는 경로가 0건이다.
- [ ] `verdict = fail`이면 실패 항목 열람 + 책임 확인 후에만 게시되고, `publish_override_at`·`publish_override` 로그·배지가 기록된다.
- [ ] `input_error`인 상품에는 게시 경로가 없다.
- [ ] 게시 후 행사명을 수정해도 slug가 변경되지 않는다.
- [ ] 재게시 시 `published_at`이 덮어써지지 않는다.
- [ ] 게시 중단 시 URL이 즉시 404가 되고 기존 신청 데이터는 삭제되지 않는다.

### Step 6 — 신청 및 이메일

- [ ] 동의 미체크 시 제출할 수 없고 서버도 거부한다.
- [ ] 신청이 적재되고 `consent_at`이 기록된다.
- [ ] `application_received` 로그가 **이메일 발송보다 먼저** 기록된다.
- [ ] 완료 응답이 이메일 발송을 기다리지 않고 즉시 반환된다.
- [ ] 발송이 `after()` 안에서 실행되어 응답 후에도 실제로 발송된다.
- [ ] `product_snapshot`이 저장되고 이메일 본문이 스냅샷 값을 사용한다.
- [ ] 이메일이 실제 수신되고 총액 계산 표기가 0건이며, 링크 만료 안내 문구가 포함된다.
- [ ] 발송 실패 시에도 신청은 성공 처리되고 `email_status = failed`가 기록되며 재발송할 수 있다.
- [ ] `published`가 아닌 상품에 대한 신청이 409로 거부된다.
- [ ] 익명 키로 `applications`를 `SELECT`하려는 시도가 차단된다.

### 관리 페이지 · 로그

- [ ] 비밀번호 없이 `/admin/*`, `/new`에 접근하면 로그인으로 리다이렉트된다.
- [ ] `POST /api/applications`를 제외한 모든 API가 인증 없이 호출되면 거부된다.
- [ ] 관리 페이지 1곳에서 **7개 상태 전부**를 필터로 조회할 수 있다.
- [ ] 신청 내역과 발송 상태를 확인할 수 있고 연락처가 기본 마스킹된다.
- [ ] `/admin/logs/{execution_id}` 단일 화면에서 전체 단계 이력과 이상 플래그를 확인할 수 있다.
- [ ] 로그 화면이 `category` 탭으로 구분되고 파이프라인 탭에 신청 로그가 섞이지 않는다.
- [ ] 로그 표에 `attempt_no`와 `retry_index`가 모두 표시되어 시도 회차와 재시도 회차가 구분된다.
- [ ] §5.4 표의 `pipeline` 10개 단계가 성공·실패 모두 기록된다.
- [ ] §5.5 표의 이상 플래그 5종이 정의된 조건대로 기록된다.
- [ ] 동일 단계 재시도가 2회를 초과하지 않는다.
- [ ] 폼 validation·일차 분해 실패에 재시도 카운터가 적용되지 않는다.

### 데이터 무결성

- [ ] AI 생성분에서 `form_input`에 없는 고유명사가 0건이다.
- [ ] 지명·시설명 표기 임의 변경(약칭·영문 변환·어순 변경)이 0건이다.
- [ ] 가격이 입력값 그대로이고 계산·할인·환산·합계가 0건이다.
- [ ] `form_input`에 출처 없는 아라비아 숫자가 0건이다.
- [ ] 소개서와 상품 페이지의 고유명사 표기가 서로 동일하다.
- [ ] 일차별 서술에 `원문근거` 밖의 장소·활동·이동이 0건이다.

---

## 20. 시연 시나리오 (3분)

| 시간 | 동작 | 보여줄 것 |
|---|---|---|
| 0:00 | `/admin/login` 비밀번호 입력 | 기획자 전용 게이트 |
| 0:15 | `/new` 폼 입력(일정 원문 포함) + 이미지 3장 업로드 | 필수/선택 구분, 일정 자유 입력, 슬롯 지정 |
| 0:50 | [소개서 생성] | **4단계 진행 표시** → 일차 분해 결과 + 8섹션 소개서 + 검증 배지 |
| 1:15 | [상품 생성] | **3단계 진행 표시** → 임시저장 등록, 검증 4축 결과 |
| 1:35 | 편집기 → 문구 수정 + 섹션 1개 삭제 + slug 지정 | 섹션 편집, 375/768/1280 전환, 배지 2종 동시 표시 |
| 2:10 | [게시] | 확인 모달 → 공개 URL 즉시 활성 |
| 2:20 | 다른 브라우저(비로그인)로 `/p/{slug}` 접속 | 실제 배포 확인. draft URL은 404 |
| 2:40 | 신청 폼 제출 | 완료 응답 → 수신함에 이메일 도착 |
| 2:55 | `/admin/applications`, `/admin/logs/{id}` | 신청 내역, 파이프라인 로그(시도·재시도 구분) |

---

## 21. 잔여 결정 사항

| 항목 | 현재 가정 | 확정 필요 시점 |
|---|---|---|
| Resend 발신 도메인 | 미인증 상태로 기본 발신 주소 사용 | 시연 전 |
| `gallery` 슬롯 렌더 위치 | `apply` 직전 별도 갤러리 블록 | 컴포넌트 구현 시 |
| 개인정보 보유 기간 문구 | 신청 접수일로부터 1년 | 폼 구현 시 |
| 테마 키 목록 | 여행스타일 select 항목과 1:1 | 폼 구현 시 |
| 일차 구분 표기 인식 범위 | `n일` · `n일차` · `첫째 날` · `Day n` | 일차 분해 프롬프트 작성 시 |
| `내용` 명사구 후보 추출 방식 | 2자 이상 한글 명사구 + 영문 고유명사 | 0차 검증 구현 시 |

---

## 부록 A. 1.0 대비 개정 내역

| 1.0 규정 | 2.x 처리 | 이유 |
|---|---|---|
| 범위(제외) "정식 게시" | **삭제** — 게시는 핵심 기능 | Step 5 신설 |
| 최종 상태값 `draft` 유지, 자동 정식 게시 0건 | `published` 추가. **사용자 명시 조작에 의한** 게시만 허용(자동 게시는 계속 0건) | Step 5 신설 |
| 입력 = 자유 텍스트 / 콤마 구분 프롬프트 | 웹 폼으로 대체. 일정만 자유 텍스트 유지 | Step 1 |
| 선택 항목 미입력 시 AI가 확인 질문 | 폼 UI가 대체. 미입력 → `추후 추가 예정` 자동 | Step 1 |
| 소개서 8개 섹션 필수 | **8개 섹션 유지.** '간단버전'은 서술 분량만 축소 | Step 2 |
| 검증 = 확정 데이터표 → 소개서 → 웹페이지 직렬 | **`form_input` 기준 4축**으로 재편 | §11.1 |
| 소개서 vs 웹페이지 전 항목 **문장** 문자열 일치 | 대응표 기준 `apply` 제외 전 섹션을 **사실정보 값 단위**로 대조 | §11.1 |
| 산출물 = `outputs/run-{id}/` 하위 4개 파일 | Supabase 테이블·jsonb·Storage로 이전 | 서버리스 파일시스템 쓰기 불가 |
| 로그 = 단일 `log.html` 파일 + 고정 HTML 마크업 | `execution_logs` 테이블 + `/admin/logs/{id}` 단일 화면 | 동일 |
| 소개서·웹페이지 = HTML 생성물 | **JSON 콘텐츠 모델 + 고정 컴포넌트 렌더링** | 섹션 편집기·반응형 보증 성립 조건 |
| 동일 단계 재시도 3회 | **2회**, 클라이언트 재호출 방식 | 실행 시간 예산 |
| 375/768/1280 렌더링 검사 통과 필수 | 컴포넌트 사전 검증 + 콘텐츠 길이 계약으로 대체 | 서버리스에서 헤드리스 검사 비현실적 |
| `숙박일정` 필수/선택 모순 | **선택**으로 확정 | §6.4 |
| 상태값 4종(처리중·임시저장·검토중·반려) | **7종** + §15.1 단일 기준표 | Step 2·5 신설 |
| 검증 통과 = 게시 시점까지 유효 | 검증 기준 시점을 고정, 편집분은 기획자 책임 | Step 4 신설 |
| 이미지 / 인증·권한 / 개인정보·RLS | 신규 정의(§7.3 / §14.2 / §13.1·§16.3) | 1.0에 개념 없음 |

---

## 부록 B. 2.0 대비 개정 내역

| # | 2.0의 문제 | 2.1 처리 | 심각도 |
|---:|---|---|---|
| 1 | **일정 입력 필드가 없어** AI가 일차별 서술을 창작해야 했고 §16.1을 필연적으로 위반했다 | 일정 원문 필수 필드 + 원문 근거 추적 규칙 신설 | 치명 |
| 2 | `hero.headline` 40자 vs 행사명 60자 | 행사명 상한 **40자** | 충돌 |
| 3 | 이미지 20장 vs 여행기간 30일 | 여행기간 **15일**, 이미지 슬롯 상한 정합 | 충돌 |
| 4 | 응답 후 메일 발송이 실행되지 않을 수 있었다 | `after()` 명시 | 충돌 |
| 5 | 신청 로그가 메일 발송 뒤에 있어 발송 실패 시 기록이 남지 않았다 | 로그를 INSERT 직후로 이동 | 충돌 |
| 6 | `verdict = fail`이 도달 불가능했다 | 2·3차 실패는 `draft` + `fail`, 책임 게시 신설 | 충돌 |
| 7 | 소개서만 만든 상품이 목록에서 사라졌다 | `brochure_ready` 신설, 필터 전 상태 확장 | 충돌 |
| 8 | "필수 필드가 **모두** 비어 있으면 차단" | "**어느 하나라도**" | 문구 오류 |
| 9 | 콤마 "처리"가 모호, 값 타입 규정 소실 | 콤마 **제거** 확정, 전 값 문자열 복원 | 중대 |
| 10 | 3차 검증 목적·실행 조건 미정 | 교차·회귀 목적 명시 + 2차 실패 시 생략 | 중대 |
| 11 | 신청 로그가 파이프라인 로그를 덮었다 | `category` 컬럼 + 탭 분리 | 중대 |
| 12 | 한글 slug의 percent-encoding | slug 영문·숫자·하이픈 고정 | 중대 |
| 13 | Storage 공개 vs "기획자만" 모순 | UUID 경로 + 한계 명시 | 중대 |
| 14 | `apply` 신청 폼이 편집 가능했다 | 안내 문구·제목만 편집 | 중대 |
| 15 | `가격.아동` 0원 표시 | "미운영" 체크 → `해당 없음` | 중대 |
| 16 | 404 대상 열거 누락 | "`published` 아닌 모든 상태"로 통일 | 중대 |
| 17 | 소개서 20초가 5섹션 기준 | 재산정 | 경미 |
| 18 | `data` 카운터가 사용자 재입력에 적용 | 카운터 3종, 폼 실패 미적용 | 경미 |
| 19 | API 표 부재 | §14.4 신설 | 경미 |
| 20 | 게시 중단 후 메일 URL 404 안내 없음 | 안내 문구 필수화 | 경미 |

---

## 부록 C. 2.1 대비 개정 내역

2.1을 6개 렌즈(내부 일관성 · 참조 정합성 · 기술적 실행 가능성 · 검증 실행 가능성 · checklist 정합성 · 완결성)로 검증해 찾은 18건을 반영했다.

| # | 2.1의 문제 | 2.2 처리 | 심각도 |
|---:|---|---|---|
| 1 | 소개서 생성이 동기 처리인데 AI 3회 × 25초 = **75초 > `maxDuration` 60초** | **§4.2 1요청 1AI호출 원칙** 신설. 소개서 4요청·페이지 3요청으로 분할(§8.5·§9.6) | 치명 |
| 2 | `202` + 폴링이 서버리스에서 성립하지 않음. `after()`는 시간 한도를 늘려주지 않는데 시간 해결책처럼 기술 | 폴링 폐기, 클라이언트 순차 호출. `after()`의 한계를 §4.2·§13.2에 정확히 기술 | 치명 |
| 3 | 3차 검증이 `원문근거` 대조를 지시하는데 그 필드가 두 콘텐츠 모델에 **존재하지 않음** | 대조 대상을 **일차 수 + 사실정보 값**으로 수정. `원문근거`는 0차의 몫. 아울러 사용자 결정에 따라 **기준값을 `form_input`으로 교체하고 0차 검증 신설** | 치명 |
| 4 | §8.4 "1차 검증 실패 시 [상품 생성] 비활성화"가 **도달 불가능**(1차 실패 → `failed` → 검토 화면 진입 불가) | 1차 재시도 소진 시 `brochure_ready` + `axis_1 = fail`. 검토 화면에서 실패 항목 열람 가능 | 치명 |
| 5 | `failed`(AI 소진)에서 **재실행 경로가 없음**. "재시도 버튼 제공"에 대응하는 전이 부재 | **[다시 생성]** 신설(§15.3, API #8). `failed` → `input_error`로 의미를 입력 오류 전용으로 축소 | 치명 |
| 6 | "6"이 **필수 폼 그룹 6**과 **데이터 키 6**의 두 의미로 혼용 | §6.0 용어 구분표 신설 | 중대 |
| 7 | 이상 플래그 감지 조건이 2.0 → 2.1에서 **소실**(5종 중 1종만 정의). checklist는 5종 요구 | §5.5 감지 조건표 복원·확장 | 중대 |
| 8 | 배지 4종의 조건이 겹치고, `fail`일 때 편집 여부가 사라짐 | **검증 배지 + 편집 배지 2축 독립**으로 재설계(§10.4) | 보통 |
| 9 | `theme`이 `products` 컬럼과 `page_content`에 **이중 저장**, 동기화 규칙 없음 | `products.theme` 컬럼 **삭제**, `page_content.theme` 단일 출처 | 보통 |
| 10 | 재제출 회차를 구분할 필드가 없어 `retry_index = 0` 행이 여러 벌 쌓임 | **`attempt_no`** 신설(products·execution_logs), 로그 표에 표시 | 보통 |
| 11 | `itinerary` 섹션이 일차 이미지를 가리키는 필드가 스키마에 없어 이미지 검증이 대상을 찾을 수 없었다 | `data.days[].image_slot` 정의(§9.2) | 보통 |
| 12 | `free_text` 블록의 길이 상한이 없어 §10.2의 길이 계약이 그 블록에 작동하지 않았다 | `free_text` **500자** 추가(§17.1) | 보통 |
| 13 | `image` 삽입 블록의 이미지 출처가 미정의 — 새 업로드를 허용하면 슬롯 체계·총량 제한 밖으로 새어 나감 | **업로드된 이미지 중 선택만** 허용, 업로드는 `/new`에서만(§7.3·§10.2) | 보통 |
| 14 | slug 자동 생성 "영문·숫자로 변환 가능하면"의 판정 기준 모호 + §16.1 영문 변환 금지와의 층위 미구분 | 한글 포함 시 **로마자 변환 시도 금지**, `p-{base36}` 발급. §16.1에 층위 명시 | 보통 |
| 15 | `내용`의 "원문근거 밖 요소 없음" 판정 주체·방법 미기술 — AI 자기검사에만 의존 | §6.3 **판정 3단계**(기계 부분문자열 → 기계 명사구 후보 → AI 확인) | 보통 |
| 16 | LCP 2.5초 vs 이미지 27장인데 지연 로딩 미기술 | `next/image` + `hero` priority + 나머지 lazy 명시(§7.3·§17.2) | 경미 |
| 17 | `itinerary_decomposed`·`draft_registered` 등 로그 기록 시점이 본문에 없었다 | §5.4 표에 **기록 시점 열** 추가, 각 API 절에도 명시 | 경미 |
| 18 | §19의 3차 검증 항목이 조건부인데 무조건처럼 표기 / 총 27장이 도달 불가한 조건 | 조건 명시, 총량 규정을 "슬롯별 상한 준수 + 절대 상한 27장"으로 수정 | 경미 |

### 이번 개정의 구조적 조치

2.0(1건) · 2.1(2건) 합계 **3건이 "실제로 도달할 수 없는 상태를 전제한 기능"**이었다. 원인은 상태가 7개인데 상태별 도달 화면·버튼을 모아둔 표가 없어 각 절이 서로 어긋난 것이다.

→ **§15.1 상태 × 화면 × 버튼 표**를 상태 규정의 단일 출처로 신설했다. 이후 화면·버튼을 다루는 모든 절은 이 표와 대조해야 하며, 표에 없는 조합은 존재하지 않는 것으로 간주한다.
