# spec.md — zentrip: 여행 상품 페이지 자동 생성·배포 플랫폼

> **문서 버전 2.8** (2026-08-13) · 개정 이력: **[CHANGELOG.md](./CHANGELOG.md)**
>
> 2.7 → 2.8은 **상품 상세페이지의 「정해진 형식」을 없앴다.** 고정 9섹션·고정 순서·소개서 8↔페이지 9 1:1 대응표를 버리고,
> **AI가 디자이너가 되어 구성·순서·분위기·블록별 레이아웃을 정한다**(§9.2·§9.3). 대신 두 경계를 지킨다 —
> ① **사실정보 값은 AI를 거치지 않는다.** 값은 `web-content-structure-gen`이 `confirmed_data`에서 치환하고, AI는 디자인 스펙 + 블록 계획 + `source:"generated"` 서술만 만든다(§16.1 구조적 보장).
> ② **색은 AI가 쓰지 않는다.** AI는 「색의 의도」(hue+mood)만 쓰고 기계가 OKLCH로 색을 계산해 WCAG 대비 4종을 강제한다(§9.4·§17.2). 어휘·재료·layout·길이 계약은 `lib/pipeline/vocabulary.ts`가 단일 출처다.
> 검증은 「섹션 대응」에서 **「`source` 커버리지」**로 옮겼다(§9.5·§11.4) — 확정 데이터의 모든 사실정보가 페이지 어느 블록에든 있으면 통과다.
> **폼·0차 검증·소개서(8섹션 고정)는 손대지 않았다.** 실행 근거는 `.claude/harness/manifest.json`(page 체인 6→7)과 `명령서.txt`.
>
> 2.6 → 2.7은 **데이터 모델과 입력 경로**를 바꿨다 — 둘 다 「한 상품에 값이 여러 개인 경우」가 예외가 아니라 일반이라는 데서 나왔다.
> ① `숙박`·`상점`을 **객체 배열**로 올렸다(§6.1·§7.1·§7.2·§7.4·§8.7·§9.3·§11.1). 숙소를 옮겨 다니는 일정과 카페·음식점 목록이 단일 객체에 담기지 않는다.
> ② **자연어 초안 라우트 §7.5**(#20)를 신설했다. 폼 앞단에서 AI가 기획해 초안을 만들고, `form_input` 확정은 여전히 사람이 한다.
> 나머지 절의 규정은 2.6과 동일하다. AI 공급자·검증 4축·상태 기계·응답 코드는 손대지 않았다.
>
> 2.5 → 2.6은 **§4·§4.3·§21만** 바뀌었다 — 주 AI 공급자를 Gemini 무료 티어 → **DeepSeek**으로 교체하고 Gemini를 예비 경로로 내렸다.
> (2.4 → 2.5는 같은 범위에서 Claude → Gemini 교체였다.)
>
> **판정 기준은 「이 문서만 보고 제3자가 구현할 수 있는가」다.** 규정은 §1~§21 본문에 전부 있고, 「왜 그렇게 됐는지」는 CHANGELOG에 있다.

---

## 1. 목표

기획이 끝난 여행 상품 정보를 웹 폼으로 입력하면, AI가 여행 상품 소개서(간단버전)와 고객용 반응형 상품 페이지를 생성하고, 기획자가 편집기에서 검토·수정한 뒤 버튼 하나로 상품별 고유 URL에 실제 배포하며, 고객의 신청을 데이터베이스에 적재하고 상품 정보 이메일을 자동 발송하는 것까지를 하나의 흐름으로 완결한다.

**측정 기준**: 기획자 1명이 상품 정보 입력부터 공개 URL 배포까지 **10분 이내**, 코드·디자인 작업 **0건**으로 완료할 수 있다.

---

## 2. 맥락

### 2.1 대상

| 역할 | 설명 | 접근 범위 |
|---|---|---|
| 기획자 | 여행 상품을 기획·등록하는 담당자 | 관리 페이지 전체(비밀번호 인증) |
| 고객 | 상품 페이지를 열람하고 신청하는 일반 이용자 | 게시된 상품 페이지, 신청 폼 |

### 2.2 사용 목적

기획이 끝난 여행 상품을 페이지 제작·디자인·배포 등 여러 과정을 거치지 않고 자동화하여, 시간적 자원과 인적 자원을 효율적으로 사용한다.

### 2.3 AI의 기여 범위 (명시적 정의)

입력이 웹 폼으로 구조화되었으므로, AI가 담당하는 일과 담당하지 않는 일을 아래와 같이 확정한다. 이 경계는 심사·검증 시 판정 기준으로 사용한다.

| AI가 하는 일 | AI가 하지 않는 일 |
|---|---|
| 폼 값의 표기 정규화 및 확정 데이터표 구성 | 값의 생성·추정·보완 |
| **일정 원문의 일차 분해·구조화**(§6.1) | 원문에 없는 일차·장소·활동·이동 생성 |
| 자유 서술 필드(일정·식사·상점정보) 내부의 요소 추출·분류 | 필수값 누락 판정(프론트엔드 validation이 담당) |
| 섹션별 문장 작성(개요·일차별 서술 문장·안내 문구) | 사실정보 변경, 요약·윤문에 의한 값 변형 |
| 여행스타일 기반 테마(컬러·헤드라인 톤·강조점) 결정 | 레이아웃·컴포넌트 구조 결정(고정 컴포넌트가 담당) |
| 폼 입력 대조 검증(사실정보·정합성) 판정 | 재시도 여부 판단(클라이언트·기획자가 담당) |
| 이미지의 지정 슬롯 배치 | 이미지 슬롯 재배치, 이미지 생성·보정 |

---

## 3. 최종 산출물 — 사용자 여정

```text
[기획자]                                              [고객]
   │
   │ ① 비밀번호 로그인 → /new
   ▼
웹 폼 입력 (필수 폼그룹 6 · 선택 4) + 이미지 업로드
   │
   │ ② [소개서 생성] 버튼 — 4개 요청을 순차 실행 (§8.5)
   │      ├─ 상품 등록 (AI 0회)           status = generating
   │      ├─ 일차 분해 + 확정 데이터표 + 0차 검증
   │      ├─ 소개서 생성
   │      └─ 1차 검증                    status = brochure_ready
   ▼
소개서 검토 화면 (8개 섹션 · 입력 전 항목 반영)
   │
   │ ③ [상품 생성] 버튼 — 3개 요청을 순차 실행 (§9.6)
   │      ├─ page_content 생성 + slug 발급
   │      ├─ 2차 검증
   │      └─ 3차 검증                    status = draft
   ▼
임시저장 목록 (/admin — 기획자만)
   │
   │ ④ [편집] 버튼 (편집 없이 바로 ⑤로 갈 수도 있다)
   ▼
편집기: 섹션 수정 · 삭제 · 삽입 · 순서변경 + 실시간 미리보기
   │                              status = reviewing, human_edited = true
   │ ⑤ [게시] 버튼
   ▼
status = published → 공개 URL 배포 ─────────────────────▶ /p/{slug}
                                                            │
                                                            │ ⑥ 신청
                                                            ▼
                                              Supabase applications 적재
                                                            │
                                                            ▼
                                              Resend 상품 정보 이메일 자동 발송
                                                            │
관리 페이지 신청 내역 ◀──────────────────────────────────────┘
```

---

## 4. 시스템 구성 (확정 사항)

| 구분 | 선택 | 근거 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | 서버 라우트·동적 라우트·Vercel 네이티브 |
| 배포 | Vercel 단일 프로젝트, 상시 배포 | 게시마다 재빌드하지 않으므로 게시 지연 0초, 게시 취소도 즉시 |
| 데이터베이스 | Supabase Postgres | 상품·신청·로그·편집이력 |
| 이미지 저장소 | Supabase Storage | 업로드 이미지 |
| AI | **DeepSeek API 직접 호출** (OpenAI 호환 SDK). 예비 경로로 Gemini(`@google/genai`) | 서버리스 환경에서 응답 지연·배포 복잡도 최소. 종량제라 잔량이 예측 가능하고, 공급자 2개로 단일 실패점을 없앤다 |
| 이메일 | Resend | Vercel 서버 라우트에서 직접 발송 |
| 인증 | 단일 공유 비밀번호 + 서명 쿠키 | 기획자 전용 게이트, 시연 단순성 |

**환경 변수** — 전부 서버 전용이며 클라이언트 번들에 노출하지 않는다(§16.3).

| 변수 | 용도 |
|---|---|
| `DEEPSEEK_API_KEY` | AI 호출(§4.3). **주 공급자** 키 |
| `GEMINI_API_KEY` | 선택. 예비 경로용 AI Studio **무료 티어** 키(§4.3) |
| `AI_PROVIDER` | 선택. 비우면 `deepseek`. `gemini`를 넣으면 예비 경로로 전환(§4.3) |
| `AI_MODEL` | 선택. 같은 공급자 안에서 모델만 교체 — 과부하 시 대비용(§4.3). **pro 계열은 거부된다** |
| `SUPABASE_URL` | DB·Storage 엔드포인트 |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버 라우트 전용 DB·Storage 접근 |
| `RESEND_API_KEY` | 이메일 발송(§13.3) |
| `ADMIN_PASSWORD` | 기획자 인증(§14.2) |
| `SESSION_SECRET` | 세션 쿠키 HMAC 서명 키(§14.2) |
| `SITE_URL` | 이메일 본문의 절대 URL 생성용. 예: `https://zentrip.example.com` |
| `CONTACT_INFO` | 이메일 본문의 문의처 안내(§13.3). 폼 입력 항목이 아니다 |

`NEXT_PUBLIC_` 접두사를 붙인 변수는 두지 않는다. 공개 페이지도 서버 렌더링하므로 클라이언트에서 Supabase를 직접 호출하지 않는다.

### 4.1 게시 = 공개 노출 전환 (배포 방식 확정)

게시는 **DB 상태값 전환**으로 구현하며, 공개 상품 페이지는 Vercel에 상시 배포된 앱의 동적 라우트 `/p/{slug}`가 서빙한다.

- **상품마다 별도 URL**: `/p/{slug}` — 상품별 고유 slug로 각각 다른 URL을 가진다.
- **실제 배포**: 게시된 페이지는 인증 없이 인터넷에서 접근 가능한 실제 공개 웹페이지다.
- **`published` 외 전 상태는 404**: `/p/{slug}`는 `status = published`일 때만 200을 반환한다. 그 외 6개 상태는 예외 없이 404다(§15.1 표 최우측 열).
- **draft는 배포하지 않는다**: 임시저장 페이지는 DB에만 존재하고, 편집기 내부 미리보기(`/admin/products/{id}/edit`)로만 렌더링된다.

> 게시마다 재빌드(Deploy Hook)하는 방식을 채택하지 않은 이유: 게시당 1~3분 지연이 발생하고, 빌드 실패 시 시연이 중단되며, draft 파일을 Vercel에 올리는 순간 URL을 아는 사람이 열람할 수 있어 "기획자만 확인 가능" 요건을 무료 플랜에서 만족시킬 수 없다.

### 4.2 실행 시간 제약과 1요청 1AI호출 원칙

**제약**: Vercel 서버리스 함수는 요청 1건을 정해진 시간 안에 끝내야 하고, 초과하면 강제 종료된다. 무료(Hobby) 요금제의 상한은 **60초**이며 설정으로 늘릴 수 없다. 응답을 먼저 반환하는 `after()` / `waitUntil`은 **실행 순서를 바꿔줄 뿐 시간 한도를 늘려주지 않는다.**

> 요금제별 상한은 서비스 정책에 따라 바뀔 수 있다. 배포 전 현재 문서를 확인한다.

**원칙**: 서버 라우트 1건은 **AI를 최대 1회만 호출한다.**

| 항목 | 규정 |
|---|---|
| 요청당 AI 호출 | 최대 1회 |
| AI 호출 1회 타임아웃 | 25초 |
| 서버 라우트 `maxDuration` | 60초 |
| 여러 AI 호출이 필요한 작업 | **여러 API로 분할하고 클라이언트가 순차 호출**한다 |
| 재시도 | 서버 내부 루프가 아니라 **클라이언트가 같은 API를 재호출**한다. 카운터는 `products.retry_counts`에 보관 |
| 진행 표시 | 클라이언트가 현재 호출 중인 단계를 화면에 표시한다. 폴링은 사용하지 않는다 |

이 원칙에 따라 소개서 생성은 **4개 요청**, 상품 페이지 생성은 **3개 요청**으로 나뉜다(§8.5·§9.6).

### 4.3 AI 호출 계약

AI가 5개 작업(일차 분해·소개서 작성·1차 검증·페이지 생성·2·3차 검증)을 수행하므로, 그 인터페이스를 여기서 확정한다.

**호출 방식**: 주 공급자 DeepSeek은 OpenAI 호환 엔드포인트를 공식 `openai` SDK의 `baseURL`만 바꿔 호출한다(DeepSeek이 문서화한 방식). 예비 경로 Gemini는 Google 공식 SDK(`@google/genai`)를 쓴다. 어느 쪽도 원시 HTTP를 쓰지 않는다.

**라우트는 `lib/ai`의 provider 중립 인터페이스만 호출한다.** 모델·공급자를 바꿔도 파이프라인 6개 라우트는 수정하지 않는다. 공급자 선택은 `AI_PROVIDER` 하나이며, 비우면 `deepseek`이다.

| 항목 | 값 |
|---|---|
| 모델 | **`deepseek-v4-flash`** (아래 실측 근거). **`deepseek-v4-pro`는 사용하지 않는다** |
| 최대 출력 토큰 | 8000. 콘텐츠 모델 JSON은 이 범위를 넘지 않는다 |
| 스트리밍 | 사용하지 않는다. 출력 상한이 낮아 비스트리밍이 안전하다 |
| 사고 깊이 | **3종.** 생성은 `reasoning_effort: medium`, 검증은 `low`, **자연어 초안(§7.5)은 사고를 끈다**(`thinking: {type:"disabled"}`) (예비 경로는 `thinkingLevel` `MEDIUM`/`LOW`) |
| 요청 타임아웃 | **55초.** 요청마다 `AbortSignal.timeout`으로 적용한다 — SDK의 `timeout` 옵션만으로는 끊기지 않는다(아래 실측). 2.6까지는 25초, 2.7은 40초였다 — DeepSeek 분해가 45~47초로 상습 초과해 55초로 올렸다(2026-08-13 실측) |
| SDK 재시도 | **없음**(`maxRetries: 0`). 재시도는 클라이언트가 담당하므로(§4.2) SDK가 자동 재시도하면 25초 예산을 초과한다 |
| 요금제 | **종량제.** 잔액이 있어야 호출된다 |

**모델 제약** — 규정이며 선택지가 아니다.

| 항목 | 내용 |
|---|---|
| 사용 모델 | **`deepseek-v4-flash` 하나.** 25초 예산·비용·산출물 품질이 전부 이 모델로 성립하는 것을 실측으로 확인했다 |
| `pro` 계열 | **금지.** `AI_MODEL`에 `pro`가 들어오면 provider가 호출 전에 예외를 던진다. 「급해서 일단 올려본다」를 환경 변수로 뚫을 수 없게 하는 것이 이 관문의 목적이다 |
| `AI_MODEL` 허용 범위 | flash 계열만. 그 외 값도 같은 관문에서 거부된다 |

**모델 선정 근거 (2026-08-12 실측)** — 파이프라인이 실제로 쓰는 스키마 3종(`npm run probe:deepseek`), 4회 반복:

**초안 호출에서 사고를 끄는 근거 (2026-08-12 실측)** — 같은 입력·같은 프롬프트로 5종을 쟀다.

| 사고 파라미터 | 소요 | 본문 | 종료 사유 |
|---|---:|---|---|
| `reasoning_effort: medium` | 69.4초 | **0자** | `length` (`reasoning_tokens: 8000`) |
| `reasoning_effort: low` | 73.8초 | **0자** | `length` (같음) |
| `reasoning_effort: minimal` | 63.0초 | **0자** | `length` (같음) |
| 파라미터 없음 | 68.0초 | **0자** | `length` (같음) |
| **`thinking: {type:"disabled"}`** | **2.9초** | 416토큰 · 완전 | `stop` |

장소 26곳을 5일에 배분하는 것은 제약 만족 문제라 **사고 연쇄가 발산한다** — 어느 effort
값을 줘도 `max_tokens` 전부를 추론에 쓰고 본문이 비어 나온다. 사고를 끄면 2.9초에 정확한
배분이 나온다(권역별 묶음·페르소나 반영 확인). 초안은 사람이 검토하는 산출물이므로(§7.5)
사고 연쇄가 필요하지 않다.

**타임아웃을 `AbortSignal`로 거는 근거 (같은 실측)** — 위 호출은 SDK 클라이언트에
`timeout: 25000`이 설정된 상태에서 **58.2초 만에 돌아왔다.** `maxDuration`이 60초이므로
그 상태로는 플랫폼이 먼저 응답을 끊어 409 대신 504가 나가고, §11.6 재시도 경로를
건너뛴다. 예비 경로(Gemini)는 처음부터 `abortSignal`을 썼고 주 경로만 SDK 옵션을
믿고 있었다.

| 호출 | effort | 소요 | 판정 |
|---|---|---:|---|
| 일차 분해 `DECOMPOSE_SCHEMA` | medium | 5.7 / 6.6 / 8.7초 | 통과 |
| 페이지 확장 `EXPAND_SCHEMA` (최장 출력) | medium | 14.7 / 14.7 / 18.8초 | 통과 3 / 빈 본문 1 |
| 사실 검증 `VALIDATION_SCHEMA` | low | 1.4 / 1.5 / 3.1초 | 통과 |

가장 무거운 페이지 확장이 최대 18.8초로 25초 예산 안에 들어온다. 다만 §5.5의 `processing_delayed` 임계값(20초)에 근접하므로, 이 단계에서 이상 플래그가 간헐적으로 뜨는 것은 설계상 정상이며 산출물 실패가 아니다.

**빈 본문** — `json_object` 모드에서 종료 사유가 `stop`인데 본문만 비는 응답이 **4회 중 1회** 관측됐다(2.6초). `schema_invalid`로 분류해 §11.6의 재시도 경로로 보내며, 실패 `detail`에 `finish_reason`·사고 토큰·사고 본문 길이를 남겨 우연한 1회와 반복 실패를 사후에 구분할 수 있게 한다.

| 항목 | 내용 |
|---|---|
| 컨텍스트 캐싱 | **자동으로 동작한다.** 시스템 프롬프트가 바이트 단위로 동일하면 캐시에 적중한다(실측: 913 입력 토큰 중 896 적중). 별도 설정·요청 필드가 없다 |
| 데이터 취급 | 종량제 API이나 데모 단계에서는 실제 고객 데이터를 넣지 않는다 |
| 가용성 | 과부하·잔액 소진으로 막힐 수 있다. `AI_PROVIDER=gemini` 한 줄로 예비 경로로 전환한다 |

**예비 경로 (Gemini)** — 주 경로가 막혔을 때만 쓴다.

| 항목 | 내용 |
|---|---|
| 전환 | `AI_PROVIDER=gemini`. 라우트 수정 없음 |
| 모델 | `gemini-3.5-flash` (2026-08-11 실측 9.8초 / 사고 1,425. `gemini-3.6-flash`는 20.3초로 §5.5 임계값을 상시 초과해 탈락) |
| 한도 | 무료 티어는 **모델당 하루 20회**(`GenerateRequestsPerDayPerProjectPerModel`)라 §20 대본(AI 6회) 3회분이다. 429의 `retryDelay`를 기다려도 일일 한도는 회복되지 않는다 — **상시 경로로 쓸 수 없어 예비로 둔다** |
| 무료 티어 제약 | flash 계열만 호출된다(pro는 유료·429). 컨텍스트 캐싱 불가(유료 전용). 입력이 모델 개선에 사용되므로 실제 고객 데이터를 넣지 않는다 |

#### 출력 형식 강제

**모든 AI 호출의 출력은 JSON 스키마로 강제한다.** 어긋난 구조가 파이프라인에 들어가는 경로는 없다. 강제를 **어디서** 하는지는 공급자에 따라 다르고, **결과는 같다.**

| 공급자 | 강제 지점 | 방식 |
|---|---|---|
| **DeepSeek (주)** | **서버 — `lib/ai/schema.ts`** | `response_format: json_object`로 JSON 문법을 보장받고, 받은 값을 스키마에 대조한다 |
| Gemini (예비) | 제공자 | `responseMimeType: 'application/json'` + `responseSchema` |

DeepSeek에는 `json_schema` strict 모드가 없다 — `json_object`(문법만 JSON 보장)까지만 지원하고, 그 모드는 프롬프트에 형식 명세를 요구한다. 따라서 주 경로에서는 **스키마를 시스템 프롬프트에 붙이고, 검사는 서버가 한다.** 「프롬프트로 JSON을 지시하지 않는다」는 제공자 강제가 가능할 때의 수단 규정이며, 규정의 목적인 **구조 보증은 서버 검증이 대신 진다.**

```ts
// 주 경로 (DeepSeek)
messages: [{ role: 'system', content: SYSTEM + JSON_SCHEMA }, { role: 'user', content: INPUT }],
max_tokens: 8000,
reasoning_effort: 'medium',
response_format: { type: 'json_object' },
// → 응답을 validateAgainstSchema로 대조. 어긋나면 schema_invalid
```

| 규칙 | 내용 |
|---|---|
| 스키마 출처 | 생성 호출은 §8.7·§9.3의 `data` 키 표, 검증 호출은 §11.3의 `items` 구조 |
| 스키마 제약 | 모든 객체에 `required`를 넣는다. 재귀 스키마·수치 제약은 지원이 불안정하므로 스키마에 쓰지 않고 서버가 별도 검사한다 |
| 파싱 | `JSON.parse` 후 스키마에 대조한다. 파싱 실패·구조 불일치 모두 `schema_invalid`로 분류해 생성 실패로 취급한다 |

#### 시스템 프롬프트 불변 규칙

주 공급자는 시스템 프롬프트가 바이트 단위로 동일할 때 캐시에 적중하므로, **시스템 프롬프트는 요청 간 바이트 단위로 동일하게 유지한다.**

| 규칙 | 내용 |
|---|---|
| 금지 | 시스템 프롬프트에 **날짜·`product_id`·`execution_id`·타임스탬프를 넣지 않는다** |
| 가변 데이터 위치 | `confirmed_data`·`form_input` 등은 사용자 파트에 넣는다 |

#### 실패 처리

| 상황 | `error_type` | 판정 | 응답 |
|---|---|---|---|
| 25초 타임아웃 | `timeout` | 생성 실패 | **409 `{reason: "retry", retry_from: N}`** + 해당 카운터 +1 |
| 429 한도 초과 | `rate_limited` | 생성 실패 | 동일 |
| 5xx·네트워크·인증 | `api_error` | 생성 실패 | 동일 |
| 종료 사유 `length`(주) / `MAX_TOKENS`(예비) | `max_tokens` | 생성 실패 (출력 절단) | 동일 |
| 종료 사유 `content_filter`(주) / `{SAFETY, RECITATION, BLOCKLIST, PROHIBITED_CONTENT, SPII}`·`promptFeedback.blockReason`(예비) | `refusal` | 생성 실패 | 동일 |
| 스키마 검증·파싱 실패, **빈 본문** | `schema_invalid` | 생성 실패 | 동일 |
| 카운터 소진 | — | — | §11.6의 축별 소진 처리 |

**종료 사유를 먼저 확인한 뒤 본문을 읽는다.** 거부·절단 시 본문이 비어 있어 무조건 읽으면 예외가 발생한다.

모든 실패는 `execution_logs`에 해당 단계의 실패 행으로 기록하며, `output`에 `{error_type, detail, finish_reason, usage, elapsed_ms, model}`을 담는다(§5.4).

---

## 5. 데이터 모델

### 5.1 `products`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid PK | 상품 식별자 |
| `execution_id` | text UNIQUE | 실행 ID(로그 추적 단위). 재시도·재제출에도 동일 값을 유지한다 |
| `attempt_no` | int | 생성 시도 회차. 최초 1. **[다시 생성]·재제출 시 +1** |
| `slug` | text UNIQUE NULL | 공개 URL 경로. 페이지 생성 시 발급되며 그 전에는 NULL. 게시 후 불변 |
| `status` | text | `generating` / `input_error` / `brochure_ready` / `draft` / `reviewing` / `published` / `unpublished` |
| `current_step` | text | 마지막으로 완료한 파이프라인 단계. 클라이언트가 다음 호출 대상을 판단한다 |
| `form_input` | jsonb | 사용자 원본 폼 입력(구조는 §7.4). **같은 `attempt_no` 안에서 불변** — 교체 경로는 §14.4 #17뿐이다. **모든 검증의 기준값**(§11.1) |
| `confirmed_data` | jsonb | 확정 데이터표(§6). 같은 `attempt_no` 안에서 불변 |
| `brochure_content` | jsonb | 소개서(간단버전) 콘텐츠 모델(§8) |
| `page_content` | jsonb | 상품 페이지 콘텐츠 모델(§9). **`theme`을 이 안에만 보관한다** |
| `validation_snapshot` | jsonb | 검증 결과(§11.3). **편집으로 변경되지 않으며, `attempt_no`가 올라갈 때만 갱신** |
| `retry_counts` | jsonb | `{normalization, brochure, page, consistency}` 단계별 재시도 횟수. **4종**(§11.6) |
| `human_edited` | boolean | 편집기에서 1회 이상 저장되었는지 |
| `publish_override_at` | timestamptz | 검증 실패 상태에서 기획자가 책임 확인 후 게시한 시각(§11.5) |
| `failure_reason` | text | `input_error` 상태의 사유 |
| `published_at` | timestamptz | 최초 게시 시각 |
| `created_at` / `updated_at` | timestamptz | 모든 쓰기 작업에서 `updated_at`을 갱신한다 |

> `theme` 컬럼을 두지 않는 이유: `page_content.theme` 단일 출처로만 보관해 이중 저장·동기화 문제를 없앤다.

### 5.2 `product_images`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid PK | |
| `product_id` | uuid FK | |
| `slot` | text | `hero` / `accommodation` / `itinerary_day_{n}` / `shop` (§7.3의 4종) |
| `storage_path` | text | Supabase Storage 경로. `{product_id}/{uuid}.{ext}` |
| `alt` | text | 대체 텍스트 |
| `sort_order` | int | 같은 슬롯 내 순서 |
| `width` / `height` / `bytes` | int | 업로드 시 추출 |

### 5.3 `applications`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid PK | |
| `product_id` | uuid FK | |
| `name` / `email` / `phone` | text | 신청자 정보 |
| `headcount` | int | 인원수 (1~20) |
| `consent_at` | timestamptz NOT NULL | 개인정보 수집·이용 동의 시각 |
| `product_snapshot` | jsonb | **신청 시점** 상품 정보 스냅샷 |
| `email_status` | text | `pending` / `sent` / `failed` |
| `email_error` | text | 발송 실패 사유 |
| `created_at` | timestamptz | |

### 5.4 `execution_logs`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | bigserial PK | |
| `execution_id` | text | |
| `product_id` | uuid FK nullable | |
| `category` | text | **`pipeline`** / `lifecycle` / `application` — 로그 화면 탭 기준(§14.3) |
| `step` | text | 아래 표 참조 |
| `attempt_no` | int | 기록 시점 시도 회차 |
| `retry_index` | int | 해당 시도 안에서의 재시도 회차. 0 = 최초 |
| `verdict` | text | `pass` / `fail` / `-` — **저장값은 영어다.** 화면에는 `통과`/`반려`/`-`로 표시한다(§14.3) |
| `status` | text | 기록 시점 상품 상태 |
| `input` / `output` | jsonb | 가공·요약 없는 원본 |
| `created_at` | timestamptz | UTC |

| `category` | `step` 값 | 기록 시점 |
|---|---|---|
| `pipeline` | `pipeline_started` | 폼 제출 + 서버 validation 통과(§8.1) |
| | `itinerary_decomposed` | 일차 분해 완료(§8.2) |
| | `normalization_validated` | 0차 검증 완료(§8.2) |
| | `brochure_generated` | 소개서 생성 완료(§8.3) |
| | `validation_1_completed` | 1차 검증 완료(§8.4) |
| | `page_generated` | `page_content` 생성 완료(§9.5) |
| | `validation_2_completed` | 2차 검증 완료(§9.5) |
| | `validation_3_completed` | 3차 검증 완료(§9.5) |
| | `draft_registered` | `status = draft` 전이 직후(§9.5) |
| | `regenerate_requested` | [다시 생성] 조작 시(§15.3) |
| | `form_input_resubmitted` | [입력 수정 후 재제출](§14.4 #17) |
| `lifecycle` | `content_edited` · `slug_changed` · `published` · `unpublished` · `publish_override` · `product_deleted` | 각 조작 직후 |
| `application` | `application_received` · `email_sent` · `email_resent` · `application_deleted` | §13.2 · §12.4 |

각 단계는 **성공·실패 모두** 1행을 기록한다. 재시도는 `retry_index`를 올려 새 행으로 누적하고 기존 행을 덮어쓰지 않는다.

**개인정보 예외**: `category = application` 행의 `input`·`output`은 **신청자의 이름·이메일·연락처를 저장 시점에 마스킹**해 기록한다(`홍*동` · `ho***@example.com` · `010-****-1234`). 원본은 `applications` 표에만 보관한다.

> 이 예외를 둔 이유: 「가공 없는 원본 기록」 규정이 개인정보에도 적용되면, 신청 내역 화면에서 연락처를 마스킹해도(§13.1) **로그 화면에서 원본이 그대로 노출**된다. 마스킹이 한쪽에만 있으면 다른 쪽으로 새어 나간다. 파이프라인·상태변경 로그에는 개인정보가 없으므로 원본 그대로 기록한다.

> `category`를 둔 이유: 신청이 100건 누적되면 `application` 로그가 실행 로그 화면을 덮어 "단일 화면에서 파이프라인 이력 확인"이 무의미해진다.

### 5.5 `abnormality_flags`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | bigserial PK | |
| `execution_id` | text | |
| `product_id` | uuid FK nullable | 상품 단위 조회용. Step 01 실패 시 null |
| `attempt_no` | int | **감지 시점 시도 회차.** 중복 기록 판정과 `validation_repeated_failure` 집계의 기준 |
| `type` | text | 아래 표 |
| `step` | text | 감지 단계 |
| `detail` | text | 감지 내용 |
| `detected_at` | timestamptz | |

**중복 기록 범위**: `(execution_id, attempt_no, step, type)` 조합당 1행이다. `attempt_no`가 올라가면 같은 단계·같은 유형도 **새로 기록한다.** 이전 시도의 행은 삭제하지 않는다.

**감지 조건**

| `type` | 감지 조건 |
|---|---|
| `retry_accumulated` | 한 단계의 `retry_counts` 값이 **2에 도달**한 시점(= 마지막 재시도 진입) |
| `pipeline_aborted` | 한 단계의 재시도가 **소진**되어 `input_error` 또는 `verdict = fail`로 확정된 시점 |
| `validation_repeated_failure` | **같은 검증 항목**이 같은 `attempt_no` 안에서 2회 이상 실패 |
| `processing_delayed` | 한 요청의 소요 시간이 **AI 호출 타임아웃(25초)의 80%를 초과**(= 20초 이상) |
| `itinerary_partial` | 일정 원문의 일차 수가 여행기간보다 적어 `추후 추가 예정`으로 채운 경우(§6.1) |

감지된 경우에만 기록한다. "이상 없음" 류 항목은 남기지 않는다.

### 5.6 `edit_history`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | bigserial PK | |
| `product_id` | uuid FK | |
| `action` | text | `update` / `delete` / `insert` / `reorder` |
| `section_id` | text | 대상 섹션 |
| `before` / `after` | jsonb | 변경 전·후 값 |
| `edited_at` | timestamptz | |

---

## 6. 확정 데이터표 (`confirmed_data`)

### 6.0 용어 구분 — "폼 그룹"과 "데이터 키"

"6개"가 두 의미로 쓰여 혼동을 일으키기 쉽다. 아래와 같이 용어를 분리한다.

| 용어 | 개수 | 구성 |
|---|---:|---|
| **필수 폼 그룹** (입력 화면 단위, §7.1) | 6 | 행사정보 · **일정** · 숙박 · 상점 · 가격 · 식사 |
| **데이터 키** (확정 데이터표 최상위, §6) | 6 | 행사정보 · 숙박 · 상점 · 가격 · 식사 · **항공편** (이 중 필수 5, 항공편은 선택) |

`일정`은 **데이터 키가 아니라 `행사정보`의 하위 필드**다. 폼에서는 별도 그룹으로 보이지만 저장 구조상 `행사정보.일정원문`·`행사정보.일정`에 들어간다.

### 6.1 구조

데이터 키는 6개다. 1~5는 필수, 6은 선택이다. **모든 하위 값은 문자열이며, 숫자·불리언 타입을 사용하지 않는다.** 값이 여럿인 키는 **객체 배열**을 쓰고, 그 원소의 하위 값도 전부 문자열이다. 객체 배열은 `행사정보.일정` · `숙박` · `상점` **3개뿐이다.**

| 순번 | 데이터 키 | 필수 | 형태 | 하위 필드 |
|---:|---|---|---|---|
| 1 | 행사정보 | 필수 | 객체 | `행사명`, `여행지`, `행사기간`*(행사 자체 날짜 — 여행기간과 별개), `여행기간`, `일정원문`, `일정`(객체 배열), `여행스타일`*, `타겟층`*, `여행주제`*, `기획메모`*† |
| 2 | 숙박 | 필수 | **객체 배열 · 1건 이상** | 행마다 `숙소명`, `위치`, `객실타입`*, `숙박일정`* |
| 3 | 상점 | 필수 | **객체 배열 · 1건 이상** | 행마다 `상점명`, `구분`, `위치`*, `상점정보`* |
| 4 | 가격 | 필수 | 객체 | `성인`, `아동`, `기타` |
| 5 | 식사 | 필수 | 객체 | `식사정보` |
| 6 | 항공편 | 선택 | 객체 | `공항`, `항공사`, `편명`, `출발시간`, `도착시간` |

\* 선택 필드. 데이터 키는 필수이지만 표시된 하위 필드는 미입력을 허용한다.
† `기획메모`는 **고객에게 표시되지 않는 참고 필드**이므로 미입력이어도 채움 대상이 아니다. 빈 문자열로 남고, 그때는 AI 프롬프트에도 싣지 않는다(§8.3·§9.5).

**숙박·상점을 배열로 둔 근거**: 한 상품이 숙소를 옮겨 다니거나 카페·음식점 여러 곳을 안내하는 것은 예외가 아니라 일반이다. 단일 객체로 두면 그 목록이 `상점정보` 한 칸(500자)에 문장으로 밀려 들어가 구조가 사라지고, 상한에 걸리는 순간 값을 잘라내야 한다 — §16.1이 금지하는 일이다. 배열은 원소 수가 늘어도 계약이 바뀌지 않는다.

**`상점.구분`**: `제휴` 또는 `추천` 2종. 폼 select의 기본값이 `추천`이라 미입력 상태가 존재하지 않으므로 채움 대상이 아니다. 제휴 관계가 실제로 있는 상점만 기획자가 `제휴`로 바꾼다. **AI는 이 값을 `제휴`로 올리지 않는다** — 없는 제휴 관계를 만드는 일이기 때문이다.

**배열 원소의 `source` 경로**: 인덱스를 붙여 `숙박[0].숙소명` · `상점[3].상점명`으로 적는다. 인덱스는 0부터이며 **입력 순서를 보존한다.** 검증은 이 경로를 조인 키로 원소 단위로 대조한다(§11.1).

**선택 항목 미입력 처리**: 웹 폼에서 선택 항목이 비어 있으면 문자열 `추후 추가 예정`으로 채운다. 소개서·상품 페이지 양쪽에 동일하게 반영되고, 해당 섹션은 삭제되지 않는다.

**`가격.아동` 미운영 처리**: 아동 요금을 운영하지 않는 상품은 폼의 "아동 요금 미운영" 체크로 문자열 `해당 없음`을 저장한다. `0`을 넣어 "0원"으로 표시되는 것을 방지한다.

### 6.2 정규화 규칙 (허용 3종)

| 대상 | 규칙 |
|---|---|
| 날짜 | `YYYY-MM-DD` 형식으로 통일 |
| 금액 | **천 단위 콤마를 제거**하고 숫자와 단위만 남긴다. `120,000원` → `120000원` |
| 공백 | 앞뒤 공백 제거. 문자열 내부 연속 공백은 1칸으로 축약 |

이 3종 외의 값 변형은 금지한다. **이 3종에 의한 표기 차이는 §11.2의 허용 차이에 포함된다** — `form_input`을 기준으로 검증하므로 명시가 필요하다.

**적용 대상 한정** — 규칙마다 대상 필드가 다르다. 이 구분이 없으면 자유 서술 필드의 문장이 정규화 대상이 되어 §16.1을 위반한다.

| 규칙 | 적용 대상 |
|---|---|
| 날짜 | `행사정보.여행기간_시작` · `행사정보.여행기간_종료` **2개 필드만** |
| 금액 | `가격.성인` · `가격.아동` **2개 필드만** |
| 공백 | **모든 문자열 필드** (자유 서술 필드 포함 · **배열 원소의 문자열 필드까지**) |

`숙박[i].*` · `상점[i].*`도 공백 규칙의 대상이다. 배열이라고 건너뛰면 원소 안의 표기만 정규화되지 않아 0차가 「정규화 누락」을 잡는다.

`일정원문` · `상점정보` · `식사정보` · `가격.기타`는 **공백 규칙만** 적용한다. 이 필드들 안에 날짜나 금액처럼 보이는 문자열이 있어도 손대지 않는다.

**금액 단위** — 단위는 `원` 하나로 고정한다.

| 항목 | 규정 |
|---|---|
| 허용 단위 | **`원`만.** `USD` · `달러` · `만원` · `₩`는 허용하지 않는다 |
| 폼 입력 형태 | 숫자 입력 필드 + 고정 접미사 `원` 표시. 사용자가 단위를 타이핑하지 않는다 |
| 저장 형태 | `{숫자}원` 문자열. 예: `120000원` |
| `가격.아동` 예외 | `해당 없음`은 단위 규칙의 대상이 아니다(§6.1) |
| `가격.기타` | 자유 서술 필드다. 금액 규칙을 적용하지 않으며 안에 어떤 표기가 있어도 그대로 둔다 |

> 단위를 하나로 고정하는 이유: 다중 통화를 허용하면 환산 금지 규칙(§16.1)과 총액 계산 금지 규칙(§13.3)이 통화별로 갈리고, 검증에서 `120000원`과 `120000 KRW`를 같은 값으로 볼지 판정할 근거가 필요해진다. 다중 통화는 범위에서 제외한다(§18.2).

### 6.2.1 여행기간 — 결합과 일수

§6.2의 3종은 **값의 표기**를 바꾸고, 아래 결합은 **필드의 개수**를 바꾸는 유일한 변환이다. 일수는 그 두 필드에서 파생되므로 함께 규정한다.

| 항목 | 규칙 |
|---|---|
| **결합** (허용 1종) | `form_input`의 `행사정보.여행기간_시작`·`여행기간_종료`를 **`{시작일} ~ {종료일}`** 한 문자열로 합쳐 `confirmed_data.행사정보.여행기간`에 저장한다(물결표 앞뒤 공백 1칸). 예: `2026-03-14 ~ 2026-03-17` |
| 행사기간(선택) | `행사기간_시작`·`행사기간_종료`도 같은 규칙으로 `confirmed_data.행사정보.행사기간`에 결합한다. **선택이므로 둘 다 비면 빈 문자열**이고 그때는 페이지에 나오지 않는다. 여행 일수·일정을 정하지 않는다(그건 여행기간). 한쪽만 채우면 관문 400 |
| 결합의 검증 취급 | **`form_input`과 `confirmed_data`의 경로가 1:1로 대응하지 않는 유일한 지점이다.** 0차는 위반으로 판정하지 않고, 1·2차는 `source`가 `행사정보.여행기간`을 가리킬 때 원본 두 필드를 합친 값과 대조한다 |
| 그 외 구조 변환 | 필드 분할·병합·이동·삭제는 **금지**. 새 결합은 이 표에 추가한 뒤에만 허용된다 |
| **일수 계산식** | `일수 = 종료일 − 시작일 + 1` (**양끝 포함**). `2026-03-14 ~ 03-14`은 1일, `~ 03-17`은 4일 |
| 일수 범위 | **`1 ≤ 일수 ≤ 15`.** 양끝을 포함하므로 하루 여행도 15일 상한도 포함이다(§7.1) |
| 일수의 저장 | **저장하지 않고 필요할 때마다 계산한다.** 이 값이 `일정` 배열 길이(§6.3)·`itinerary_day_{n}` 슬롯 수(§7.3)·이미지 절대 상한을 결정한다 |
| 계산 기준 | 달력 날짜 차이만 쓴다. 시각·시간대를 개입시키지 않으므로 서머타임·윤년에 영향받지 않는다 |
| 계산 주체 | 서버. 프론트엔드도 같은 식으로 슬롯 수를 표시하되 판정은 서버 값을 쓴다 |

### 6.3 일정 원문의 일차 분해

폼의 일정 필드는 자유 텍스트 1개다(§7.1). AI가 이를 일차 단위로 분해하되, **분해 결과가 원문에 근거함을 검증할 수 있어야 한다.**

```json
"행사정보": {
  "일정원문": "1일은 김해공항에서 출발해 올레 7코스 걷기, 중식·석식 제공. 2일은 성산일출봉과 해녀박물관 관람...",
  "일정": [
    { "day": "1", "원문근거": "1일은 김해공항에서 출발해 올레 7코스 걷기, 중식·석식 제공", "내용": "..." },
    { "day": "2", "원문근거": "2일은 성산일출봉과 해녀박물관 관람", "내용": "..." }
  ]
}
```

| 규칙 | 내용 |
|---|---|
| `일정원문` | 사용자 입력 원본. **불변** |
| `원문근거` | `일정원문`에서 **발췌한 부분 문자열**이어야 한다(공백 정규화 후 부분 문자열 일치). 요약·재작성·의역 금지 |
| `내용` | `원문근거`에 등장하는 요소만 사용해 작성한 서술 문장. 새 장소·활동·이동을 추가할 수 없다 |
| 일차 수 | `일정` 배열 길이는 여행기간 일수와 **정확히 일치**한다 |
| 원문 일차 < 여행기간 | 부족한 일차는 `{"day": "n", "원문근거": "", "내용": "추후 추가 예정"}`으로 채우고 이상 플래그 `itinerary_partial`을 남긴다 |
| 원문 일차 > 여행기간 | **실패**. `status = input_error`, 사유를 폼에 표시하고 재입력을 요청한다 |
| 일차 구분 불가 | 원문에서 **아래 6종 표기 중 하나도** 식별할 수 없으면 **실패**. "일차 구분을 포함해 입력해 주세요"를 표시한다. 임의 배분하지 않는다 |

**일차 구분 인식 범위 (확정 · 6종)**

```text
n일      n일차      n일 차      첫째 날 / 둘째 날 …      Day n      DAY n
```

이 중 하나라도 식별되면 분해를 시도한다. 하나도 없으면 `구분불가`로 실패한다. **이 목록이 실패 판정의 기준이므로 구현에서 임의로 넓히거나 좁히지 않는다.**

**`원문근거`·`내용`의 판정 방법** — 각 단계의 주체를 명시한다:

| 순서 | 검사 | 주체 |
|---:|---|---|
| 1 | `원문근거`가 `일정원문`의 부분 문자열인가 | **기계**(문자열 포함 검사) |
| 2 | `내용`에 등장하는 고유명사·명사구가 `원문근거` 또는 `confirmed_data`의 다른 값 안에 부분 문자열로 존재하는가 | **기계**(후보 추출 + 포함 검사). 미존재 후보는 위반 후보로 표시 |
| 3 | 2에서 표시된 후보가 실제 위반인지(조사·어미 변화 등 정상 변형 제외) | AI(`itinerary-decomposition`의 호출 1회 안에서) |

기계 검사가 1차 필터이므로, AI가 자기 생성물을 자기가 검사하는 구조에만 의존하지 않는다.

### 6.3.1 출처 없는 숫자 판정 (알고리즘)

§11.2의 「출처 없는 아라비아 숫자 0건」은 판정 방법이 없으면 실행 불가능한 규정이다. 일차 번호·가격·날짜가 모두 숫자이므로 무엇이 「출처 있음」인지 정의한다.

**판정 대상**: 생성물의 서술 필드 — `overview.핵심일정` · `itinerary.days[].text` · `apply.안내문구` · 삽입 블록의 `본문`. 값 필드는 §11.2의 「값 일치」가 이미 대조하므로 이 검사의 대상이 아니다.

| 순서 | 검사 | 주체 |
|---:|---|---|
| 1 | 서술 필드에서 **숫자 토큰**을 추출한다 — 정규식 `[0-9]+` 로 잡히는 연속 숫자와 그 뒤에 붙은 단위 문자(`일`·`박`·`회`·`원`·`명`·`시`·`분`·`코스` 등)까지 한 토큰으로 본다 | 기계 |
| 2 | 각 토큰이 **허용 출처 4종** 중 하나에 있는지 부분 문자열로 검사한다 | 기계 |
| 3 | 어디에도 없는 토큰을 **위반 후보**로 표시한다 | 기계 |
| 4 | 표시된 후보가 실제 위반인지 판정한다 — 허용 출처의 값에서 정상 파생된 경우는 제외 | AI (같은 호출 안에서) |

**허용 출처 4종** — 토큰이 이 중 하나에 있으면 출처가 있다.

| # | 출처 |
|---:|---|
| 1 | `form_input`의 모든 값 (문자열 연결한 전체) |
| 2 | 같은 일차의 `원문근거` (일정 서술인 경우) |
| 3 | **일차 번호** — `일정[n].day` 값. `1일차`·`2일`처럼 쓰는 것은 정상이다 |
| 4 | **여행기간 일수**(§6.2.1)에서 파생된 수 — `4일`·`3박`. `일수`와 `일수 - 1`만 허용한다 |

**4번의 근거**: `3박4일` 같은 표현은 입력에 없어도 여행기간에서 결정적으로 파생되므로 창작이 아니다. 다만 `일수`와 `일수 - 1` 두 값만 허용하며, 그 외의 산술(인원·요금·거리)은 위반이다.

**대표 위반 예**: `약 2시간 소요` · `총 5개 코스` · `왕복 40분` · `4인 기준` — 어느 출처에도 없는 숫자다.

### 6.4 해소한 1.0 모순

1.0에서 `숙박일정`이 숙박의 필수 필드이면서 동시에 "선택 4개 항목"에 포함되어 있었다. **선택**으로 확정한다. 미입력 시 여행기간에서 파생 계산하지 않고 `추후 추가 예정`으로 둔다.

---

## 7. Step 1 — 웹 폼 입력 및 이미지 업로드

**경로**: `/new` (인증 필요)

### 7.1 필수 입력 (필수 폼 그룹 6개)

| 폼 그룹 | 필드 | 입력 형태 | validation | 저장 경로 |
|---|---|---|---|---|
| 행사정보 | 행사명 | 텍스트 | 2~40자 | `행사정보.행사명` |
| | 여행지 | 텍스트 | 2~60자 | `행사정보.여행지` |
| | 행사기간(선택) | 시작일 + 종료일 | 둘 다 비거나 둘 다 채움. 채우면 시작일 ≤ 종료일 | `행사정보.행사기간_시작` · `행사정보.행사기간_종료` |
| | 여행기간 | 시작일 + 종료일 | 시작일 ≤ 종료일, 최대 15일 | `행사정보.여행기간_시작` · `행사정보.여행기간_종료` |
| **일정** | 일정 원문 | 여러 줄 텍스트 | 20~2000자. 일차 구분 표기 필수 | `행사정보.일정원문` |
| 숙박 | 숙소 행 | **행 추가·삭제** | **1행 이상** | `숙박[]` |
| | 숙소명 | 텍스트 (행마다) | 2~60자 | `숙박[i].숙소명` |
| | 위치 | 텍스트 (행마다) | 1~60자 | `숙박[i].위치` |
| 상점 | 상점 행 | **행 추가·삭제** | **1행 이상** | `상점[]` |
| | 상점명 | 텍스트 (행마다) | 1~80자 | `상점[i].상점명` |
| | 구분 | select `제휴`/`추천` (행마다) | 2종 중 하나. 기본값 `추천` | `상점[i].구분` |
| 가격 | 성인 | 숫자 + 단위 | 0 이상 정수 | `가격.성인` |
| | 아동 | 숫자 + 단위 또는 "미운영" 체크 | 0 이상 정수 또는 `해당 없음` | `가격.아동` |
| | 기타 | 여러 줄 텍스트 | 0~300자 (포함/불포함 사항) | `가격.기타` |
| 식사 | 식사정보 | 여러 줄 텍스트 | 1~500자 | `식사.식사정보` |

**필수 필드 중 어느 하나라도 비어 있으면** 프론트엔드에서 제출을 차단하고 해당 필드에 오류를 표시한다. 서버는 동일 규칙으로 재검증하며, 위반 시 400을 반환하고 파이프라인을 시작하지 않는다.

일정 필드에는 아래 형식의 placeholder를 표시한다.

```text
1일: 김해공항 출발, 올레 7코스 걷기, 중식·석식 제공
2일: 성산일출봉, 해녀박물관 관람, 조식·중식 제공
...
```

**행사명 상한을 40자로 정한 근거**: `hero.headline`의 콘텐츠 길이 계약이 40자이고(§17.1), 값을 잘라내는 것은 §16.1 위반이므로 입력 단계에서 상한을 맞춘다.

**여행기간 상한을 15일로 정한 근거**: 일차별 이미지 슬롯이 총 이미지 상한과 충돌하지 않게 하기 위함이다(§7.3).

### 7.2 선택 입력 (9종)

| 필드 | 입력 형태 | 저장 경로 |
|---|---|---|
| 타겟층 | 텍스트 (최대 100자) | `행사정보.타겟층` |
| 여행스타일 | select (테마 키와 1:1 대응. 선택한 옵션의 표시 문자열이 그대로 들어간다) | `행사정보.여행스타일` |
| 여행주제 | 텍스트 (최대 200자) | `행사정보.여행주제` |
| 기획메모 | 여러 줄 텍스트 (최대 1000자) · **고객 미노출 · 채움 대상 아님**(§6.1) | `행사정보.기획메모` |
| 객실타입 | 텍스트 (최대 40자) · 숙소 행마다 | `숙박[i].객실타입` |
| 숙박일정 | 텍스트 (예: `1~2박`) · 숙소 행마다 | `숙박[i].숙박일정` |
| 상점 위치 | 텍스트 (최대 60자) · 상점 행마다 | `상점[i].위치` |
| 상점정보 | 여러 줄 텍스트 (최대 500자) · 상점 행마다 | `상점[i].상점정보` |
| 항공편 | 공항 / 항공사 / 편명 / 출발시간 / 도착시간 | `항공편.*` |

**`객실타입`·`상점정보`를 선택으로 내린 근거**: 숙박·상점이 배열이 되면서 행마다 이 값을 요구하면 **객실이 아직 확정되지 않은 숙소나 설명이 없는 가게를 목록에 넣을 수 없다.** 목록에서 빠지는 것이 빈칸으로 남는 것보다 나쁘다 — 미입력 표기 규정(§6.1)이 이미 그 빈칸을 `추후 추가 예정`으로 처리하며, 섹션도 삭제되지 않는다. 행의 **식별에 필요한 값**(숙소명·위치, 상점명·구분)만 필수로 남긴다.

### 7.3 이미지 업로드

| 항목 | 규정 |
|---|---|
| 슬롯 | **4종.** `hero`(0~1) / `accommodation`(0~3) / `itinerary_day_{n}`(일차별 0~1, 일차 수만큼) / `shop`(0~2) |
| 슬롯 지정 주체 | **사용자**. 업로드 시 슬롯을 반드시 선택한다. AI는 슬롯을 재배치하지 않는다 |
| 포맷 | JPG / PNG / WebP |
| 용량 | 장당 5MB 이하 |
| 총 장수 | **슬롯별 상한을 모두 준수**하며, 절대 상한은 21장(15일 여행에서 전 슬롯 만재 시 `1+3+15+2`). 실제 상한은 여행 일수에 따라 줄어든다 |
| 대체 텍스트 | 사용자 입력. 미입력 시 `{행사명} {슬롯 한글명}`으로 자동 채움 |
| 이미지 없을 때 | `hero` 미업로드 시 테마 컬러 그라디언트 배경으로 폴백. 그 외 슬롯은 이미지 영역을 렌더링하지 않으며, 섹션 자체는 유지한다 |
| 렌더링 계약 | 고정 종횡비 컨테이너 + `object-fit:cover` + `max-width:100%`. `hero`를 제외한 전 이미지는 지연 로딩(`loading="lazy"`), `hero`는 우선 로딩(§17.2) |
| 검증 범위 | 이미지-텍스트 내용 정합성(사진 속 장소가 여행지와 같은지)은 **검증 대상이 아니며 기획자 책임**이다. 검증은 슬롯 배치·개수·용량·대체 텍스트 존재 여부만 판정한다 |
| 권리 | 업로드 이미지의 저작권·초상권 확보는 기획자 책임이다. 폼에 고지 문구를 표시한다 |

`itinerary_day_{n}` 슬롯은 여행기간 입력 후 일차 수만큼 자동 생성된다. 여행기간을 줄여 일차가 사라지면 **해당 슬롯의 이미지를 삭제할지 사용자에게 확인**하고, 확인 없이 삭제하지 않는다.

슬롯 4종 밖의 사진을 더 넣으려면 편집기의 `image` 블록을 쓴다(§10.2·§18.2).

**이미지 업로드는 이 폼에서만 가능하다.** 편집기의 `image` 블록은 이미 업로드된 이미지 중에서만 선택한다(§10.2). 편집기에서 새 업로드를 허용하면 슬롯 체계와 총량 제한 밖으로 새어 나간다.

**Storage 접근에 대한 명시**: 버킷은 읽기 공개이고 경로에 UUID를 사용한다(§5.2). 따라서 **임시저장 상품의 이미지 파일은 정확한 URL을 아는 경우 접근이 가능하다.** 페이지 콘텐츠·가격·일정은 DB에 있어 노출되지 않으므로 "임시저장 페이지는 기획자만 확인" 요건은 유지되지만, 이미지 파일 자체는 완전 비공개가 아니다. 완전 비공개가 필요하면 signed URL 프록시로 전환해야 하며 이번 범위에서는 채택하지 않는다.

### 7.4 `form_input` 구조 (확정)

`form_input`은 **모든 검증의 기준값**(§11.1)이므로 구조가 확정되어야 한다. 이 구조가 흔들리면 `source` 맵의 경로로 원본을 조회할 수 없어 1·2차 검증이 성립하지 않는다.

**§7.1·§7.2의 「저장 경로」 열이 `form_input`의 실제 구조다.** 폼 화면의 그룹 이름이 아니라 이 경로를 그대로 쓴다.

| 규칙 | 내용 |
|---|---|
| 최상위 키 | `행사정보` · `숙박` · `상점` · `가격` · `식사` · `항공편` — **`confirmed_data`와 같은 6개**(§6.1) |
| 경로 표기 | 점(`.`)으로 잇는다. 밑줄 표기(`가격_성인`)나 평면 키(`행사명`)를 쓰지 않는다 |
| **배열 키** | `숙박` · `상점`은 **객체 배열**이다(§6.1). 원소 경로는 `숙박[0].숙소명`처럼 인덱스를 붙인다. 인덱스는 0부터이며 **입력 순서를 보존한다** — 순서가 흔들리면 `source` 경로가 다른 원소를 가리켜 1·2·3차가 전부 어긋난다 |
| **배열 최소 길이** | `숙박` · `상점` 모두 **1건 이상.** 0건은 필수 폼 그룹 미충족이므로 400이며 파이프라인을 시작하지 않는다 |
| 여행기간 | `행사정보.여행기간_시작` · `행사정보.여행기간_종료` **두 필드로 저장한다.** 결합은 `confirmed_data`에서만 일어난다(§6.2.1) |
| 값 타입 | 전부 문자열. 숫자·불리언·`null`을 쓰지 않는다. **배열 원소의 하위 값도 전부 문자열이다** |
| 미입력 선택 항목 | 빈 문자열 `""`. **`추후 추가 예정` 채움은 `confirmed_data`에서만 일어난다**(§6.1) |
| 불변 | 같은 `attempt_no` 안에서 수정하지 않는다. 교체 경로는 §14.4 #17뿐이다 |

**두 데이터가 같은 경로 체계를 쓰므로 `source` 맵의 경로를 양쪽에 그대로 적용할 수 있다.** 예외는 `행사정보.여행기간` 하나이며 §6.2.1이 대응 관계를 정의한다. 이 구조를 지키지 않으면 1·2차 검증이 원본을 조회할 수 없어 성립하지 않는다.

### 7.5 자연어 초안 (`/api/plan-draft`)

기획자는 폼을 채우기 전에 메모 형태로 생각을 적는다 — 축제 일정, 가고 싶은 카페 목록, 숙소 두 곳, 페르소나. **그 텍스트를 폼 초안으로 바꾸는 것이 이 라우트다.**

| 항목 | 규정 |
|---|---|
| 경로 | `POST /api/plan-draft` (§14.4 #20). 인증 필요 |
| AI | **1회**(§4.2). 담당은 `planner-agent`이며 체인은 `manifest.json`이 정한다 |
| 입력 | 자유 텍스트 1개 (20~4000자) + 여행기간 2필드(선택 — 텍스트에서 못 읽으면 사람이 지정) |
| 출력 | `{draft, origin, notes}`. **`form_input`이 아니다** |
| 부수 효과 | **없다.** `products` 행을 만들지 않고 DB에 쓰지 않으며 `execution_logs`에도 남지 않는다 |
| 실패 | AI 실패는 409. 재시도는 클라이언트가 같은 요청을 다시 보낸다. 카운터·상태 전이가 없다 |

#### 초안은 `form_input`이 아니다 — 확정은 사람이 한다

```text
자연어 텍스트  →  [#20 초안]  →  폼에 채워진 초안  →  사람이 검토·수정  →  [#1 등록]  →  form_input 확정
                    AI 1회         (DB에 없음)          ← 여기가 관문        §7.1 검증
```

**§16.1의 「입력에 없는 값을 만들지 않는다」는 `form_input`이 확정된 뒤의 구간(§8·§9)에 걸린다.** 초안 단계는 그 구간보다 앞에 있고 사이에 사람의 검토가 있으므로, AI가 일정을 배분하고 문장을 쓰는 것은 그 규칙과 충돌하지 않는다. 확정된 `form_input`은 여전히 **사람이 승인한 값**이고, 1·2차 검증의 기준값 자격도 그대로다(§11.1).

대신 초안 단계에는 아래 세 규정이 걸린다. 이것이 「사람이 검토한다」를 실효 있게 만드는 장치다.

#### ① 하는 일 / 하지 않는 일

| 한다 | 하지 않는다 |
|---|---|
| 텍스트에 적힌 장소·상점·숙소를 **일차별로 배분**한다 | 텍스트에 없는 상점·숙소·관광지를 **새로 만든다** |
| 행사 기간(예: 축제 11.05~11.07)을 축으로 동선을 짠다 | 영업시간·소요시간·거리·평점을 만든다 |
| 권역(예: 구좌읍/조천읍)이 같은 장소를 같은 날에 묶는다 | 주소를 고치거나 보완한다 — 적힌 그대로 옮긴다 |
| `일정원문`을 §6.3의 일차 표기(`1일:`)로 정리한다 | **`가격.성인`을 채운다** (아래 ③) |
| 페르소나 문장을 `기획메모`로 옮긴다 | `상점[i].구분`을 `제휴`로 올린다(§6.1) |

**「실제로 존재하는 장소만 쓴다」의 실행 방법**: 초안 AI에게는 웹 검색 도구가 없다(§4.3). 따라서 실재 여부는 **입력 텍스트를 유일한 출처로 삼는 것**으로 보장한다 — 기획자가 적은 가게는 기획자가 실재를 아는 가게이고, AI가 보탠 가게는 아무도 확인할 수 없다. 그래서 후자를 금지한다.

#### ② 누락 0건 (기계 검사)

**입력 텍스트에서 추출된 고유명사는 전부 초안 안에 남아야 한다.** 카페 13곳을 적었으면 13곳이 다 있어야 하고, AI가 「대표적인 몇 곳」으로 줄이는 것은 실패다.

| 순서 | 검사 | 주체 |
|---:|---|---|
| 1 | 입력 텍스트에서 상점·숙소·장소 후보를 추출한다 | **기계** |
| 2 | 각 후보가 초안(`상점[]` · `숙박[]` · `일정원문` 중 하나)에 부분 문자열로 있는지 검사한다 | **기계** |
| 3 | 없는 후보를 `notes.누락[]`에 담아 화면에 표시한다 | **기계** |

판정은 기계가 하고 AI에게 되묻지 않는다 — AI 호출은 1회이며(§4.2) 자기 누락을 자기가 판정하는 구조를 만들지 않는다. 누락이 있으면 화면이 그 목록을 보여주고 사람이 [다시 채우기]를 누르거나 직접 입력한다.

#### ③ 필드별 출처 표기 (`origin`)

초안의 모든 필드는 **어디서 왔는지**를 함께 반환한다. 폼은 이 값으로 칸의 표시를 바꾼다.

| `origin` | 의미 | 폼 표시 |
|---|---|---|
| `input` | 입력 텍스트에 그대로 있던 값 | 표시 없음 |
| `planned` | AI가 배분·정리해 쓴 값 (일정 서술, 식사정보 등) | 「AI 초안」 배지 |
| `empty` | 채우지 않았다 | 「직접 입력 필요」 · 빨간 표시 |

**`가격.성인`은 항상 `empty`다.** 금액은 공개 페이지에 그대로 실리고(§9.3 `sec_price`) 신청 이메일에도 들어가므로(§13.3), 사람이 입력하지 않은 숫자가 고객에게 도달하는 경로를 만들지 않는다. 초안이 이 칸을 비워 두므로 §7.1 검증이 제출을 막고, 기획자는 반드시 이 값을 본다.

#### 실행 예산

| 항목 | 값 |
|---|---|
| AI 호출 | 1회 · **`effort: plan`**(추론 `low`). 근거는 §4.3의 실측 — `medium`은 추론이 예산을 다 먹고 출력이 잘린다 |
| 타임아웃 | 25초(§4.3). 입력 4000자 + 장소 목록이므로 §5.5의 `processing_delayed`(20초)를 넘길 수 있다 |
| `maxDuration` | 60초 |
| 이상 플래그 | **남기지 않는다** — 상품 행이 없어 `abnormality_flags.product_id`를 채울 수 없다(§5.5) |

---

## 8. Step 2 — 소개서(간단버전) 생성

§4.2의 1요청 1AI호출 원칙에 따라 **4개 요청**으로 나눈다. ①은 AI를 쓰지 않으므로 즉시 끝나고, ②~④가 각각 AI 1회를 쓴다.

### 8.1 ① 상품 등록 — `POST /api/products`

AI 호출 없음. 폼 값을 검증하고 상품 행을 만든다.

1. §7.1·§7.2 규칙으로 서버 재검증. 위반 시 **400**을 반환하고 행을 만들지 않는다.
2. `products` 행 생성: `form_input` 저장, `execution_id` 발급, `attempt_no = 1`, `retry_counts` 전부 0, `status = generating`, `current_step = pipeline_started`.
3. 이미지를 Storage에 업로드하고 `product_images`에 슬롯별로 기록.
4. `execution_logs`에 `pipeline_started` 기록.
5. **200 + `{product_id}`** 반환.

### 8.2 ② 일차 분해 및 확정 데이터표 — `POST /api/products/{id}/decompose`

AI 1회. `form_input`을 §6.2 정규화와 §6.3 일차 분해로 `confirmed_data`로 만든다.

1. 정규화 3종 적용(기계 처리).
2. 일정 원문 일차 분해(AI 1회).
3. **0차 검증**(§11.1) 실행 — 기계 검사 중심이므로 추가 AI 호출을 하지 않는다.
4. `execution_logs`에 `itinerary_decomposed`, `normalization_validated` 기록.
5. 분해 실패(일차 초과·구분 불가) 시 `status = input_error` + `failure_reason`, `pipeline_aborted` 플래그, **422**를 반환한다.
6. 0차 검증 실패 시 — 재시도 여력이 있으면 `retry_counts.normalization += 1` 후 **409 `{reason: "retry", retry_from: 2}`**(클라이언트가 이 단계를 재호출), 소진되면 `status = input_error` + `pipeline_aborted` 플래그, **422**.
7. 성공 시 `validation_snapshot.axes.axis_0 = pass`, `current_step = normalization_validated`, **200**.

### 8.3 ③ 소개서 생성 — `POST /api/products/{id}/brochure`

AI 1회(§4.3 계약). `confirmed_data`로 `brochure_content`(§8.7 스키마)를 만든다.

1. AI 호출 — 출력은 §8.7의 8개 섹션 스키마로 강제한다.
2. **서버 검사**(AI 호출 없음): 섹션 8개·순서 일치, `source` 누락 0건, 미치환 토큰 0건, 길이 계약 4종(§17.1).
3. 성공 시 `brochure_content` 저장, `current_step = brochure_generated`, **200**.
4. `execution_logs`에 `brochure_generated` 기록(성공·실패 모두).

**실패 처리** — 생성 실패는 §4.3의 실패 표를 따른다.

- 재시도 여력 있음 → `retry_counts.brochure += 1`, **409 `{reason: "retry", retry_from: 3}`**.
- 재시도 소진 → `status = brochure_ready`, `axis_1 = fail`, `items`에 실패 사유 1건(`검증영역: "생성"`), `pipeline_aborted` 플래그, **200**.

> 생성 실패가 1차 검증 실패와 같은 카운터·같은 소진 상태를 쓰는 이유: 기획자에게는 둘 다 "소개서가 제대로 안 나왔다"이고, 복귀 대상도 ③ 재호출로 동일하다. 원인 구분은 `execution_logs`의 `output`에 남는다.

### 8.4 ④ 1차 검증 — `POST /api/products/{id}/validate-brochure`

AI 1회. `form_input` vs `brochure_content` 대조(§11.1 1차).

- 통과 → `status = brochure_ready`, `validation_snapshot.axes.axis_1 = pass`.
- 실패 & 재시도 여력 있음 → `retry_counts.brochure += 1`, **409 `{reason: "retry", retry_from: 3}` + 실패 항목**. 클라이언트가 ③부터 재호출한다.
- 실패 & 재시도 소진(`retry_counts.brochure = 2`) → **`status = brochure_ready`, `axis_1 = fail`**, `pipeline_aborted` 플래그, 200 + 실패 항목.

`execution_logs`에 `validation_1_completed`(통과·실패 모두) 기록.

> 재시도가 소진되어도 `input_error`가 아니라 `brochure_ready`가 되는 이유: 소개서는 이미 만들어졌고 사용자 입력에는 문제가 없다. 검토 화면에서 실패 항목을 보여주고 [다시 생성]·[입력 수정]을 제공하는 것이 맞다(§15.1).

### 8.5 클라이언트 호출 순서

```text
[소개서 생성] 버튼
  │
  ├─ ① POST /api/products                        (AI 0회 · 즉시)     "저장 중…"
  ├─ ② POST /api/products/{id}/decompose         (AI 1회 · ~20초)   "일정 정리 중…"
  ├─ ③ POST /api/products/{id}/brochure          (AI 1회 · ~20초)   "소개서 작성 중…"
  └─ ④ POST /api/products/{id}/validate-brochure (AI 1회 · ~20초)   "검사 중…"
                                                  ─────────────────
                                        요청당 60초 예산 / 총 체감 ~60초
```

각 요청이 독립적으로 60초 예산을 가지므로 §4.2의 제약을 위반하지 않는다. 실패 시 클라이언트는 해당 단계부터 재호출한다.

### 8.6 소개서의 위치 정의

소개서(간단버전)는 **기획자가 상품 페이지를 생성할지 판단하기 위한 검토용 문서**다. 폼에서 입력받은 필수 폼 그룹 6개와 선택 4개 항목을 **전부 반영한다.**

**"간단버전"은 섹션을 줄인다는 뜻이 아니라 표현을 줄인다는 뜻이다.**

| 구분 | 소개서(간단버전) | 상품 페이지 |
|---|---|---|
| 반영 사실정보 | 입력된 전 항목 | 입력된 전 항목 (동일) |
| 서술 분량 | 압축 (개요 2~3문장, 일차별 1~2문장) | 확장 서술 |
| 이미지 | 없음 | 슬롯별 배치 |
| 테마·디자인 | 없음 (단일 컬럼 텍스트) | 여행스타일 테마 적용 |
| 신청 폼 | 없음 | 있음 |
| 용도 | 기획자 검토 | 고객 열람·신청 |

### 8.7 섹션 구성 (8개 · `data` 키 확정)

**이 표가 `brochure_content` 스키마의 단일 출처다.** `data` 키 이름이 `source` 맵의 키와 1:1로 대응하므로, 키를 임의로 바꾸면 검증이 성립하지 않는다.

| 순서 | `id` | `type` | `data` 키 | `source` 값 |
|---:|---|---|---|---|
| 1 | `b_title` | `title` | `text` | `행사정보.행사명` |
| 2 | `b_overview` | `overview` | `여행지` · `행사기간`(선택) · `여행기간` · `타겟층` · `여행스타일` · `핵심일정` | `핵심일정` 외 전부 `행사정보.*`, `핵심일정`은 `"generated"`. `행사기간`이 빈 값이면 렌더러가 표시하지 않는다 |
| 3 | `b_itinerary` | `itinerary` | `days[]` = `{day, text}` | `days`: `행사정보.일정` |
| 4 | `b_accommodation` | `accommodation` | `숙소들[]` = `{숙소명, 객실타입, 위치, 숙박일정}` | `숙소들`: `숙박` |
| 5 | `b_flight` | `flight` | `공항` · `항공사` · `편명` · `출발시간` · `도착시간` | `항공편.*` |
| 6 | `b_meal` | `meal` | `식사정보` | `식사.식사정보` |
| 7 | `b_price` | `price` | `성인` · `아동` · `기타` | `가격.*` |
| 8 | `b_shop` | `shop` | `상점들[]` = `{상점명, 구분, 위치, 상점정보}` | `상점들`: `상점` |

| 규칙 | 내용 |
|---|---|
| `id` 접두사 | `b_`. 상품 페이지는 `sec_`를 쓴다(§9.2) |
| 값 타입 | 전부 문자열. 배열은 `days` · `숙소들` · `상점들` **3개**이며 그 원소도 전부 문자열 필드다 |
| 배열 섹션의 `source` | 배열 필드 자체에 **배열의 경로 하나**를 적는다(`숙소들`: `숙박`). 원소 단위 대조는 검증기가 인덱스를 붙여(`숙박[0].숙소명`) 수행한다 — `days`가 `행사정보.일정` 하나를 적는 것과 같은 규약이다 |
| 배열 원소 수 | 확정 데이터표의 배열 길이와 **정확히 일치**한다. 행을 요약·병합·생략하지 않는다 |
| 이미지·테마·신청 폼 | **필드를 넣지 않는다.** 소개서에는 없는 개념이다(§8.6) |
| `visible`·`locked`·`order` | **넣지 않는다.** 소개서는 편집 대상이 아니라 읽기 전용 검토 문서다 |
| `핵심일정` | `source`가 `"generated"`인 유일한 필드. `행사정보.일정`의 `내용` 값들만 근거로 2~3문장 |

**필수 폼 그룹 6개의 값은 항상 위 8개 섹션 안에 전부 반영된다.** 필수 요소를 제외하는 구성은 허용하지 않는다. 미입력 표기는 §6.1이 단일 출처이며, 페이지와 동일한 표기이므로 §11의 검증에서 양쪽 섹션이 1:1로 대응한다.

**이미지를 포함하지 않는 근거**: 소개서는 사실정보 정확성을 빠르게 확인하는 문서이고, 이미지 배치 확인은 편집기 미리보기(§10.1)에서 수행한다.

### 8.8 산출 형태

`brochure_content` jsonb에 저장하고, 고정 React 컴포넌트가 렌더링한다. 별도 HTML 파일을 생성하지 않는다.

최상위는 `{schema_version, sections[]}`이며, 각 섹션은 `{id, type, data, source}` 4개 필드를 가진다. **8개 섹션 전부의 `data` 키와 `source` 값은 §8.7 표가 단일 출처다.**

```json
{
  "schema_version": "1.0",
  "sections": [
    { "id": "b_title", "type": "title",
      "data":   { "text": "제주 올레 바람 여행" },
      "source": { "text": "행사정보.행사명" } }
  ]
}
```

`source` 맵은 각 필드가 어느 경로에서 왔는지를 기록하며, 검증의 기준이 된다. 출처가 없는 연결 문구는 `source`를 `"generated"`로 표기한다.

**`source` 경로는 `form_input`과 `confirmed_data`에 공통으로 적용된다**(§7.4). 두 데이터가 같은 6개 최상위 키와 같은 경로 표기를 쓰므로, 1·2차 검증은 `source` 경로를 그대로 `form_input`에 적용해 기준값을 조회한다. 유일한 예외인 `행사정보.여행기간`은 §6.2.1의 결합 규칙으로 대조한다.

**조사 처리는 값 필드에 적용하지 않는다.** 치환 시 조사를 자동 적용하는 표기(`{{숙박[0].객실타입|로}}`)는 `source`가 `"generated"`인 **서술 필드 안에서만** 쓸 수 있다. 사실정보 값 필드에 조사를 붙이면(`디럭스룸` → `디럭스룸으로`) `source` 경로의 원본값과 달라져 1·2차 검증이 실패한다.

### 8.9 검토 화면 동작

- 소개서 8개 섹션을 읽기 전용으로 표시한다.
- 1차 검증 결과(통과/실패, 실패 항목 전체)를 함께 표시한다.
- 버튼 구성은 §15.1 표를 따른다. **`axis_1 = fail`이면 [상품 생성]이 잠기고 [다시 생성]·[입력 수정]이 제공된다.**

---

## 9. Step 3 — 상품 페이지 생성

§4.2 원칙에 따라 **3개 요청**으로 나눈다.

### 9.1 콘텐츠 모델이 산출물이다 (HTML 아님)

AI는 **`page_content` JSON만 생성**하고, 렌더링은 고정된 React 컴포넌트 세트가 담당한다.

| 이유 | 설명 |
|---|---|
| 편집기 성립 | 섹션 단위 수정·삭제·삽입은 안정적인 구조가 있어야 가능하다 |
| 반응형 보증 | 컴포넌트가 고정이므로 375/768/1280 동작이 사전에 보증된다 |
| 검증 가능 | `source` 맵으로 필드별 출처 대조가 가능하다 |
| 성능 | HTML 전문 생성 대비 출력 토큰과 지연이 크게 줄어든다 |

### 9.2 `page_content` 스키마

> **2.8 — 고정 형식을 없앴다.** 섹션 개수·순서·타입은 이제 **AI가 정한다.** 아래 「9개」는
> 어휘 목록이지 필수 구성이 아니다. `schema_version`은 `"2.0"`, `theme`은 계산된 객체(§9.4),
> 각 섹션은 `style`(블록별 레이아웃)을 추가로 가질 수 있다. 어휘·layout·타입별 재료·타입별
> 길이 계약은 `lib/pipeline/vocabulary.ts`가 단일 출처다. **값 필드는 `buildPage`가 치환하고
> AI는 `type`·스타일·서술만 낸다** — 아래 `data` 키 표는 그 치환 규약이다.

최상위는 `{schema_version, theme, sections[]}`이며, 각 섹션은 **`{id, type, order, visible, locked, style?, data, source}`** 필드를 가진다. 각 타입의 `data` 키·`locked`·`source`는 §9.3 표가 단일 출처다.

```json
{
  "schema_version": "1.0",
  "theme": "nature",
  "sections": [
    { "id": "sec_hero", "type": "hero", "order": 1, "visible": true, "locked": true,
      "data":   { "headline": "제주 올레 바람 여행", "subcopy": "2026-03-14 ~ 2026-03-17", "image_slot": "hero" },
      "source": { "headline": "행사정보.행사명", "subcopy": "행사정보.여행기간" } }
  ]
}
```

`theme`은 여기에만 보관한다(§5.1). 일차 이미지는 `data.days[].image_slot`으로 참조한다 — 이 필드가 없으면 §11.2의 이미지 검증이 대상을 찾을 수 없다.

### 9.3 블록 어휘 (`data` 키 확정)

> **2.8 — 「9개 섹션」이 아니라 「블록 어휘」다.** 아래 9종은 사실정보를 담는 블록이고, 여기에
> **삽입 3종**(`free_text`·`image`·`notice`, §10.2)과 **추가 7종**(`gallery`·`highlight`·`spotlight`·
> `timeline`·`cta`·`stat`·`divider`)이 더해진다. AI가 이 어휘 안에서 **몇 개·어떤 순서·어떤
> 분위기로** 놓을지 정한다. `timeline`은 `itinerary`와 같은 재료(일정)다. `quote`(후기)류는
> 어휘에 **없다** — 입력에 없는 값이라 만들면 §16.1 위반이다. 단일 출처는 `vocabulary.ts`.
> hero는 항상 처음·apply는 항상 마지막(둘 다 locked), 그 사이는 AI 계획 순서다.

**아래 표는 사실정보 블록 9종의 `data` 키 치환 규약이다.** 각 섹션은 `{id, type, order, visible, locked, style?, data, source}` 최상위 필드를 가진다(§9.2).

| `order` | `id` | `type` | `data` 키 | `locked` | 소개서 대응 |
|---:|---|---|---|:---:|---|
| 1 | `sec_hero` | `hero` | `headline` · `subcopy` · `image_slot` | **true** | `b_title` |
| 2 | `sec_summary` | `summary` | `여행기간` · `여행지` · `타겟층` · `여행스타일` | false | `b_overview` |
| 3 | `sec_itinerary` | `itinerary` | `days[]` = `{day, text, image_slot}` | false | `b_itinerary` |
| 4 | `sec_accommodation` | `accommodation` | `숙소들[]` = `{숙소명, 객실타입, 위치, 숙박일정}` · `image_slots[]` | false | `b_accommodation` |
| 5 | `sec_flight` | `flight` | `공항` · `항공사` · `편명` · `출발시간` · `도착시간` | false | `b_flight` |
| 6 | `sec_meal` | `meal` | `식사정보` | false | `b_meal` |
| 7 | `sec_price` | `price` | `성인` · `아동` · `기타` | false | `b_price` |
| 8 | `sec_shop` | `shop` | `상점들[]` = `{상점명, 구분, 위치, 상점정보}` · `image_slots[]` | false | `b_shop` |
| 9 | `sec_apply` | `apply` | `제목` · `안내문구` · `가격요약`{`성인`,`아동`} · `행사정보요약`{`행사명`,`여행기간`} | **true** | — (§11.1에서 `skipped`) |

**`source` 값**은 §8.7의 소개서 표를 승계한다. 페이지에만 있는 필드는 아래 3줄이 전부다.

| 필드 | `source` |
|---|---|
| `hero.headline` · `hero.subcopy` | `행사정보.행사명` · `행사정보.여행기간` |
| `apply.*` | `제목`·`안내문구`는 `"generated"`, `가격요약`·`행사정보요약`은 `가격.*`·`행사정보.*` |
| `image_slot` · `image_slots` | **붙이지 않는다.** 사실정보가 아니라 이미지 참조다 |

#### 이미지 참조 필드 (단수·복수 구분)

| 필드 | 타입 | 쓰는 섹션 | 값 |
|---|---|---|---|
| `image_slot` | 문자열 | `hero`, `itinerary.days[]` | 슬롯 이름 1개. `hero` / `itinerary_day_{n}` |
| `image_slots` | 문자열 배열 | `accommodation`, `shop` | 같은 슬롯의 사진이 여러 장일 때. `["accommodation"]`처럼 슬롯 이름을 담고, 실제 장수·순서는 `product_images.sort_order`가 결정한다 |

슬롯에 이미지가 없어도 **필드는 남긴다.** 렌더링 시 이미지 영역만 생략하며 섹션은 유지한다(§7.3). `hero` 미업로드 시에만 테마 그라디언트로 폴백한다.

미입력·미운영 표기(`추후 추가 예정` · `해당 없음`)는 **§6.1이 단일 출처**다. 섹션을 삭제하지 않는다.

`hero`와 `apply`는 `locked: true`로, 편집기에서 삭제할 수 없다(§10.2). **`sec_apply`의 신청 폼 필드 구성(이름·이메일·연락처·인원수·동의)은 `data`에 넣지 않는다** — 고정 컴포넌트가 렌더링하며 편집 불가다(§13.1).

### 9.4 테마 — 「색의 의도」를 AI가, 색을 기계가

> **2.8 — 여행스타일 1:1 룩업을 버렸다.** 감귤축제 상품이 여행스타일 「자연」이면 초록이
> 나오던 문제를 없앤다. **AI는 색이 아니라 `hue`(0~359)+`mood`를 쓰고, 기계가 OKLCH로 색을
> 계산한 뒤 WCAG 대비 4종을 강제한다.** 「AI가 `#RRGGBB`를 쓰지 않는다」는 그대로 지켜지고,
> 보증이 AI가 아니라 **계산기 쪽에** 남는다.

AI가 내는 디자인 의도(`theme`): `hue`·`mood`(vivid·warm·cool·earthy·muted·deep)·`background`(6종)·
`headline`(7종)·`accent`(6종)·`rhythm`·`scale`·`근거`. **색값은 없다.** 무효 필드는 여행스타일
매핑 → 무채색으로 **필드별** 폴백하며, 폴백 목록을 로그에 남긴다(`resolveThemeSpec`).

기계는 `hue+mood`에서 `primary·secondary·surface·surfaceDeep·text` 5색을 유도하고 대비 4종을 강제한다:

| 대비 | 기준 | 용도 |
|---|---|---|
| `text` vs `surfaceDeep` | ≥ 7:1 | 본문(§17.2의 4.5:1에 여유) |
| `primary` vs `#FFFFFF` | ≥ 4.5:1 | `tone:invert` 띠·`block-accent`의 흰 글자 |
| `secondary` vs `text` | ≥ 4.5:1 | `tone:tint` 띠·`pill-badge` |
| `primary` vs `surface` | ≥ 3:1 | 제목·강조(대형 텍스트) |

`npm run verify:a11y`가 **hue 360 × mood 6 = 2160 조합 전수**로 이를 증명한다. 저장은 계산된
객체(§9.2)이며, **레거시 문자열 테마**(`"nature"` 등)는 옛 `THEME_TOKENS` hex로 그대로 렌더한다 —
게시된 페이지의 색이 바뀌지 않는다. 테마는 편집기에서 변경할 수 없다.

### 9.5 API 분할

| 순서 | 엔드포인트 | AI | 동작 |
|---:|---|---:|---|
| ① | `POST /api/products/{id}/page` | 1회 | `page_content` 생성(§9.3 스키마), slug 발급(§12.1), 로그 `page_generated` |
| ② | `POST /api/products/{id}/validate-page` | 1회 | **2차 검증**(`form_input` vs `page_content`), 로그 `validation_2_completed` |
| ③ | `POST /api/products/{id}/validate-consistency` | 1회 | **3차 검증**(소개서 vs 페이지), 로그 `validation_3_completed` → `status = draft`, 로그 `draft_registered` |

**②가 실패하면 ③을 호출하지 않는다**(§11.1). 클라이언트는 ②의 응답을 보고 ③ 호출 여부를 결정한다. [상품 생성] 버튼은 `page`·`consistency` 카운터를 초기화한다(§11.6).

#### ① 페이지 생성

**서버 검사**(AI 호출 없음 · 2.8): **어휘 준수**(모든 `type`이 어휘에), **`source` 커버리지**(확정 데이터의 모든 사실정보 경로가 페이지 어느 블록에든 있다 — `해당 없음`은 제외, `추후 추가 예정`은 포함), hero 1개(order 1·locked)·apply 1개(마지막·locked), `order` 1..n 연속, `source` 누락 0건, 미치환 토큰 0건, **타입별** 길이 계약, `image_slot`·`image_slots` 값이 존재하는 슬롯인지. (2.7의 「섹션 9개·순서 일치」를 대체한다.)

**실패 처리** — §4.3의 실패 표를 따르되 **소진 시 `draft`로 가지 않는다.**

| 상황 | 처리 |
|---|---|
| 재시도 여력 있음 | `retry_counts.page += 1`, **409 `{reason: "retry", retry_from: 5}`** |
| 재시도 소진 | **`status = brochure_ready`로 되돌리고** `axis_2 = fail`, `items` 1건, `pipeline_aborted` 플래그, **200** |

> `draft`로 보내지 않는 이유: `page_content`가 없으면 편집기가 렌더링할 대상이 없고, §14.5 #10의 편집 시작 조건도 `page_content` 존재를 요구한다. 소개서까지는 정상이므로 소개서 검토 화면으로 되돌린다.

#### ②③ 검증

| 상황 | 처리 |
|---|---|
| 재시도 여력 있음 | 해당 카운터 +1, **409 `{reason: "retry", retry_from: 5}` + `items`**. 클라이언트가 ①부터 재호출 |
| 재시도 소진 | **`status = draft`, 해당 축 `fail`**, `pipeline_aborted` 플래그, **200 + `items`**. 편집·책임 게시 경로 유지(§11.5) |

**`draft` 전이는 어느 경로로 도달해도 `draft_registered`를 기록한다.** ②가 실패로 확정되면 ③을 호출하지 않으므로, 그때는 ②의 라우트가 전이와 로그를 함께 담당한다.

| `draft` 도달 경로 | 주체 | `axis_3` |
|---|---|---|
| ③ 통과 / ③ 소진 | ③ | `pass` / `fail` |
| **② 소진**(③ 미호출) | **②** | `null` (미실행) |

### 9.6 클라이언트 호출 순서

```text
[상품 생성] 버튼
  │
  ├─ ① POST /api/products/{id}/page                 (AI 1회) "페이지 구성 중…"
  ├─ ② POST /api/products/{id}/validate-page        (AI 1회) "사실 확인 중…"
  └─ ③ POST /api/products/{id}/validate-consistency (AI 1회) "소개서와 대조 중…"
        └─ ②가 fail이면 ③을 건너뛴다
```

### 9.7 실행 예산

§4.2(요청·타임아웃 상한)와 §4.3(AI 호출 계약)이 단일 출처다. 이 절에만 있는 값은 아래 2개다.

| 항목 | 값 |
|---|---|
| 소개서 완료까지 체감 시간 (4요청 순차) | 목표 ≤ 60초 |
| 페이지 완료까지 체감 시간 (3요청 순차) | 목표 ≤ 60초 |

---

## 10. Step 4 — 편집기

**경로**: `/admin/products/{id}/edit` (인증 필요)

### 10.1 화면 구성

좌측 섹션 목록 + 중앙 편집 패널 + 우측 실시간 미리보기(375 / 768 / 1280 뷰포트 전환). 임시저장 페이지의 미리보기는 이 화면에서만 가능하며 공개 URL로 노출되지 않는다.

`draft` / `reviewing` 상태에서는 slug 편집 필드도 제공한다(§12.1).

### 10.2 편집 가능 범위

> **2.8 — 불변 대상이 「고정 9종」에서 「그 상품이 생성한 집합」으로 바뀌었다.** 구성이
> 자유로워 고정 9종이 없으므로, 저장 직전 DB의 non-insert 섹션(생성된 집합)이 불변 멤버십이다.
> 삽입 3종(`blk_`)만 통째로 걷어낼 수 있다. hero·apply 판정은 id가 아니라 **type**으로 한다.
> 테마가 객체가 되어 불변 비교는 값 비교(`same`)로 한다. 길이 계약은 `vocabulary.ts`가 단일
> 출처라 생성 시점과 저장 시점 상한이 갈릴 수 없다.

| 동작 | 허용 범위 |
|---|---|
| 수정 | 섹션의 `data` 필드 텍스트, 이미지 슬롯 교체, 대체 텍스트 |
| 삭제 | `locked: false`인 섹션. `visible: false`로 전환하며 데이터는 보존한다 |
| 삽입 | `free_text`, `image`, `notice` 3종 블록 |
| 순서변경 | `order` 값 변경. `hero`는 항상 첫 번째, `apply`는 항상 마지막 |
| 삭제 불가 | `hero`, `apply` (`locked: true`) · 생성된 집합의 멤버십(숨김만 가능) |
| `apply` 예외 | 안내 문구·제목만 편집 가능. **신청 폼의 필드 구성(이름·이메일·연락처·인원수·동의)은 편집할 수 없다** — 신청 API 계약(§13.1)과 어긋나기 때문이다 |
| `image` 블록 | **이미 업로드된 이미지 중에서만 선택**한다. 편집기에서 새 업로드를 하지 않는다(§7.3) |
| 테마 | 변경 불가(§9.4) |
| 길이 계약 | §17.1 상한을 초과하는 입력은 저장 시 거부한다 |

#### 삽입 블록 스키마 (3종)

삽입 블록도 `page_content.sections` 배열의 원소이며 §9.3의 7개 최상위 필드를 그대로 가진다. 다른 점은 `id` 접두사와 `source`다.

| `type` | `id` 접두사 | `data` 키 | `locked` | `source` |
|---|---|---|:---:|---|
| `free_text` | `blk_` | `제목`(선택) · `본문` | false | 전 필드 `"generated"` |
| `image` | `blk_` | `image_id` · `캡션`(선택) | false | 없음 (사실정보 아님) |
| `notice` | `blk_` | `본문` | false | `"generated"` |

| 규칙 | 내용 |
|---|---|
| `image_id` | **`product_images.id`를 직접 참조한다.** 슬롯 이름이 아니다 — 이미 업로드된 특정 사진 1장을 고르는 것이므로 슬롯 체계 밖이다(§7.3) |
| `order` | 저장 시 전 섹션을 1부터 다시 번호 매긴다. `hero`는 항상 1, `apply`는 항상 마지막 |
| 삽입 위치 | `hero`와 `apply` **사이에만** 삽입할 수 있다 |
| 검증 대상 | **삽입 블록은 검증 대상이 아니다.** 2·3차 검증은 편집 이전 시점에 실행되므로 블록이 존재하지 않는다. 편집분의 정확성은 기획자 책임이다(§10.4) |
| 개수 상한 | 블록 종류별 상한은 두지 않는다. 길이 계약(§17.1)만 적용한다 |

### 10.3 저장 시 동작

`PATCH /api/products/{id}/content` 호출 시:

1. 길이 계약을 검사한다. 위반 시 400을 반환하고 저장하지 않는다.
2. `page_content`를 갱신한다.
3. `human_edited = true`로 설정한다.
4. `status`가 `draft`이면 `reviewing`으로 전이한다. `published` / `unpublished` / `reviewing`이면 유지한다.
5. 변경된 섹션마다 `edit_history`에 `{action, section_id, before, after}`를 기록한다.
6. `execution_logs`에 `content_edited`(`category = lifecycle`)를 기록한다.
7. **`form_input`, `confirmed_data`, `validation_snapshot`, `attempt_no`는 수정하지 않는다.**

### 10.4 편집분에 대한 책임 분리

AI 검증은 §11.4의 기준 시점에 고정되며, **편집 이후 재검증하지 않는다.** 편집분의 사실 정확성은 기획자 책임이다.

이를 명시하기 위해 관리 페이지와 편집기에 **배지 2종을 각각 독립적으로** 표시한다. 두 축을 하나로 합치면 조건이 겹치고, 검증 실패 시 편집 여부가 화면에서 사라진다.

| 배지 축 | 값 | 조건 |
|---|---|---|
| **검증** | `AI 검증 통과` | `validation_snapshot.verdict = pass` |
| | `AI 검증 실패` | `verdict = fail` 이고 `publish_override_at`이 없음 |
| | `검증 실패 · 책임 게시됨` | `verdict = fail` 이고 `publish_override_at`이 있음 |
| **편집** | (표시 없음) | `human_edited = false` |
| | `사람 편집됨` | `human_edited = true` |

두 축은 서로 독립이므로 `AI 검증 실패` + `사람 편집됨`이 동시에 표시될 수 있다. 검증 배지 3종은 상호 배타적이며 위에서 아래 순으로 판정한다.

---

## 11. 검증 정책

### 11.1 검증 4축 — 기준값은 `form_input`

**기준값은 `form_input`이다.** `confirmed_data`를 기준으로 삼으면 정규화·분해 단계에서 값이 변형돼도 어느 축도 잡아내지 못하므로, 사용자가 실제로 입력한 값을 기준으로 두고 `confirmed_data`의 생성 자체를 0차로 검증한다.

| 축 | 시점 | 대상 | 스킬 | 판정 범위 |
|---|---|---|---|---|
| **0차** | §8.2 ② 직후 | `form_input` vs `confirmed_data` | 기계 검사 + `itinerary-decomposition`(3단계 판정) | 정규화 3종(§6.2)·결합 1종(§6.2.1) 외 변형 0건, 일차 분해의 원문 근거(§6.3) |
| 1차 | §8.4 ④ | **`form_input`** vs `brochure_content` | `fact-check` | 소개서 8개 섹션 전 필드 |
| 2차 | §9.5 ② | **`form_input`** vs `page_content` | `fact-check` | 페이지 9개 섹션 전 필드 (**주 검증**) |
| 3차 | §9.5 ③ (2차 통과 후) | `brochure_content` vs `page_content` | `consistency-check` | 대응표 기준, `apply` 제외 전 섹션 |

**0차를 둔 이유**: 기준값이 `form_input`이 되면서 "정규화가 규칙대로 됐는가"가 별도 판정 대상이 된다. 이 축은 문자열 비교 위주라 **추가 AI 호출 없이** ② 요청 안에서 처리한다. 3단계 판정 중 AI가 필요한 마지막 단계는 `itinerary-decomposition`에 배정된 호출 1회 안에서 함께 수행한다(§6.3).

**3차의 목적과 실행 조건**: 1·2차가 모두 통과하면 3차는 논리적으로 통과해야 한다. 따라서 3차는 **교차 검증·회귀 감지용**이다 — `source` 맵 누락, 두 생성 경로의 스키마 드리프트, 검증에서 빠진 필드를 잡아낸다. **2차 실패 시 3차는 실행하지 않는다.**

#### 3차 검증 섹션 대응표

| 소개서 섹션 | 상품 페이지 섹션 | 대조 대상 값 |
|---|---|---|
| 제목 | `hero` | 행사명 |
| 개요 | `summary` | 여행지, 여행기간, 타겟층, 여행스타일 |
| 일정 | `itinerary` | **일차 수, 각 일차의 사실정보 값(장소·활동·식사)** |
| 숙박 | `accommodation` | **숙소 행 수**, 행마다 숙소명·위치·객실타입·숙박일정 |
| 항공 | `flight` | 공항, 항공사, 편명, 출발·도착 시간 |
| 식사 | `meal` | 식사정보 |
| 가격 | `price` | 성인, 아동, 기타 |
| 제휴상점 | `shop` | **상점 행 수**, 행마다 상점명·구분·위치·상점정보 |
| — | `apply` | 소개서에 대응 섹션이 없어 제외. 단 `apply` 내부의 가격·행사 정보는 `price`·`hero`와 일치해야 한다 |

**일정 행에서 `원문근거`를 대조하지 않는다.** 그 필드는 `confirmed_data`에만 있고 두 콘텐츠 모델에는 없다 — 0차의 몫이다.

**대조 단위는 사실정보 값이다.** 소개서는 압축 서술, 상품 페이지는 확장 서술이므로 **문장 전체의 문자열 일치는 요구하지 않는다.** 값을 감싸는 서술 문장의 길이·어순·문장 수 차이는 실패 사유가 아니며, `source` 맵이 가리키는 **값 자체가 다르면** 실패다.

선택 항목 미입력 시 양쪽 모두 `추후 추가 예정`이어야 한다. 한쪽만 공란이거나 한쪽에서 섹션이 삭제된 경우 실패로 판정한다.

**배열 섹션(일정·숙박·상점)의 대조 단위는 원소다.** 검증기는 배열 필드의 `source` 경로에 인덱스를 붙여(`숙박[0].숙소명`) 원소 단위로 값을 맞댄다. **행 수가 다르면 그 자체로 실패**이고, 행 순서가 바뀐 것도 실패다 — 인덱스가 다른 원소를 가리키게 되어 값 대조가 성립하지 않는다. 행을 요약·병합해 줄이는 것은 §16.1의 부분 삭제에 해당한다.

### 11.2 검증 항목과 허용 차이

| 영역 | 확인 내용 | 적용 축 |
|---|---|---|
| 정규화 범위 | `form_input` → `confirmed_data`가 허용 3종(§6.2) + 결합 1종(§6.2.1)만 적용 | 0차 |
| 일정 근거 | `원문근거` ⊂ `일정원문`, `내용`이 §6.3 판정 3단계를 통과 | 0차 |
| 값 일치 | `source` 맵이 가리키는 경로의 값이 `form_input`의 대응 값과 일치 | 1·2차 |
| 치환 | 미치환 토큰(`{{...}}`)·조사 파이프 기호 0건, 오치환 0건 | 1·2차 |
| 출처 없는 수치 | 서술 필드의 숫자 토큰이 허용 출처 4종 중 하나에 존재(§6.3.1 알고리즘) | 1·2차 |
| 입력 외 고유명사 | 입력에 없는 지명·시설·경유지·관광지·부대시설 0건 | 1·2차 |
| 일정 일치 | 일차 수가 여행기간과 정확히 일치(초과·미달 0일) | 0·1·2·3차 |
| 선택 항목 | 미입력 항목이 `추후 추가 예정`으로 양쪽 동일 표기, 섹션 삭제 0건 | 1·2·3차 |
| 이미지 | `image_slot` 값이 업로드 시 지정 슬롯과 동일, 대체 텍스트 존재 | 2차 |
| 문서 간 일치 | 대응표 기준 사실정보 값 동일 | 3차 |

**허용 차이** (기준값이 `form_input`이 되면서 정규화 항목이 추가됐다)

| 허용 | 예 |
|---|---|
| 앞뒤 공백, 내부 연속 공백 축약 | `제주  올레` → `제주 올레` |
| HTML 이스케이프 | `&` → `&amp;` |
| **정규화 3종에 의한 표기 차이** | `120,000원` → `120000원`, `2026.03.14` → `2026-03-14` |
| **결합 1종에 의한 필드 차이**(§6.2.1) | `여행기간_시작` + `여행기간_종료` → `2026-03-14 ~ 2026-03-17` |
| **채움 1종에 의한 값 차이**(§6.1) | `form_input`의 `""` → 생성물의 `추후 추가 예정` |
| **분해 1종에 의한 경로 차이**(§6.3) | `행사정보.일정원문` → `행사정보.일정[]` |
| 값을 둘러싼 서술 문장의 분량·어순·문장 수 | 소개서 압축 vs 페이지 확장 |

**실패로 판정하는 차이**: 값의 임의 재표기, 요약, 윤문, 부분 삭제, 어순 변경, 약칭·영문 변환.

**채움과 분해는 「기준값은 `form_input`」의 예외가 아니라 그 규칙이 전제하는 변환이다.** 둘 다 `form_input`에 대응 값이 **없는 것이 정상**이므로, 대응 값이 없다는 사실만으로 실패 판정하면 안 된다.

| 변환 | `form_input` | 생성물 | 1·2차가 보는 것 |
|---|---|---|---|
| 채움(§6.1) | `""` (빈 문자열) | `추후 추가 예정` | `form_input`에 **값이 있는데** 생성물이 `추후 추가 예정`이면 실패. 그 역은 정상 |
| 분해(§6.3) | `행사정보.일정원문`만 존재. `행사정보.일정` 키는 **없다** | `행사정보.일정[]` | **일차 수**와 일차별 사실정보 값(장소·활동·식사)이 `일정원문`에 등장하는가. `원문근거` 대조는 0차의 몫이다(§11.2 「일정 근거」 행) |

> 이 두 행은 2.6에서 명시했다. 규정 자체는 §6.1·§6.3에 처음부터 있었으나 §11.2의 허용 목록에 옮겨 적히지 않아, **검증기가 「목록에 없으니 위반」으로 읽을 여지**가 남아 있었다. 실제로 공급자를 바꾸자 그 자리에서 1차 검증이 반려됐다 — 앞선 공급자가 규정을 추론으로 메워 주던 것이 드러난 것이며, 판정 기준은 추론이 아니라 이 문서여야 한다.

### 11.3 `validation_snapshot` 구조

```json
{
  "attempt_no": 1,
  "verdict": "pass",
  "validated_at": "2026-08-10T04:12:33Z",
  "content_hash": "sha256:...",
  "axes": {
    "axis_0": { "verdict": "pass", "items": [] },
    "axis_1": { "verdict": "pass", "items": [] },
    "axis_2": { "verdict": "pass", "items": [] },
    "axis_3": { "verdict": "pass", "skipped": ["apply"], "items": [] }
  }
}
```

최상위 `verdict`는 **완료된 전 축이 통과했을 때만** `pass`다. 한 축이라도 `fail`이면 `fail`이다. 아직 실행되지 않은 축은 `null`로 두며 `verdict` 계산에 넣지 않는다.

`content_hash`는 검증 시점 `page_content`의 SHA-256이다. **계산 방법**: 키를 사전순 정렬하고 공백 없이 직렬화한 JSON 문자열을 해싱한다(`sha256:` 접두사 포함). 정렬 없이 해싱하면 같은 내용이 다른 해시를 낸다. 용도는 **§10.4 배지 판정이 아니라 검증 시점 추적**이다 — 현재 해시와 다르면 그 이후 편집이 있었다는 뜻이며, 관리 화면 상세(§14.1)에 "검증 이후 편집됨"으로 표시한다.

#### `items` 구조 (실패 항목)

`axes.axis_n.items`는 **실패한 항목의 배열**이다. 통과 시 빈 배열이다. 검토 화면(§8.9)과 책임 게시 모달(§11.5)이 이 배열을 그대로 렌더링하므로 구조가 확정되어야 한다.

```json
{
  "검증영역": "숙박[0].숙소명",
  "source경로": "숙박[0].숙소명",
  "기준값": "롯데호텔 제주",
  "발견값": "롯데 제주 호텔",
  "사유": "값의 어순이 변경됨. 표기 임의 변경은 허용되지 않는다",
  "위치": "b_accommodation.data.숙소들[0].숙소명"
}
```

| 필드 | 타입 | 설명 |
|---|---|---|
| `검증영역` | string | §11.2 표의 영역명 또는 `source` 경로 |
| `source경로` | string \| null | 해당 필드의 `source` 값. 구조 문제(섹션 누락 등)면 `null` |
| `기준값` | string | 비교 기준. 1·2차는 `form_input` 값, 3차는 소개서 값 |
| `발견값` | string | 생성물에서 발견된 값 |
| `사유` | string | 사람이 읽고 바로 판단할 수 있는 한 문장 |
| `위치` | string | 콘텐츠 모델 안의 경로. 편집기가 해당 필드로 이동하는 데 쓴다 |

**실패 항목은 전부 담는다.** 첫 실패에서 멈추지 않으며, 개수 상한도 두지 않는다 — 기획자가 실패 항목 전체를 열람해야 하기 때문이다(§11.5).

### 11.4 기준 시점 고정

> **2.8 — 3차(소개서↔페이지)를 「섹션 대응」에서 「`source` 커버리지」로 옮겼다.** 페이지 구성이
> 자유로워져 소개서 8 ↔ 페이지 9 1:1 대응표(`SECTION_PAIRS`)가 성립하지 않는다. 대신 ①
> **소개서의 `source` 경로 집합 ⊆ 페이지의 `source` 경로 집합**, ② 같은 경로의 값이 문자열로
> 동일, ③ 일정 배열 대조(일차 수·번호·명사구 생존 — 일정 섹션을 **id가 아니라 type**으로 찾는다:
> `itinerary` 또는 `timeline`)로 판정한다. `skipped`는 여전히 `["apply"]`다. 2차 fact-check는
> `source` 조인이라 변경 없다 — 섹션 id에 의존하지 않는다.

- 각 축의 결과는 **그 축이 완료된 시점**에 기록되고, **같은 `attempt_no` 안에서 변경되지 않는다.**
- **편집은 `validation_snapshot`을 바꾸지 않는다**(§10.3 7항).
- **[다시 생성]으로 `attempt_no`가 올라갈 때만** 새 결과로 갱신된다. 이전 시도의 결과는 `execution_logs`에 남아 추적 가능하다.

### 11.5 게시 게이트

| `verdict` | 게시 버튼 | 절차 |
|---|---|---|
| `pass` | 활성 | `human_edited = true`면 확인 모달에 "편집된 내용은 AI 검증 대상이 아닙니다"를 표시하고 명시적 확인을 받는다 |
| `fail` | 기본 비활성 | **[검증 실패 항목 확인]** 모달에서 실패 항목 전체를 열람하고 "내용을 확인했으며 책임하에 게시합니다" 체크 시 활성된다. 게시 시 `publish_override_at` 기록 + `publish_override` 로그 |

`status = input_error`에는 게시 경로가 없다(§15.1).

> 책임 게시를 둔 이유: 검증 실패 한 번으로 상품을 영구히 게시 불가로 만들면 기획자가 직접 고쳐 쓸 수 있는 편집기의 의미가 사라진다. 대신 이력을 남겨 사후 추적이 가능하게 한다.

### 11.6 검증 실패 시 복귀

| 실패 지점 | 성격 | 복귀 대상 | 유지 항목 | 카운터 |
|---|---|---|---|---|
| 폼 검증(§8.1) | 입력 오류 | 폼 화면 | 없음(행 미생성) | 없음 |
| 일차 분해(§8.2) | 입력 오류 | 폼 화면 (값 유지) | `form_input` | 없음 |
| 0차 검증 | 생성 오류 | §8.2 ② 재호출 | `form_input` | **`normalization`** |
| **소개서 생성**(§8.3) | 생성 오류 | §8.3 ③ 재호출 | `form_input`, `confirmed_data` | `brochure` |
| 1차 검증 | 생성 오류 | §8.3 ③부터 재호출 | `form_input`, `confirmed_data` | `brochure` |
| **페이지 생성**(§9.5 ①) | 생성 오류 | §9.5 ① 재호출 | `form_input`, 소개서 | `page` |
| 2차 검증 | 생성 오류 | §9.5 ① 부터 재호출 | `form_input`, 소개서 | `page` |
| 3차 검증 | 생성 오류 | §9.5 ① 부터 재호출 | `form_input`, 소개서 | `consistency` |

**생성 실패와 검증 실패는 같은 카운터를 공유한다.** 복귀 대상과 소진 시 상태가 동일하므로 나눌 이유가 없다. 원인 구분은 `execution_logs`의 `output`에 남는다(§4.3).

재시도 카운터는 **`normalization` · `brochure` · `page` · `consistency` 4종**이다. 각 카운터의 상한은 2회(총 3회 시도)이며, **카운터끼리 예산을 공유하지 않는다.** 재시도 시 `form_input`은 항상 유지하고 문장·구성·렌더 결과만 다시 만든다.

> **0차와 1차가 카운터를 공유하면 안 되는 이유**: 0차가 재시도를 1회 쓰면 값이 1로 남아 1차의 재시도 예산이 줄고, 값이 2에 도달했을 때 소진 결과가 `input_error`(§8.2)인지 `brochure_ready + axis_1 = fail`(§8.4)인지 카운터 값만으로 판별할 수 없다. 복귀 대상도 §8.2 ②와 §8.3 ③으로 다르다.

**카운터와 축의 대응은 1:1이다.**

| 카운터 | 축 · 생성 단계 | 소진 시 상태 |
|---|---|---|
| `normalization` | 0차 | `input_error` (입력 문제) |
| `brochure` | 소개서 생성 · 1차 | `brochure_ready` + `axis_1 = fail` |
| `page` | 페이지 생성 · 2차 | 페이지 생성 실패는 `brochure_ready` + `axis_2 = fail`, 2차 검증 실패는 `draft` + `axis_2 = fail` |
| `consistency` | 3차 | `draft` + `axis_3 = fail` |

#### 카운터 초기화 시점

**사람이 조작한 시점에만 초기화한다.** 시스템의 자동 재호출은 초기화하지 않는다.

| 조작 | 초기화 대상 |
|---|---|
| 폼 제출 (§8.1) | 4종 전부 |
| [입력 수정 후 재제출] (§15.2) | 4종 전부 |
| [다시 생성] (§15.3) | 4종 전부 |
| **[상품 생성]** (§15.2) | `page` · `consistency` |

[상품 생성]이 초기화 대상인 이유: 페이지 생성이나 2·3차 검증이 소진돼 `brochure_ready`로 되돌아온 뒤 다시 누를 때, 초기화하지 않으면 즉시 실패한다.

---

## 12. Step 5 — 게시

**API**: `POST /api/products/{id}/publish` / `POST /api/products/{id}/unpublish` (인증 필요)

### 12.1 slug 정책

| 항목 | 규정 |
|---|---|
| 허용 문자 | **영문 소문자·숫자·하이픈만.** 한글·공백·특수문자 불허 |
| 생성 시점 | §9.5 ① (페이지 생성) |
| 재발급 금지 | 이미 `slug`가 있으면 검증 재시도·[다시 생성]에서 **재발급하지 않고 유지한다**(§15.3) |
| 자동 생성 규칙 | 행사명이 **영문·숫자·공백만으로 구성된 경우에만** 그것을 변환해 사용한다(공백 → 하이픈, 소문자화). 한글이 한 글자라도 포함되면 **로마자 변환을 시도하지 않고** `p-{6자 base36}`(예: `p-k3m9x2`)을 발급한다 |
| 중복 처리 | 동일 slug가 있으면 `-2`, `-3` … 접미사 부여 |
| 수정 가능 | `draft` / `reviewing` 상태에서만 편집기에서 변경 가능. 허용 문자·중복 검사 적용. 로그 `slug_changed` |
| **게시 후 불변** | `published`로 전이한 시점부터 변경 불가. 이후 행사명을 수정해도 URL은 유지된다 |

> **근거**: 자동 로마자 변환은 표기 왜곡(`제주` → `jeju`/`cheju`)을 낳고 규칙이 모호해지므로, 한글이 섞이면 무작위 slug를 발급하고 원하는 주소는 기획자가 직접 입력하게 한다. 한글 slug는 `/p/%EC%A0%9C%EC%A3%BC…`로 percent-encoding되어 링크 복사·QR에서 읽을 수 없다.

### 12.2 게시 동작

1. 게시 게이트(§11.5)를 확인한다.
2. `status = published`, `published_at` 기록(**최초 게시 시에만**. 재게시 시 덮어쓰지 않는다).
3. `execution_logs`에 `published`(`category = lifecycle`) 기록.
4. `/p/{slug}`가 즉시 공개된다. 재빌드·배포 대기 시간은 없다.

### 12.3 게시 중단

`status = unpublished`로 전이한다. `/p/{slug}`는 404를 반환한다. 이미 접수된 신청 데이터는 삭제하지 않는다. 게시 중단된 상품의 신청 폼은 표시되지 않는다.

### 12.4 상품 삭제

**API**: `DELETE /api/products/{id}` (인증 필요)

삭제가 없으면 실패한 상품과 그 이미지가 영구히 쌓이고, 개인정보 보유 기간(§13.1)이 지난 신청 데이터를 지울 수단도 없다.

| 항목 | 규정 |
|---|---|
| 허용 상태 | `input_error` · `brochure_ready` · `draft` · `reviewing` · `unpublished` |
| **금지 상태** | `published` — 먼저 [게시 중단]을 해야 한다. 공개 중인 URL이 갑자기 사라지는 것을 막는다 |
| **금지 조건** | 해당 상품에 `applications` 행이 **1건 이상 있으면 삭제할 수 없다.** §12.3이 신청 데이터 보존을 규정하므로 상품만 지워 고아 신청을 만들지 않는다 |
| `generating` | 삭제 가능하다. 진행 주체가 사라진 상품을 정리하는 것이 이 기능의 주 용도다(§15.1.1) |
| 확인 | 확인 모달에서 행사명을 표시하고 명시적 확인을 받는다 |
| 미충족 시 | **409 `{reason: "precondition"}`** + 사유(게시 중 / 신청 존재) |

**삭제 범위** — 단일 트랜잭션으로 처리한다(§16.1.1).

| 대상 | 처리 |
|---|---|
| `products` 행 | 삭제 |
| `product_images` · `edit_history` | FK CASCADE로 함께 삭제 |
| **`execution_logs` · `abnormality_flags`** | **삭제하지 않는다.** `execution_id`로 남겨 사후 추적을 유지한다. `product_id`는 `NULL`로 설정한다(두 표 모두 nullable, §5.4·§5.5) |
| Storage 파일 | 트랜잭션 커밋 후 `{product_id}/` 접두사로 일괄 삭제. 실패해도 요청은 성공으로 확정하고 고아 파일로 남긴다 |

**신청 데이터가 있는 상품을 지우려면**: 먼저 관리 화면에서 해당 신청 내역을 삭제한다. 개인정보 보유 기간 경과분 처리도 이 경로를 쓴다.

| API | 동작 |
|---|---|
| `DELETE /api/applications/{id}` (인증 필요) | 신청 1건 삭제. `application_deleted` 로그(`category = application`) 기록 |

> 신청 삭제를 상품 삭제와 분리한 이유: 신청 데이터는 상품보다 오래 보존해야 하고(분쟁 대응), 지우는 판단 기준도 다르다(보유 기간 경과). 한 버튼으로 묶으면 상품을 정리하다 신청 이력을 함께 날린다.

---

## 13. Step 6 — 신청 및 자동 이메일

**경로**: `/p/{slug}` 내 `apply` 섹션 / **API**: `POST /api/applications` (인증 불필요)

### 13.1 신청 폼

| 필드 | 형태 | validation |
|---|---|---|
| 이름 | 텍스트 | 2~30자 |
| 이메일 | 이메일 | RFC 형식 검사 |
| 연락처 | 텍스트 | 숫자·하이픈 9~15자 |
| 인원수 | 숫자 | 1~20 정수 |
| 개인정보 수집·이용 동의 | 체크박스 | **필수**. 미체크 시 제출 버튼 비활성 |

이 5개 필드 구성은 **고정 계약**이며 편집기에서 변경할 수 없다(§10.2).

동의 영역에는 아래 3개를 표시한다.

| 항목 | 표시 내용 |
|---|---|
| 수집 항목 | 이름 · 이메일 · 연락처 · 인원수 |
| 수집 목적 | 여행 상품 신청 접수 및 안내 |
| 보유 기간 | **신청 접수일로부터 1년** |

보유 기간이 지난 신청은 `DELETE /api/applications/{id}`(§12.4)로 삭제한다. 자동 삭제 배치는 두지 않으며(§18.2의 Cron 제외), 관리 화면에서 접수일 기준으로 정렬해 수동 처리한다.

### 13.2 처리 순서

```text
POST /api/applications
  1. 서버 재검증 (필드 + 동의 여부)          위반 → 400
  2. product.status = published 확인          아니면 → 409
  3. product_snapshot 구성 (행사명·여행기간·숙소·가격·상품 URL)
  4. applications INSERT (email_status='pending', consent_at=now())
  5. execution_logs: application_received 기록   ← 메일보다 먼저 기록한다
  6. 사용자에게 완료 응답 ✅  (이메일 발송을 기다리지 않는다)
  7. after() 안에서 Resend 발송              ← 응답 후 실행을 보장하는 방법
       성공 → email_status='sent',   로그 email_sent(통과)
       실패 → email_status='failed', email_error 기록, 로그 email_sent(반려)
```

> **5번이 메일보다 앞인 이유**: 로그를 마지막에 쓰면 메일 단계에서 예외·타임아웃이 났을 때 신청 접수 기록 자체가 남지 않는다.
> **7번을 `after()`로 감싸는 이유**: 서버리스 함수는 응답을 반환하면 실행 컨텍스트가 종료될 수 있다. `after()`는 실행 순서만 보장하고 `maxDuration`을 늘려주지 않지만(§4.2), 이메일 발송은 AI 호출이 없어 60초 예산 안에서 안전하다.

### 13.3 이메일

| 항목 | 규정 |
|---|---|
| 발송 수단 | Resend |
| 수신자 | 신청자 이메일 |
| 본문 데이터 출처 | **`product_snapshot`(신청 시점 스냅샷)**. 현재 상품 값을 다시 읽지 않는다 |
| 본문 구성 | 신청자명, 상품명, 여행지, 여행기간, 숙소, 성인·아동 가격, 신청 인원수, 상품 페이지 URL, 문의 안내 |
| 문의 안내 출처 | 환경 변수 `CONTACT_INFO`(§4). 폼 입력 항목이 아니며 상품마다 달라지지 않는다 |
| 상품 URL 생성 | `{SITE_URL}/p/{slug}` — 절대 URL이어야 메일에서 열린다 |
| 금지 | **총액 계산 금지**. 인원수 × 가격을 계산해 표기하지 않는다 |
| URL 안내 문구 | "상품이 마감·중단된 경우 링크가 열리지 않을 수 있습니다. 문의처로 연락해 주세요." |
| 발신 도메인 | 자체 도메인 인증 전에는 Resend 기본 발신 주소를 사용한다 |
| 실패 처리 | 신청 자체는 성공으로 확정한다. 관리 페이지에서 **[재발송]**(`email_resent` 로그) |

**스냅샷을 쓰는 이유**: 게시 후 가격이 수정되면, 스냅샷 없이는 이미 발송한 메일과 현재 페이지 값이 불일치하고 분쟁의 근거가 된다.

### 13.4 범위 제외

정원·재고 관리, 초과 신청 차단, 중복 신청 방지, 결제는 포함하지 않는다. 인원수는 입력값을 그대로 저장한다.

---

## 14. 관리 페이지 및 인증

### 14.1 라우트

| 경로 | 인증 | 내용 |
|---|---|---|
| `/admin/login` | 불필요 | 비밀번호 입력 |
| `/admin` | 필요 | 상품 목록. 상태 필터: **전체 / 처리중 / 입력오류 / 소개서 완료 / 임시저장 / 검토중 / 게시됨 / 게시중단** (7개 상태 전부). 각 행에 배지 2종(§10.4)과 상태별 버튼(§15.1) |
| `/new` | 필요 | Step 1 입력 폼 (신규 작성) |
| `/new?product_id={id}` | 필요 | **`input_error` 재제출용.** `form_input` 값을 채운 상태로 열고 `failure_reason`을 상단에 표시한다. §15.1의 「폼 화면 (값 유지)」가 이 경로다 |
| `/admin/products/{id}` | 필요 | 상품 상세 — **소개서 8개 섹션 읽기 전용 + 검증 결과(실패 항목 전체)**, 로그 요약, 신청 내역 요약, 상태별 버튼. §15.1의 「소개서 검토」·「상세」가 이 경로다 |
| `/admin/products/{id}/edit` | 필요 | Step 4 편집기 + 미리보기 |
| `/admin/applications` | 필요 | 전체 신청 내역, 이메일 발송 상태, 재발송 |
| `/admin/logs/{execution_id}` | 필요 | 실행 로그 단일 뷰 + 이상 플래그 |
| `/p/{slug}` | 불필요 | 공개 상품 페이지 (`published`만 200) |

**§15.1의 「도달 화면」 → 경로 대응**

| §15.1의 도달 화면 | 경로 |
|---|---|
| 진행 표시 | `/admin/products/{id}` (진행 중 표시 + §15.1.1 복구 버튼) |
| 폼 화면 (값 유지) + 사유 | `/new?product_id={id}` |
| 소개서 검토 (+ 실패 항목) | `/admin/products/{id}` |
| 편집기 (+ 실패 항목) | `/admin/products/{id}/edit` |
| 상세 | `/admin/products/{id}` |

**소개서 생성 중 진행 표시**는 폼 화면(`/new`)에서 인라인으로 보여준다. ①이 `product_id`를 반환한 뒤에도 화면을 이동하지 않고, ④가 끝나면 `/admin/products/{id}`로 이동한다. 중간에 이탈하면 §15.1.1의 복구 경로를 쓴다.

### 14.2 인증 방식

| 항목 | 규정 |
|---|---|
| 방식 | 단일 공유 비밀번호(환경변수 `ADMIN_PASSWORD`) |
| 세션 | HMAC 서명된 `httpOnly` + `Secure` + `SameSite=Lax` 쿠키, 유효기간 7일 |
| 보호 대상 | `/admin/*`, `/new/*`, 그리고 `POST /api/applications`를 **제외한** 모든 API — Next.js middleware에서 차단 |
| 비교 | 타이밍 공격 방지를 위해 상수 시간 비교 |
| 시도 제한 | IP당 분당 5회 |
| 범위 제외 | 다중 계정, 역할 분리, 기획자별 상품 소유권 |

### 14.3 실행 로그 뷰

- 로그는 `execution_logs`에 단계당 1행씩 append한다. 재시도는 `retry_index`, 재생성·재제출은 `attempt_no`로 구분한다.
- `/admin/logs/{execution_id}` **단일 화면**에서 해당 실행의 전체 이력과 이상 플래그를 모두 확인할 수 있어야 한다. 단계별로 화면이 나뉘지 않는다.
- 화면은 `category` 탭으로 구분한다: **파이프라인**(기본) / 상태변경 / 신청·메일. 파이프라인 탭에는 `category = pipeline` 행만 표시한다.
- 표 컬럼 순서: `타임스탬프 → 시도 → 재시도 → 단계명 → 판정 → 상태 → 입력 → 출력`. 입력·출력은 가공·요약 없이 원본 JSON을 표시한다.
- 타임스탬프는 UTC ISO 8601, 빈 값은 `-`로 표기한다.
- **판정 열은 저장값(`pass`/`fail`/`-`)을 `통과`/`반려`/`-`로 바꿔 표시한다**(§5.4). 표시 문자열을 DB에 저장하지 않는다.
- `category = application` 행의 개인정보는 저장 시점에 이미 마스킹되어 있다(§5.4). 화면에서 복원하지 않는다.
- 이상 플래그는 같은 화면 하단에 감지된 것만 나열한다.

### 14.4 API 엔드포인트

| # | 메서드 | 경로 | 인증 | AI | 동작 | 응답 |
|---:|---|---|---|---:|---|---|
| 1 | POST | `/api/products` | 필요 | 0 | 폼 검증 + 상품 등록 + 이미지 업로드 | 200 `{product_id}` / 400 |
| 2 | POST | `/api/products/{id}/decompose` | 필요 | 1 | 정규화 + 일차 분해 + 0차 검증 | 200 / 409(재시도) / 422(입력 오류) |
| 3 | POST | `/api/products/{id}/brochure` | 필요 | 1 | 소개서 생성 | 200 / 409(생성 실패) |
| 4 | POST | `/api/products/{id}/validate-brochure` | 필요 | 1 | 1차 검증 | 200 / 409(재시도 필요) |
| 5 | POST | `/api/products/{id}/page` | 필요 | 1 | 페이지 생성 + slug 발급 | 200 / 409(생성 실패) |
| 6 | POST | `/api/products/{id}/validate-page` | 필요 | 1 | 2차 검증 | 200 / 409 |
| 7 | POST | `/api/products/{id}/validate-consistency` | 필요 | 1 | 3차 검증 + `draft` 등록 | 200 / 409 |
| 8 | POST | `/api/products/{id}/regenerate` | 필요 | 0 | `attempt_no` +1, 카운터 초기화, 재생성 시작점 지정 | 200 |
| 9 | GET | `/api/products/{id}` | 필요 | 0 | 상태·단계·검증 결과 조회(새로고침 복귀용) | 200 |
| 10 | PATCH | `/api/products/{id}/content` | 필요 | 0 | 편집 저장 | 200 / 400 |
| 11 | PATCH | `/api/products/{id}/slug` | 필요 | 0 | slug 변경 (`draft`/`reviewing`만) | 200 / 409 |
| 12 | POST | `/api/products/{id}/publish` | 필요 | 0 | 게시 (게시 게이트 확인) | 200 / 403 |
| 13 | POST | `/api/products/{id}/unpublish` | 필요 | 0 | 게시 중단 | 200 |
| 14 | POST | `/api/applications` | **불필요** | 0 | 신청 접수 + 메일 발송 | 200 / 400 / 409 |
| 15 | GET | `/api/applications` | 필요 | 0 | 신청 내역 조회 | 200 |
| 16 | POST | `/api/applications/{id}/resend` | 필요 | 0 | 이메일 재발송 | 200 |
| 17 | PATCH | `/api/products/{id}/form-input` | 필요 | 0 | **입력 수정 후 재제출**(§15.2). `form_input` 교체 + `attempt_no` +1 + 카운터 0 + `status = generating` | 200 / 400 / 409 |
| 18 | DELETE | `/api/products/{id}` | 필요 | 0 | 상품 삭제(§12.4) | 200 / 409 |
| 19 | DELETE | `/api/applications/{id}` | 필요 | 0 | 신청 1건 삭제(§12.4) | 200 |
| 20 | POST | `/api/plan-draft` | 필요 | 1 | **자연어 입력 → 폼 초안**(§7.5). 상품 행을 만들지 않고 DB에 쓰지 않는다 | 200 `{draft, origin, notes}` / 400 / 409(AI 실패) |

**AI 열의 값은 전부 0 또는 1이다** — §4.2 원칙의 준수 여부를 이 표에서 바로 확인할 수 있다.

**#20은 `{id}`를 받지 않는 유일한 쓰기 계열 라우트다.** 상품이 아직 없는 시점에 호출되므로 `products` 행·`execution_id`·조건부 갱신(§16.1.1)이 성립하지 않는다. 따라서 재시도 카운터도 없다 — 실패하면 클라이언트가 같은 요청을 다시 보내는 것이 전부다(§4.2 원칙 2).

#### #17 상세 — `form_input` 교체의 유일한 경로

| 규정 | 내용 |
|---|---|
| 불변 범위 | `form_input`은 **같은 `attempt_no` 안에서만** 불변이다. `attempt_no`가 올라갈 때만 교체할 수 있고, **그 경로는 #17뿐이다** |
| 허용 상태 | `input_error` · **`brochure_ready`**. 다른 상태에서 호출하면 409 `precondition` |
| 검증 | §7.1·§7.2 규칙으로 재검증. 위반 시 400, 행은 그대로 둔다 |
| 부수 효과 | `confirmed_data`·`brochure_content`·`page_content`를 비우고 4개 축 폐기, `current_step = pipeline_started`, `failure_reason` 초기화 |
| 보존 | `execution_id` · `slug`(있으면) · 업로드 이미지 |
| 이미지 | #17이 함께 받으며, 교체된 슬롯만 `product_images`를 갱신한다 |

**`brochure_ready`를 허용 상태에 넣은 근거**: §15.1이 그 상태의 제공 버튼으로 **[입력 수정]** 을 규정하고, §8.3이 「재시도가 소진되면 `input_error`가 아니라 `brochure_ready`로 두고 검토 화면에서 [다시 생성]·[입력 수정]을 제공한다」고 규정한다. 즉 **소개서를 보고 입력이 틀린 것을 발견하는 것이 검토 화면의 목적**이고 그때 유일한 길이 이 라우트다. 허용 상태를 `input_error`로만 두면 §15.1의 버튼이 갈 곳이 없다.

**불변 규칙을 깨지 않는다**: #17은 `attempt_no`를 올리고 축 4개를 폐기하므로 「같은 `attempt_no` 안에서 불변」(§5.1)이 그대로 성립한다. 소개서·페이지는 §14.4의 부수 효과로 비워지므로 낡은 산출물이 남지도 않는다.

### 14.5 라우트별 시작 조건 (재료 기준)

**시작 조건은 「지금 어느 상태인가」가 아니라 「필요한 재료가 있는가」로 판정한다.** 이것이 이 절의 유일한 원칙이다.

> **조건을 `current_step`의 특정 값으로 걸면 안 된다.** 그 값은 재시도·재생성 시점에 이미 다음 단계로 넘어가 있어, §11.6·§15.3의 재시도·재생성 경로가 첫 재호출에서 전부 거부된다. 재료(산출물 존재 여부, 선행 축의 판정값)는 재호출해도 그대로이므로 이 문제가 생기지 않는다.

| # | 라우트 | 시작 조건 (전부 충족해야 한다) | 미충족 시 |
|---:|---|---|---|
| 1 | `/api/products` | (없음) | — |
| 2 | `/decompose` | `status = generating` · `form_input` 존재 | 409 `precondition` |
| 3 | `/brochure` | `status = generating` · `confirmed_data` 존재 · `axis_0 = pass` | 409 `precondition` |
| 4 | `/validate-brochure` | `status = generating` · `brochure_content` 존재 | 409 `precondition` |
| 5 | `/page` | `status ∈ {brochure_ready, generating}` · `brochure_content` 존재 · `axis_1 = pass` | 409 `precondition` |
| 6 | `/validate-page` | `status = generating` · `page_content` 존재 | 409 `precondition` |
| 7 | `/validate-consistency` | `status = generating` · `page_content` 존재 · `axis_2 = pass` | 409 `precondition` |
| 8 | `/regenerate` | `status ∈ {brochure_ready, draft, generating}` | 409 `precondition` |
| 9 | `GET /api/products/{id}` | (없음) | — |
| 10 | `/content` (편집) | `page_content` 존재 · `status ∈ {draft, reviewing, published, unpublished}` | 409 `precondition` |
| 11 | `/slug` | `status ∈ {draft, reviewing}` | 409 `precondition` |
| 12 | `/publish` | 게시 게이트 통과(§11.5) | 403 |
| 13 | `/unpublish` | `status = published` | 409 `precondition` |
| 14 | `/applications` | 대상 상품 `status = published` | 409 `product_not_published` |
| 15 | `GET /api/applications` | (없음) | — |
| 16 | `/applications/{id}/resend` | 신청 행 존재 · `product_snapshot` 존재 | 404 / 409 `precondition` |
| 17 | `/form-input` | `status ∈ {input_error, brochure_ready}` | 409 `precondition` |
| 18 | `DELETE /api/products/{id}` | `status ≠ published` · 해당 상품의 `applications` 0건 | 409 `precondition` |
| 19 | `DELETE /api/applications/{id}` | (없음) | — |

**`status = generating`은 재시도를 막지 않는다.** 생성·검증 계열(2~7)은 모두 `generating` 안에서 실행되고, 검증 실패로 재호출할 때도 상태가 그대로 유지되기 때문이다(§15.2). 이 조건은 이미 `draft`·`published`가 된 상품에 생성 라우트를 다시 호출하는 것만 막는다.

**5번이 두 상태를 허용하는 이유**: [상품 생성] 버튼을 누른 시점의 상태는 `brochure_ready`이고, `brochure_ready → generating` 전이는 이 라우트가 수행한다(§15.2). 2·3차 실패로 재호출할 때는 이미 `generating`이므로 두 값을 모두 허용해야 한다.

**상태값을 조건에 쓰는 라우트는 4개뿐이다**(8·11·13·14). 이 4개는 사람의 조작 지점이라 자동 재시도 루프에 들어가지 않는다.

### 14.6 응답 코드 규약

| 코드 | 의미 | 응답 본문 | 클라이언트 동작 |
|---:|---|---|---|
| 200 | 단계 완료 (검증 `fail` 확정 포함) | `{current_step, axes, items?}` | 다음 단계 호출 또는 화면 전환 |
| 400 | 입력 규칙 위반 | `{field_errors}` | 폼 화면에 필드별 오류 표시 |
| 403 | 게시 게이트 미통과 | `{reason}` | 게시 버튼 비활성 유지 |
| 409 | 아래 `reason`으로 갈린다 | `{reason, retry_from?, items?}` | `reason`에 따라 다르다 |
| 422 | 입력 문제로 중단(`input_error`) | `{failure_reason}` | 폼 화면으로 이동, 사유 표시 |

`202`는 사용하지 않는다(§4.2).

#### `409`는 `reason`을 반드시 반환한다

하나의 코드가 네 가지 상황을 겸하므로, `reason`이 없으면 클라이언트가 전부 "재호출하라"로 해석해 **같은 요청을 무한 반복**한다.

| `reason` | 상황 | 클라이언트 동작 |
|---|---|---|
| `retry` | 검증 실패 + 재시도 여력 있음 | `retry_from`이 지정한 라우트부터 재호출한다 |
| `precondition` | 시작 조건 미충족(§14.5) | **재호출하지 않는다.** `GET /api/products/{id}`로 현재 상태를 다시 읽고 화면을 §15.1 표에 맞춰 갱신한다 |
| `slug_conflict` | slug 중복(§12.1) | 다른 slug로 재요청한다 |
| `product_not_published` | 신청 대상 상품이 `published`가 아님 | 신청 폼을 닫고 안내를 표시한다 |

`retry_from`은 §14.4 표의 라우트 번호를 쓴다.

| 실패 지점 | 응답 |
|---|---|
| 0차 검증(§8.2) | `{reason: "retry", retry_from: 2}` |
| **소개서 생성**(§8.3) | `{reason: "retry", retry_from: 3}` |
| 1차 검증(§8.4) | `{reason: "retry", retry_from: 3}` |
| **페이지 생성**(§9.5 ①) | `{reason: "retry", retry_from: 5}` |
| 2차 검증(§9.5 ②) | `{reason: "retry", retry_from: 5}` |
| 3차 검증(§9.5 ③) | `{reason: "retry", retry_from: 5}` |

`409 {reason: "retry"}` 본문에는 `items`(§11.3)를 함께 담아, 클라이언트가 재호출 중에도 진행 화면에 실패 사유를 표시할 수 있게 한다.

`retry_from`은 §11.6의 복귀 대상 표와 항상 일치해야 한다.

---

## 15. 상태

### 15.1 상태 × 화면 × 버튼 (단일 기준표)

**이 표가 상태 관련 규정의 단일 출처다.** 다른 절에서 화면·버튼을 언급할 때는 이 표와 일치해야 한다.

> 상태가 7개인데 상태별 도달 화면·버튼을 모아둔 표가 없으면 각 절을 따로 쓸 때마다 어긋나고, **도달할 수 없는 상태를 전제한 기능**이 생긴다. 이 표를 두는 목적이 그것이다.

| `status` | `verdict` | 표시 이름 | 도달 화면 | 제공 버튼 | `/p/{slug}` |
|---|---|---|---|---|---|
| `generating` | — | 처리중 | 진행 표시 | [이어서 진행] · [처음부터 다시] | 404 |
| `input_error` | — | 입력오류 | 폼 화면 (값 유지) + 사유 | [입력 수정 후 재제출] | 404 |
| `brochure_ready` | `pass` | 소개서 완료 | 소개서 검토 | [상품 생성] · [다시 생성] · [입력 수정] | 404 |
| `brochure_ready` | `fail` | 소개서 완료 | 소개서 검토 + 실패 항목 | [다시 생성] · [입력 수정]<br>**[상품 생성] 잠김** | 404 |
| `draft` | `pass` | 임시저장 | 편집기 / 상세 | [편집] · [게시] · [다시 생성] | 404 |
| `draft` | `fail` | 임시저장 | 편집기 + 실패 항목 | [편집] · [다시 생성] · [책임 게시] | 404 |
| `reviewing` | `pass` | 검토중 | 편집기 | [편집] · [게시] | 404 |
| `reviewing` | `fail` | 검토중 | 편집기 + 실패 항목 | [편집] · [책임 게시] | 404 |
| `published` | 무관 | 게시됨 | 편집기 (게시 중 경고) | [편집] · [게시 중단] | **200** |
| `unpublished` | 무관 | 게시중단 | 편집기 | [편집] · [게시] | 404 |

**규칙**

- `reviewing` · `published` · `unpublished`에는 **[다시 생성]을 제공하지 않는다.** 사람이 편집한 내용이 사라지기 때문이다.
- `brochure_ready` + `fail`에서 [상품 생성]을 잠그는 이유: 소개서가 입력과 어긋난 상태로 페이지를 만들면 오류가 증폭된다. 대신 [다시 생성]·[입력 수정]이 있어 막힌 길이 아니다.
- `input_error`는 **입력 문제 전용**이다. AI가 재시도를 소진한 경우는 `input_error`가 아니라 생성물을 남긴 채 `verdict = fail`이 된다.
- `generating`은 **버림받을 수 있는 유일한 상태**다. 재시도 주체가 클라이언트이므로(§4.2) 브라우저를 닫으면 진행 주체가 사라진다. 그래서 이 상태에만 복구 버튼 2개를 둔다 — §15.1.1 참조.

### 15.1.1 `generating` 복구

클라이언트가 순차 호출과 재시도를 담당하므로(§4.2), 탭을 닫으면 그 상품은 아무도 진행시키지 않는 `generating`에 남는다. 그래서 이 상태에만 복구 버튼 2개를 둔다. 둘 다 새 라우트를 만들지 않고 기존 것을 재사용한다.

| 버튼 | 동작 | 쓸 때 |
|---|---|---|
| **[이어서 진행]** | `current_step`이 가리키는 라우트부터 순차 호출을 재개한다(아래 표) | 기본 |
| **[처음부터 다시]** | `POST /api/products/{id}/regenerate`(§15.3) — `attempt_no` +1, 카운터 초기화 후 시작점부터 | [이어서 진행]이 `409 precondition`을 받을 때. 산출물이 저장되기 전에 요청이 끊긴 경우다 |

#### `current_step` → 다음 호출 대상

**이 표가 클라이언트 재개 규칙의 단일 출처다.** `GET /api/products/{id}`(§14.4 #9)가 반환하는 `current_step`으로 판단한다.

| `current_step` | 다음 호출 | §14.4 # |
|---|---|---:|
| `pipeline_started` | `/decompose` | 2 |
| `normalization_validated` | `/brochure` | 3 |
| `brochure_generated` | `/validate-brochure` | 4 |
| `validation_1_completed` | `/page` | 5 |
| `page_generated` | `/validate-page` | 6 |
| `validation_2_completed` | `/validate-consistency` | 7 |

`validation_3_completed`·`draft_registered`는 `generating`에서 나타날 수 없다. 그 단계가 끝나면 `draft`로 전이하기 때문이다.

[다시 생성](`brochure_ready`·`draft`의 재생성)과 [처음부터 다시](`generating`의 복구)는 같은 라우트를 쓰며, 그래서 §14.5 #8의 시작 조건에 `generating`이 포함된다.

### 15.2 상태 전이

| 현재 | 조건 | 다음 | 처리 |
|---|---|---|---|
| — | 폼 제출, 서버 validation 통과(§8.1) | `generating` | 행 생성, `attempt_no = 1`, 카운터 0 |
| `generating` | 일차 분해 실패(초과·구분 불가) | `input_error` | `failure_reason`, `pipeline_aborted` 플래그 |
| `generating` | **0차 검증 재시도 소진** | `input_error` | `failure_reason`, `pipeline_aborted` 플래그. 정규화·분해가 3회 실패하면 입력에 처리 불가 요소가 있다는 뜻이다 |
| `generating` | 소개서 생성 + 1차 검증 통과 | `brochure_ready` | `axis_1 = pass` |
| `generating` | **1차 검증 재시도 소진** | `brochure_ready` | `axis_1 = fail`, `pipeline_aborted` 플래그 |
| `brochure_ready` | [상품 생성] (`axis_1 = pass`일 때만) | `generating` | `page`·`consistency` 카운터 0, 페이지 생성 시작(§11.6) |
| `generating` | **페이지 생성 재시도 소진**(§9.5 ①) | `brochure_ready` | `axis_2 = fail`, `pipeline_aborted` 플래그. `page_content`가 없어 `draft`로 가지 않는다 |
| `generating` | 페이지 생성 + 2·3차 통과 | `draft` | `verdict = pass` 확정, `draft_registered` |
| `generating` | **2·3차 재시도 소진** | `draft` | 해당 축 `fail`, `pipeline_aborted` 플래그 |
| `draft` | 편집기에서 최초 저장 | `reviewing` | `human_edited = true`, 편집 이력 |
| `draft` / `reviewing` / `unpublished` | 게시 게이트 통과 + [게시] | `published` | `published_at` 기록(최초만) |
| `published` | [게시 중단] | `unpublished` | 공개 URL 404, 신청 폼 미표시 |
| `published` | 편집기 저장 | `published` | 게시 유지, 즉시 반영, 편집 이력 |
| `input_error` | [입력 수정 후 재제출] (§14.4 #17) | `generating` | `form_input` 교체, `attempt_no` +1, 카운터 4종 0, 축 4개 폐기, §8.2부터 재실행 |
| `brochure_ready` | **[입력 수정]** (§15.1 · 같은 라우트 #17) | `generating` | 위와 동일. 소개서를 보고 입력이 틀린 것을 발견하는 경로다 |
| `brochure_ready` / `draft` / `generating` | **[다시 생성]** · **[처음부터 다시]** | `generating` | §15.3 |
| `input_error` / `brochure_ready` / `draft` / `reviewing` / `unpublished` / `generating` | **삭제** (§12.4) | (행 삭제) | `published`에서는 불가. 신청 1건 이상이면 불가 |

> `published` 상태에서의 편집은 즉시 공개 페이지에 반영된다. 게시본/작업본 분리(스테이징)는 도입하지 않으며, 편집기 상단에 "이 상품은 현재 게시 중입니다 — 저장 시 즉시 반영됩니다" 경고를 표시한다.

### 15.3 [다시 생성] — `POST /api/products/{id}/regenerate`

입력은 그대로 두고 AI 작업만 다시 실행한다.

| 항목 | 규정 |
|---|---|
| 제공 상태 | `brochure_ready`, `draft` (§15.1) |
| 동작 | `attempt_no` +1, `retry_counts` 전부 0, `status = generating`, `current_step` 되돌림(아래 표), 로그 `regenerate_requested` |
| 재실행 시작점 | `brochure_ready`에서는 §8.3 ③부터, `draft`에서는 §9.5 ①부터 |
| 보존 | `form_input`, `confirmed_data`, 업로드 이미지, **시작점 이전 축의 검증 결과** |
| 폐기 | 시작점 **이후**의 산출물과 시작점 **이후** 축의 검증 결과(로그에는 남는다) |
| 제한 | `human_edited = true`인 경우 확인 모달에 "편집한 내용이 사라집니다"를 표시하고 명시적 확인을 받는다 |

#### 되돌림 범위 (시작점별)

`validation_snapshot`을 통째로 비우면 §14.5의 시작 조건(`axis_0 = pass` · `axis_1 = pass`)을 충족할 수 없어 재실행 자체가 거부된다. **시작점 이전 축은 보존한다.**

| 이전 상태 | 시작점 | `current_step`을 이 값으로 되돌린다 | 보존하는 축 | 폐기하는 축 | 폐기하는 산출물 |
|---|---|---|---|---|---|
| `brochure_ready` | §8.3 ③ (`/brochure`) | `normalization_validated` | `axis_0` | `axis_1`·`axis_2`·`axis_3` | `brochure_content` |
| `draft` | §9.5 ① (`/page`) | `validation_1_completed` | `axis_0`·`axis_1` | `axis_2`·`axis_3` | `page_content` |
| `generating` ([처음부터 다시], §15.1.1) | 아래 규칙 | 시작점의 직전 단계 | 시작점 이전 축 | 시작점 이후 축 | 시작점 이후 산출물 |

**`generating`의 시작점 결정** — 어디까지 진행됐는지 모르므로 검증 축의 통과 여부로 판단한다. §14.5와 같은 「재료 기준」이다.

| 조건 | 시작점 | `current_step` |
|---|---|---|
| `axis_1 = pass` | §9.5 ① (`/page`) | `validation_1_completed` |
| `axis_0 = pass` 이고 `axis_1 ≠ pass` | §8.3 ③ (`/brochure`) | `normalization_validated` |
| 그 외 | §8.2 ② (`/decompose`) | `pipeline_started` |

폐기한 축은 `null`로 두며 최상위 `verdict` 계산에서 제외한다(§11.3).

**`slug`는 어느 경우에도 폐기·재발급하지 않는다**(§12.1). `brochure_ready`에서는 아직 발급 전이므로 `NULL`이고, `draft`에서는 이미 발급된 값을 유지한다 — 주소를 바꾸면 이미 공유된 링크가 끊긴다.

`input_error` 재제출은 시작점이 §8.2 ②이므로 `current_step`을 `pipeline_started`로 되돌리고 4개 축 전부를 폐기한다.

---

## 16. 제약

### 16.1 데이터 무결성 (AI 생성 구간)

- 여행지·숙소·상점·공항·항공편 등 고유명사는 사용자가 입력한 명칭을 그대로 사용한다. 존재하지 않는 장소명을 만들거나 유사한 다른 지명·호텔명으로 바꿔 쓰지 않는다.
- 지명·시설명의 표기를 임의로 변경하지 않는다(예: `롯데호텔 제주` → `롯데 제주 호텔` 금지, 약칭·영문 변환 금지). **slug는 표시값이 아니라 URL 식별자이므로 이 규칙의 대상이 아니며, 자동 로마자 변환도 하지 않는다**(§12.1).
- **입력에 없는 경유지·관광지·부대시설·이동 수단을 일정 설명에 추가하지 않는다.** 일차별 서술은 `원문근거`에 등장하는 요소만 사용한다(§6.3).
- 일정 원문에서 일차 구분을 식별할 수 없을 때 임의로 배분하지 않는다.
- 정규화 규칙(§6.2) 3종 외에 입력값을 변형하지 않는다.
- 가격은 입력값을 그대로 사용하고 계산·할인·환산·합계하지 않는다.
- 지정된 여행기간·숙박일정을 초과·미달하는 일정을 구성하지 않는다.
- 선택 항목 미입력 시 `추후 추가 예정`으로 표기하고 섹션을 삭제하지 않는다.
- `form_input`에 출처가 없는 아라비아 숫자를 생성하지 않는다.
- 이미지 슬롯을 재배치하지 않는다.

### 16.1.1 동시성 · 원자성

단일 공유 비밀번호(§14.2)이므로 **여러 사람이 같은 상품을 동시에 조작할 수 있다.** 그 전제에서 규정한다.

**낙관적 잠금** — 모든 쓰기 라우트는 `updated_at`을 조건으로 갱신한다.

| 항목 | 규정 |
|---|---|
| 요청 | 쓰기 라우트는 클라이언트가 읽은 시점의 `updated_at`을 함께 보낸다 |
| 갱신 | `UPDATE ... WHERE id = ? AND updated_at = ?` — 영향 행이 0이면 그 사이 다른 요청이 갱신한 것이다 |
| 불일치 시 | **409 `{reason: "stale"}`**. 클라이언트는 `GET /api/products/{id}`로 다시 읽고 화면을 갱신한 뒤 사용자에게 알린다. 자동 재시도하지 않는다 |
| 적용 대상 | 상태·산출물을 바꾸는 전 라우트 (§14.4의 2~8·10~13·18) |
| 미적용 | `POST /api/products`(행 생성) · `POST /api/applications`(고객 신청, 상품을 바꾸지 않음) · 조회 라우트 |

**편집기 동시 편집**: 잠금을 걸지 않는다. 마지막 저장이 이기되, `updated_at` 불일치로 409를 받으면 **"다른 사람이 먼저 저장했습니다. 최신 내용을 불러옵니다"**를 표시하고 다시 읽는다. 저장 전 내용은 사용자가 복사할 수 있도록 화면에 남긴다.

**slug 발급 경쟁**: `slug`는 UNIQUE 제약이 있으므로 동시 발급 시 한쪽이 DB 오류를 받는다. 그때는 접미사를 올려 최대 5회 재시도하고, 모두 실패하면 **409 `{reason: "slug_conflict"}`**를 반환한다.

**원자성** — 다단계 쓰기는 단일 트랜잭션으로 처리한다.

| 라우트 | 한 트랜잭션에 묶는 것 | 트랜잭션 밖 |
|---|---|---|
| §8.1 상품 등록 | `products` 행 생성 + `product_images` 기록 | Storage 파일 업로드, 로그 기록 |
| §10.3 편집 저장 | `page_content` 갱신 + `human_edited` + 상태 전이 + `edit_history` | 로그 기록 |
| §12.2 게시 | `status` + `published_at` | 로그 기록 |
| §13.2 신청 | `applications` INSERT | 로그 기록, 이메일 발송 |

| 규칙 | 내용 |
|---|---|
| Storage 업로드 실패 | 트랜잭션을 커밋하지 않고 **400**을 반환한다. 이미 올라간 파일은 삭제를 시도하고, 실패해도 고아 파일로 남긴 뒤 요청은 실패로 확정한다(§16.2의 고아 파일 정리 대상) |
| **로그 기록 실패는 본 동작을 실패시키지 않는다** | 트랜잭션 밖에서 실행하며, 실패 시 서버 오류 로그에만 남긴다. 로그 실패로 게시나 신청을 되돌리면 사용자 피해가 더 크다 |
| 이메일 발송 실패 | §13.2 7항대로 신청은 성공으로 확정한다 |

### 16.2 시스템 제약

- **서버 라우트 1건은 AI를 최대 1회 호출한다**(§4.2). 이를 어기면 `maxDuration`을 초과할 수 있다.
- `published` 외 모든 상태의 상품 페이지는 공개 라우트에서 404다(§15.1).
- `form_input`과 `confirmed_data`는 **같은 `attempt_no` 안에서 불변**이다. `form_input`을 교체할 수 있는 경로는 §14.4 #17 하나뿐이며, 그 라우트는 `attempt_no`를 올린다.
- `validation_snapshot`은 편집으로 변경되지 않으며 `attempt_no`가 올라갈 때만 갱신된다.
- 게시된 상품의 slug는 변경할 수 없다.
- `applications`는 익명 사용자에게 `INSERT`만 허용한다.
- 개인정보 수집 동의 없이 신청을 접수하지 않는다.
- 이메일 발송 실패가 신청 접수를 실패시키지 않는다.
- 서버는 프론트엔드 validation을 신뢰하지 않고 동일 규칙으로 재검증한다.
- 신청 폼의 필드 구성과 테마는 편집기에서 변경할 수 없다.
- 이미지 업로드는 `/new` 폼에서만 가능하다.
- **알려진 한계**: Storage 버킷이 읽기 공개이므로 임시저장 상품의 **이미지 파일**은 정확한 URL을 아는 경우 접근 가능하다(§7.3). 페이지 콘텐츠·가격·일정은 노출되지 않는다.

### 16.3 보안 (RLS)

| 테이블 | anon | service role |
|---|---|---|
| `products` | `SELECT` (조건: `status = 'published'`) | 전체 |
| `product_images` | `SELECT` (게시된 상품의 것만) | 전체 |
| `applications` | **`INSERT`만** | 전체 |
| `execution_logs` | 전면 거부 | 전체 |
| `abnormality_flags` | 전면 거부 | 전체 |
| `edit_history` | 전면 거부 | 전체 |

Storage `product-images` 버킷은 읽기 공개, 업로드는 service role만 허용한다. service role 키는 서버 라우트에서만 사용하고 클라이언트에 노출하지 않는다.

---

## 17. 반응형 및 비기능 요건

### 17.1 반응형 보증 방식

서버리스 환경에서 헤드리스 브라우저 검사는 비현실적이므로 **보증 방식을 바꾼다.**

| 계층 | 방식 |
|---|---|
| 컴포넌트 사전 검증 | 어휘의 블록 컴포넌트(기존 9종 + 삽입 3종 + 추가 7종)와 공용 래퍼 `Band`를 개발 단계에서 375 / 768 / 1280px에 대해 검증하고 결과를 문서화한다. 시각적 다양성의 대부분을 `Band`(tone·width·pad·align·edge)가 처리하므로 검증 표면이 블록 수만큼 늘지 않는다 |
| 콘텐츠 길이 계약 | **2.8: 타입별.** `vocabulary.ts`의 `lengths`가 단일 출처이며 생성 시점(`checkPage`)과 저장 시점(`validateEdit`)이 같은 표를 읽어 상한이 갈릴 수 없다. **각 항목이 존재하는 단계에서** 초과를 차단한다 |
| 이미지 계약 | 고정 종횡비 컨테이너 + `object-fit: cover` + `max-width: 100%` |
| 표·긴 텍스트 | 가로 스크롤이 필요한 요소는 자체 `overflow-x: auto` 컨테이너 안에서만 스크롤한다. 페이지 본문은 가로 스크롤이 발생하지 않는다 |
| 편집기 확인 | 편집기 미리보기에서 375 / 768 / 1280 뷰포트를 전환해 기획자가 직접 확인한다 |

**콘텐츠 길이 계약**

**전체 6종이다.** 다만 `free_text`·`notice`는 편집기에서 사람이 끼워 넣는 블록이라 **생성 시점에는 존재하지 않는다.** 강제 시점을 열로 구분한다.

| 항목 | 상한 | 생성 시(§9.5 ①) | 편집 저장 시(§10.3) | 연동된 입력 상한 |
|---|---:|:---:|:---:|---|
| `hero.headline` | 40자 | ● | ● | 행사명 40자(§7.1) |
| `hero.subcopy` | 80자 | ● | ● | — |
| 일차별 서술 | 200자 | ● | ● | — |
| 섹션 제목 | 30자 | ● | ● | — |
| `free_text` 블록 | **500자** | — | ● | — |
| `notice` 블록 | 300자 | — | ● | — |

**생성 단계에서 강제하는 것은 4종, 편집 저장에서 강제하는 것은 6종이다.** 생성 시점에 없는 블록의 상한을 검사하도록 요구하면 실행 불가능한 규정이 된다.

> AI가 매 실행마다 반응형 검사를 "통과했다고 기록"하는 방식은 실제 검사 없는 형식적 기록이 되므로 채택하지 않는다.

### 17.2 기타

| 항목 | 기준 |
|---|---|
| 접근성 | 모든 이미지에 대체 텍스트, 폼 필드에 label, 본문 대비 4.5:1 이상 |
| 공개 페이지 로딩 | LCP 2.5초 이내. `next/image` 사용, `hero`는 `priority`, 나머지 전 이미지는 `loading="lazy"` |
| 에러 UX | 단계별 진행 표시(§8.5·§9.6), 실패 시 사유와 [다시 생성] 제공 |
| 다크 모드 | 범위 제외. 라이트 테마 단일 |

---

## 18. 범위

### 18.1 포함

- 웹 폼 입력(필수 폼 그룹 6 · 선택 4) 및 이미지 업로드
- 일정 원문의 일차 분해(원문 근거 추적 + 기계 1차 검사 포함)
- 확정 데이터표 구성 및 정규화
- 소개서(간단버전, 8섹션 · 입력된 전 항목 반영) 생성 및 검토 화면
- 상품 페이지 콘텐츠 모델 생성 + 고정 컴포넌트 렌더링
- **검증 4축**(`form_input` 기준) 및 결과 스냅샷
- 섹션 단위 편집기(수정·삭제·삽입·순서변경) + 미리보기
- [다시 생성] 및 시도 회차 추적
- 비밀번호 인증 관리 페이지(상품 목록·상세·신청 내역·실행 로그)
- **게시 및 상품별 고유 URL 실제 공개 배포**, 게시 중단, 책임 게시
- 신청 접수(Supabase) 및 자동 이메일 발송(Resend), 재발송
- 실행 로그 및 이상 플래그 기록

### 18.2 제외

| 제외 항목 | 비고 |
|---|---|
| 결제 | |
| 정원·재고 관리, 초과·중복 신청 차단 | |
| 가격 산정·계산·환산 | 1.0에서 유지 |
| 상품 기획 자체(입력 전 단계) | 1.0에서 유지 |
| 다중 계정·역할 분리·상품 소유권 | 단일 공유 비밀번호로 대체 |
| 게시본/작업본 분리(스테이징) | 게시 중 편집은 즉시 반영 |
| 이미지 AI 생성·보정, 편집기 내 새 업로드 | 업로드는 `/new`에서만 |
| `gallery` 슬롯(사진 모아 보여주기) | 2.3에서 삭제. 배치할 섹션이 없었다(§7.3). 추가 사진은 편집기 `image` 블록으로 |
| Storage signed URL 프록시 | §7.3의 한계를 감수 |
| 백그라운드 워커·큐·Cron | §4.2의 API 분할로 대체 |
| 상태 폴링 | 클라이언트 순차 호출로 대체 |
| 로마자 자동 변환 slug | §12.1 |
| 다국어, 고급 SEO, 다크 모드 | |
| 헤드리스 브라우저 반응형 자동 검사 | §17.1 방식으로 대체 |
| 이미지-텍스트 내용 정합성 검증 | 기획자 책임 |
| 편집 후 AI 재검증 | 기획자 책임(§10.4) |
| 일차 구분 없는 일정 원문의 자동 배분 | §6.3 — 창작 방지를 위해 실패 처리 |

---

## 19. 성공 기준

**판정 항목의 단일 출처는 `checklist.md`다.** 예/아니오로 판정 가능한 개별 항목 약 355개가 그 파일의 A~W · AA · AB · X~Z 섹션에 있으며, 판정 단위는 `products.id` 1건이다.

이 절은 **게이트 구조와 각 절이 어느 게이트에 대응하는지**만 정의한다. 개별 항목을 여기에도 나열하면 `checklist.md`와 중복된다.

| 게이트 | 대응 절 | `checklist.md` 섹션 |
|---|---|---|
| **G0. 실행 구조** | §4.2 · §4.3 · §14.4 · §14.5 · §14.6 | A |
| **G1. 입력·데이터 무결성** | §5 · §6 · §7 · §16.1 | B · C · D · E · F · W |
| **G2. 소개서 정합성** | §8 · §11.1(0·1차) | G · I-01~I-03 |
| **G3. 페이지 정합성** | §9 · §11.1(2·3차) · §11.2 · §11.3 | H · I-04~I-23 |
| **G4. 상태·편집·게시** | §10 · §12.1~12.3 · §15 · §17 | J · K · N · O · V |
| **G5. 신청·이메일·보안** | §13 · §16.3 | L · M · U |
| **G6. 기록·운영 완결성** | §5.4 · §5.5 · §11.6 · §12.4 · §14.1~14.3 · §16.1.1 | P · Q · R · S · T · **AA · AB** · X · Y |

앞 게이트가 실패하면 뒤 게이트는 판정하지 않는다. 게이트별 통과 조건과 실패 시 복귀 단계는 `checklist.md` §Z에 있다.

`AA`(동시성·원자성, §16.1.1)와 `AB`(삭제·정리, §12.4)는 2.4에서 신설된 섹션이다.

---

## 20. 시연 시나리오 (3분)

| 시간 | 동작 | 보여줄 것 |
|---|---|---|
| 0:00 | `/admin/login` 비밀번호 입력 | 기획자 전용 게이트 |
| 0:15 | `/new` 폼 입력(일정 원문 포함) + 이미지 3장 업로드 | 필수/선택 구분, 일정 자유 입력, 슬롯 지정 |
| 0:50 | [소개서 생성] | **4단계 진행 표시** → 일차 분해 결과 + 8섹션 소개서 + 검증 배지 |
| 1:15 | [상품 생성] | **3단계 진행 표시** → 임시저장 등록, 검증 4축 결과 |
| 1:35 | 편집기 → 문구 수정 + 섹션 1개 삭제 + slug 지정 | 섹션 편집, 375/768/1280 전환, 배지 2종 동시 표시 |
| 2:10 | [게시] | 확인 모달 → 공개 URL 즉시 활성 |
| 2:20 | 다른 브라우저(비로그인)로 `/p/{slug}` 접속 | 실제 배포 확인. draft URL은 404 |
| 2:40 | 신청 폼 제출 | 완료 응답 → 수신함에 이메일 도착 |
| 2:55 | `/admin/applications`, `/admin/logs/{id}` | 신청 내역, 파이프라인 로그(시도·재시도 구분) |

---

## 21. 잔여 결정 사항

| 항목 | 현재 가정 | 확정 필요 시점 |
|---|---|---|
| Resend 발신 도메인 | 미인증 상태로 기본 발신 주소 사용 | 시연 전 |
| `내용` 명사구 후보 추출 방식 | 2자 이상 한글 명사구 + 영문 고유명사 | 0차 검증 구현 시 |

**본문에서 확정한 항목** — 아래는 결정이 끝나 잔여 목록에서 내렸다.

| 항목 | 확정 내용 | 위치 |
|---|---|---|
| 테마 키 목록 | 6종 + `default` | **§9.4** |
| 일차 구분 표기 인식 범위 | 6종 (`n일`·`n일차`·`n일 차`·`첫째 날`·`Day n`·`DAY n`) | **§6.3** |
| `gallery` 슬롯 렌더 위치 | 슬롯 자체를 삭제 | **§7.3**·§18.2 |
| 개인정보 보유 기간 | 신청 접수일로부터 1년, 수동 삭제 | **§13.1**·§12.4 |
| AI 모델·파라미터 | `deepseek-v4-flash`, effort 생성 `medium`/검증 `low` (2026-08-12 실측 확정). **`pro` 계열 금지** · 예비 경로 `gemini-3.5-flash` | **§4.3** |
| 금액 단위 | `원` 고정 | **§6.2** |
| 여행기간 일수 계산 | 양끝 포함, `1 ≤ 일수 ≤ 15` | **§6.2.1** |
| 출처 없는 숫자 판정 | 허용 출처 4종 + 4단계 판정 | **§6.3.1** |

---

## 22. 개정 이력

개정 내역은 **[CHANGELOG.md](./CHANGELOG.md)** 로 분리했다. 1.0 · 2.0 · 2.1 · 2.2 대비 개정 내역 4개 절이 그 파일에 있다.

구현에 필요한 규정은 전부 이 문서 본문(§1~§21)에 있다. CHANGELOG는 **왜 그 규정이 됐는지**를 확인할 때만 참조한다.
