# CHANGELOG.md — zentrip spec 개정 이력

> `spec.md`의 부록 A~D를 분리한 문서다. 현재 규정은 `spec.md` 본문이 단일 출처이며, 이 파일은 **왜 그렇게 됐는지**만 기록한다.
> 구현 시에는 이 파일을 읽지 않아도 된다.

---

## 부록 E. 2.3 대비 개정 내역 (2.4)

**초점**: 2.3까지 제어 흐름은 정밀했으나 그 흐름이 다루는 **데이터의 모양과 AI 호출 계약이 비어 있었다.** 「이 문서만 보고 제3자가 구현할 수 있는가」를 기준으로 6단계에 걸쳐 채웠다.

| # | 2.3의 문제 | 2.4 처리 |
|---:|---|---|
| 1 | AI가 5개 작업을 수행하는데 모델·파라미터·출력 강제 방식·파싱 실패 처리가 전부 없었다. 타임아웃 25초만 숫자로 있고 초과 시 동작이 없었다 | **§4.3 AI 호출 계약 신설** — `claude-opus-5`, 금지 파라미터 4종, `output_config.format` 스키마 강제, 프롬프트 캐싱, 실패 6종 |
| 2 | 콘텐츠 모델의 `data` 키가 17개 섹션 중 5개만 예시로 존재했다. `source` 맵이 이 키를 참조하므로 검증이 성립하지 않았다 | **§8.7·§9.3 `data` 키 전수 확정**, §10.2 삽입 블록 3종 스키마, §11.3 `items` 6필드 |
| 3 | 생성 라우트 2개(`/brochure`·`/page`)에 실패 경로가 전무했다. §11.6은 검증 실패만 다뤘다 | §8.3·§9.5에 실패 처리 신설. §11.6 표에 생성 실패 2행 추가. **[상품 생성]의 카운터 초기화 규칙** 신설 |
| 4 | 「여행기간 일수」 계산식, 금액 단위 집합, 「출처 없는 숫자」 판정 방법이 없어 3개 검증 항목이 판정 불가였다 | **§6.2.1**(일수, 양끝 포함) · **§6.2**(단위 `원` 고정 + 규칙별 적용 대상 한정) · **§6.3.1**(허용 출처 4종 + 4단계 판정) |
| 5 | 동시성 규정이 한 줄도 없었다. 단일 공유 비밀번호이므로 동시 편집이 실제로 가능한데 낙관적 잠금도 트랜잭션 경계도 없었다 | **§16.1.1 신설** — `updated_at` 조건부 갱신, `409 stale`, 라우트별 트랜잭션 경계표 |
| 6 | `generating`에서 브라우저를 닫으면 재시도 주체가 사라져 **복구 경로 없이 갇혔다.** §15.1은 그 상태에 버튼을 주지 않았다 | **§15.1.1 신설** — [이어서 진행]·[처음부터 다시] + `current_step` → 다음 호출 대상 표 |
| 7 | **상품 삭제 기능이 존재하지 않았다.** 라우트에도 범위 제외 목록에도 없어 의도인지 누락인지 판별 불가. 실패한 상품·이미지가 영구 잔존 | **§12.4 신설** — 상품·신청 삭제 분리. `published`·신청 존재 시 차단, 로그 보존, Storage 정리 |
| 8 | 소개서 검토 화면의 경로가 없었고, 「`form_input` 불변」과 [입력 수정 후 재제출]이 정면 충돌했으며 재제출 라우트도 없었다 | §14.1에 화면↔경로 대응표. 라우트 #17~19 추가. 불변 범위를 **「같은 `attempt_no` 안에서」**로 좁혀 충돌 해소 |
| 9 | 부록 4개(156줄)와 §19 성공 기준(145줄)이 `checklist.md`와 중복이었다 | 부록을 이 파일로 분리, §19를 게이트↔절 대응표로 압축 |
| 10 | 환경 변수 목록이 없었다(`ADMIN_PASSWORD`만 언급) | §4에 8개 목록 추가 |

**압축**: 부록 이관·§19 축약·JSON 예시 중복 제거·이력형 근거 이관으로 약 300줄을 걷어내고, 그 자리에 위 규정을 채웠다.

### 하위 문서 동기화 (2.4)

`workflow.md`·`checklist.md`를 spec 2.4에 맞췄다. 두 문서는 2.2 기준이어서 위 10개 항목 전부가 반영되어 있지 않았다.

| 문서 | 처리 |
|---|---|
| `workflow.md` | **참조 구조로 재작성.** spec 표를 중복 서술하던 8개 항목(콘텐츠 스키마·시작 조건·응답 코드·엔드포인트 등)을 §0의 단일 출처 표로 대체했다. 시작 조건을 재료 기준으로 교체, `409 reason` 분기 추가, 카운터 3종 → 4종(0차는 `normalization`), Step 03·05에 생성 실패 경로 신설, 페이지 생성 소진을 `brochure_ready` 복귀로 수정, 삭제·재제출·`generating` 복구를 Step 11~13으로 추가, 조사 처리를 서술 필드로 한정, web-builder의 draft 등록 주장 삭제, §12 동시성 신설 |
| `checklist.md` | 265 → **355개 항목.** **AA(동시성·원자성) · AB(삭제·정리) 섹션 신설.** A 섹션에 AI 호출 계약 14개 항목 추가, `data` 키 표 대조(G-01b·H-01b), 카운터 4종(S-03·S-08), `verdict` 영어 저장(Q-09), 개인정보 마스킹(Q-10), `attempt_no` 중복 범위(R-06), 길이 계약 생성 4종 / 편집 6종 분리(H-11·J-09), `gallery` 삭제(F-02·Y-11), T-07 중복 ID 해소(→ T-09~T-11) |
| `spec.md` | §19의 「2.4 추가 대상」 표를 제거(checklist 반영 완료). §14.5에 라우트 #15·#16 추가로 19개 전부 커버. §15.1의 `§15.4` 오참조를 `§15.1.1`로 수정 |

**남은 대상**: `.claude/agents/` 5개와 `.claude/skills/` 15개는 아직 2.2 기준이다. 수정 항목은 `workflow.md` §15 「하위 문서 동기화 대상」 표에 있다.

---

## 부록 A. 1.0 대비 개정 내역

| 1.0 규정 | 2.x 처리 | 이유 |
|---|---|---|
| 범위(제외) "정식 게시" | **삭제** — 게시는 핵심 기능 | Step 5 신설 |
| 최종 상태값 `draft` 유지, 자동 정식 게시 0건 | `published` 추가. **사용자 명시 조작에 의한** 게시만 허용(자동 게시는 계속 0건) | Step 5 신설 |
| 입력 = 자유 텍스트 / 콤마 구분 프롬프트 | 웹 폼으로 대체. 일정만 자유 텍스트 유지 | Step 1 |
| 선택 항목 미입력 시 AI가 확인 질문 | 폼 UI가 대체. 미입력 → `추후 추가 예정` 자동 | Step 1 |
| 소개서 8개 섹션 필수 | **8개 섹션 유지.** '간단버전'은 서술 분량만 축소 | Step 2 |
| 검증 = 확정 데이터표 → 소개서 → 웹페이지 직렬 | **`form_input` 기준 4축**으로 재편 | §11.1 |
| 소개서 vs 웹페이지 전 항목 **문장** 문자열 일치 | 대응표 기준 `apply` 제외 전 섹션을 **사실정보 값 단위**로 대조 | §11.1 |
| 산출물 = `outputs/run-{id}/` 하위 4개 파일 | Supabase 테이블·jsonb·Storage로 이전 | 서버리스 파일시스템 쓰기 불가 |
| 로그 = 단일 `log.html` 파일 + 고정 HTML 마크업 | `execution_logs` 테이블 + `/admin/logs/{id}` 단일 화면 | 동일 |
| 소개서·웹페이지 = HTML 생성물 | **JSON 콘텐츠 모델 + 고정 컴포넌트 렌더링** | 섹션 편집기·반응형 보증 성립 조건 |
| 동일 단계 재시도 3회 | **2회**, 클라이언트 재호출 방식 | 실행 시간 예산 |
| 375/768/1280 렌더링 검사 통과 필수 | 컴포넌트 사전 검증 + 콘텐츠 길이 계약으로 대체 | 서버리스에서 헤드리스 검사 비현실적 |
| `숙박일정` 필수/선택 모순 | **선택**으로 확정 | §6.4 |
| 상태값 4종(처리중·임시저장·검토중·반려) | **7종** + §15.1 단일 기준표 | Step 2·5 신설 |
| 검증 통과 = 게시 시점까지 유효 | 검증 기준 시점을 고정, 편집분은 기획자 책임 | Step 4 신설 |
| 이미지 / 인증·권한 / 개인정보·RLS | 신규 정의(§7.3 / §14.2 / §13.1·§16.3) | 1.0에 개념 없음 |

---

## 부록 B. 2.0 대비 개정 내역

| # | 2.0의 문제 | 2.1 처리 | 심각도 |
|---:|---|---|---|
| 1 | **일정 입력 필드가 없어** AI가 일차별 서술을 창작해야 했고 §16.1을 필연적으로 위반했다 | 일정 원문 필수 필드 + 원문 근거 추적 규칙 신설 | 치명 |
| 2 | `hero.headline` 40자 vs 행사명 60자 | 행사명 상한 **40자** | 충돌 |
| 3 | 이미지 20장 vs 여행기간 30일 | 여행기간 **15일**, 이미지 슬롯 상한 정합 | 충돌 |
| 4 | 응답 후 메일 발송이 실행되지 않을 수 있었다 | `after()` 명시 | 충돌 |
| 5 | 신청 로그가 메일 발송 뒤에 있어 발송 실패 시 기록이 남지 않았다 | 로그를 INSERT 직후로 이동 | 충돌 |
| 6 | `verdict = fail`이 도달 불가능했다 | 2·3차 실패는 `draft` + `fail`, 책임 게시 신설 | 충돌 |
| 7 | 소개서만 만든 상품이 목록에서 사라졌다 | `brochure_ready` 신설, 필터 전 상태 확장 | 충돌 |
| 8 | "필수 필드가 **모두** 비어 있으면 차단" | "**어느 하나라도**" | 문구 오류 |
| 9 | 콤마 "처리"가 모호, 값 타입 규정 소실 | 콤마 **제거** 확정, 전 값 문자열 복원 | 중대 |
| 10 | 3차 검증 목적·실행 조건 미정 | 교차·회귀 목적 명시 + 2차 실패 시 생략 | 중대 |
| 11 | 신청 로그가 파이프라인 로그를 덮었다 | `category` 컬럼 + 탭 분리 | 중대 |
| 12 | 한글 slug의 percent-encoding | slug 영문·숫자·하이픈 고정 | 중대 |
| 13 | Storage 공개 vs "기획자만" 모순 | UUID 경로 + 한계 명시 | 중대 |
| 14 | `apply` 신청 폼이 편집 가능했다 | 안내 문구·제목만 편집 | 중대 |
| 15 | `가격.아동` 0원 표시 | "미운영" 체크 → `해당 없음` | 중대 |
| 16 | 404 대상 열거 누락 | "`published` 아닌 모든 상태"로 통일 | 중대 |
| 17 | 소개서 20초가 5섹션 기준 | 재산정 | 경미 |
| 18 | `data` 카운터가 사용자 재입력에 적용 | 카운터 3종, 폼 실패 미적용 | 경미 |
| 19 | API 표 부재 | §14.4 신설 | 경미 |
| 20 | 게시 중단 후 메일 URL 404 안내 없음 | 안내 문구 필수화 | 경미 |

---

## 부록 C. 2.1 대비 개정 내역

2.1을 6개 렌즈(내부 일관성 · 참조 정합성 · 기술적 실행 가능성 · 검증 실행 가능성 · checklist 정합성 · 완결성)로 검증해 찾은 18건을 반영했다.

| # | 2.1의 문제 | 2.2 처리 | 심각도 |
|---:|---|---|---|
| 1 | 소개서 생성이 동기 처리인데 AI 3회 × 25초 = **75초 > `maxDuration` 60초** | **§4.2 1요청 1AI호출 원칙** 신설. 소개서 4요청·페이지 3요청으로 분할(§8.5·§9.6) | 치명 |
| 2 | `202` + 폴링이 서버리스에서 성립하지 않음. `after()`는 시간 한도를 늘려주지 않는데 시간 해결책처럼 기술 | 폴링 폐기, 클라이언트 순차 호출. `after()`의 한계를 §4.2·§13.2에 정확히 기술 | 치명 |
| 3 | 3차 검증이 `원문근거` 대조를 지시하는데 그 필드가 두 콘텐츠 모델에 **존재하지 않음** | 대조 대상을 **일차 수 + 사실정보 값**으로 수정. `원문근거`는 0차의 몫. 아울러 사용자 결정에 따라 **기준값을 `form_input`으로 교체하고 0차 검증 신설** | 치명 |
| 4 | §8.4 "1차 검증 실패 시 [상품 생성] 비활성화"가 **도달 불가능**(1차 실패 → `failed` → 검토 화면 진입 불가) | 1차 재시도 소진 시 `brochure_ready` + `axis_1 = fail`. 검토 화면에서 실패 항목 열람 가능 | 치명 |
| 5 | `failed`(AI 소진)에서 **재실행 경로가 없음**. "재시도 버튼 제공"에 대응하는 전이 부재 | **[다시 생성]** 신설(§15.3, API #8). `failed` → `input_error`로 의미를 입력 오류 전용으로 축소 | 치명 |
| 6 | "6"이 **필수 폼 그룹 6**과 **데이터 키 6**의 두 의미로 혼용 | §6.0 용어 구분표 신설 | 중대 |
| 7 | 이상 플래그 감지 조건이 2.0 → 2.1에서 **소실**(5종 중 1종만 정의). checklist는 5종 요구 | §5.5 감지 조건표 복원·확장 | 중대 |
| 8 | 배지 4종의 조건이 겹치고, `fail`일 때 편집 여부가 사라짐 | **검증 배지 + 편집 배지 2축 독립**으로 재설계(§10.4) | 보통 |
| 9 | `theme`이 `products` 컬럼과 `page_content`에 **이중 저장**, 동기화 규칙 없음 | `products.theme` 컬럼 **삭제**, `page_content.theme` 단일 출처 | 보통 |
| 10 | 재제출 회차를 구분할 필드가 없어 `retry_index = 0` 행이 여러 벌 쌓임 | **`attempt_no`** 신설(products·execution_logs), 로그 표에 표시 | 보통 |
| 11 | `itinerary` 섹션이 일차 이미지를 가리키는 필드가 스키마에 없어 이미지 검증이 대상을 찾을 수 없었다 | `data.days[].image_slot` 정의(§9.2) | 보통 |
| 12 | `free_text` 블록의 길이 상한이 없어 §10.2의 길이 계약이 그 블록에 작동하지 않았다 | `free_text` **500자** 추가(§17.1) | 보통 |
| 13 | `image` 삽입 블록의 이미지 출처가 미정의 — 새 업로드를 허용하면 슬롯 체계·총량 제한 밖으로 새어 나감 | **업로드된 이미지 중 선택만** 허용, 업로드는 `/new`에서만(§7.3·§10.2) | 보통 |
| 14 | slug 자동 생성 "영문·숫자로 변환 가능하면"의 판정 기준 모호 + §16.1 영문 변환 금지와의 층위 미구분 | 한글 포함 시 **로마자 변환 시도 금지**, `p-{base36}` 발급. §16.1에 층위 명시 | 보통 |
| 15 | `내용`의 "원문근거 밖 요소 없음" 판정 주체·방법 미기술 — AI 자기검사에만 의존 | §6.3 **판정 3단계**(기계 부분문자열 → 기계 명사구 후보 → AI 확인) | 보통 |
| 16 | LCP 2.5초 vs 이미지 27장인데 지연 로딩 미기술 | `next/image` + `hero` priority + 나머지 lazy 명시(§7.3·§17.2) | 경미 |
| 17 | `itinerary_decomposed`·`draft_registered` 등 로그 기록 시점이 본문에 없었다 | §5.4 표에 **기록 시점 열** 추가, 각 API 절에도 명시 | 경미 |
| 18 | §19의 3차 검증 항목이 조건부인데 무조건처럼 표기 / 총 27장이 도달 불가한 조건 | 조건 명시, 총량 규정을 "슬롯별 상한 준수 + 절대 상한 27장"으로 수정 | 경미 |

### 이번 개정의 구조적 조치

2.0(1건) · 2.1(2건) 합계 **3건이 "실제로 도달할 수 없는 상태를 전제한 기능"**이었다. 원인은 상태가 7개인데 상태별 도달 화면·버튼을 모아둔 표가 없어 각 절이 서로 어긋난 것이다.

→ **§15.1 상태 × 화면 × 버튼 표**를 상태 규정의 단일 출처로 신설했다. 이후 화면·버튼을 다루는 모든 절은 이 표와 대조해야 하며, 표에 없는 조합은 존재하지 않는 것으로 간주한다.

---

## 부록 D. 2.2 대비 개정 내역

2.2를 문서 세트 전체(`workflow.md` · `checklist.md` · 에이전트 5 · 스킬 15)와 교차 대조해 찾은 결함 중 **실행을 막는 것들**을 먼저 반영했다.

| # | 2.2의 문제 | 2.3 처리 | 심각도 |
|---:|---|---|---|
| 1 | 시작 조건이 `status`·`current_step`의 특정 값으로 걸려 있어 **§11.6·§15.3의 재시도·재생성 경로 전부가 첫 재호출에서 거부**됐다. 1차 실패 후 소개서 재작성, 2·3차 실패 후 페이지 재생성, [다시 생성] 두 경로가 모두 해당 | **§14.5 시작 조건표 신설.** 조건을 산출물 존재 여부와 선행 축 판정값(= 재호출해도 변하지 않는 값) 기준으로 재정의. `status`는 사람 조작 지점 4개 라우트에만 사용 | 치명 |
| 2 | `409`가 "재시도하라" · "시작 조건 미충족" · "slug 중복" 세 상황을 겸했고, 클라이언트 규약은 전부 재호출로 해석하게 되어 있어 **무한 재호출**에 빠졌다 | **§14.6 응답 규약 신설.** `409`에 `reason` 4종(`retry`·`precondition`·`slug_conflict`·`product_not_published`)과 `retry_from` 필수화. §8.2·§8.4·§9.5의 서술에도 반영 | 치명 |
| 3 | `source` 맵은 `confirmed_data` 경로를 가리키는데(§8.8) 검증 기준값은 `form_input`(§11.1)이어서, **두 경로 체계를 잇는 정의가 없어 1·2차 검증이 원본을 조회할 수 없었다.** `form_input`의 구조 자체도 문서마다 다르게 예시되어 있었다 | **§7.4 `form_input` 구조 확정**(최상위 키 6개·점 표기·전부 문자열). §8.8에 `source` 경로가 양쪽 공통임을 명시 | 치명 |
| 4 | 여행기간이 `form_input` 두 필드에서 `confirmed_data` 한 필드로 합쳐지는데, 이 결합이 §6.2의 허용 3종에 없어 **0차 검증이 위반으로 잡아야 하는 모순** 상태였다 | **§6.2.1 구조 변환 1종 신설.** 결합을 명시적 허용 규칙으로 올리고 §11.1·§11.2의 판정 범위·허용 차이에 반영 | 치명 |
| 5 | [다시 생성]이 `validation_snapshot`을 통째로 폐기해, 되돌린 시작점의 선행 축(`axis_0`·`axis_1`)까지 사라져 재실행 자체가 불가능했다 | **§15.3에 되돌림 범위 표 신설.** 시작점 이전 축은 보존, 이후 축만 폐기. `current_step` 되돌림 값과 `slug` 유지 규칙 명시(§12.1) | 치명(1번에 종속) |
| 6 | 조사 자동 적용 표기(`{{키\|조사}}`)를 사실정보 값 필드에 쓰면 원본값과 달라져 1·2차 검증이 확정 실패했다 | §8.8에 **조사 처리는 `source = "generated"` 서술 필드에만 허용**한다는 규칙 추가 | 치명 |
| 7 | `gallery` 슬롯(0~6장)이 `page_content` 9개 섹션 어디에도 배치할 자리가 없어, **업로드해도 화면에 나올 수 없는 기능**이었다. §21은 위치를 미결정으로 남겨 뒀고 §9.3은 섹션 추가를 금지했다 | **`gallery` 슬롯 삭제.** 업로드 슬롯 4종으로 축소, 절대 상한 27장 → 21장(§7.3·§5.2). 슬롯 외 사진은 편집기 `image` 블록으로 배치한다. §21의 해당 잔여 결정 삭제 | 치명 |

### 확인·추적 구멍 (2.3에서 함께 해소)

| # | 2.2의 문제 | 2.3 처리 |
|---:|---|---|
| 8 | `abnormality_flags`에 `attempt_no`가 없는데 중복 기록 규칙은 "단계 + 유형당 1회"여서, **2번째 시도의 같은 이상이 기록되지 않았다.** `validation_repeated_failure`의 시도별 집계도 판정 근거가 없었다 | §5.5에 `attempt_no`·`product_id` 컬럼 추가. 중복 범위를 `(execution_id, attempt_no, step, type)`으로 재정의 |
| 9 | 0차와 1차가 `brochure` 카운터를 공유해, 0차가 재시도를 쓰면 **1차의 재시도 예산이 줄었고**, 값이 2일 때 결과가 `input_error`인지 `brochure_ready + fail`인지 판별할 수 없었다 | 카운터를 **4종으로 분리**(`normalization` 신설). §11.6에 카운터 ↔ 축 1:1 대응표와 소진 시 상태 명시 |
| 10 | 2차 재시도 소진 시 3차를 건너뛰므로 **`draft_registered` 로그가 남지 않았다.** §5.4는 10개 단계 전부 기록을 요구한다 | §9.5에 `draft` 도달 경로 3종 표를 넣고, 2차 라우트가 전이·기록을 담당하도록 규정 |
| 11 | 「가공 없는 원본 기록」이 신청 로그에도 적용돼, 신청 내역 화면에서 마스킹한 연락처가 **로그 화면에서 원본으로 노출**됐다 | §5.4에 개인정보 예외 신설. `category = application`의 이름·이메일·연락처는 저장 시점에 마스킹 |

### 문서 내 불일치 (2.3에서 함께 해소)

| # | 2.2의 문제 | 2.3 처리 |
|---:|---|---|
| 12 | §3은 소개서 생성을 "3개 요청", §8.5·§19·§20은 "4개"로 적었다. §4.2 본문도 "3개"였다 | §3 흐름도에 상품 등록 요청을 추가하고 §4.2를 **4개**로 수정 |
| 13 | `verdict`가 §5.4는 `통과`/`반려`, §11.3은 `pass`/`fail`로 **같은 이름에 다른 값 체계**였다 | 저장값을 **영어로 통일**, 한글은 화면 표시 전용으로 분리(§5.4·§14.3) |
| 14 | §6.3의 0차 3단계 판정 담당을 `fact-check`로 적었으나, 그 스킬은 1·2차 전용이고 문서에 "0차를 하지 않는다"고 명시돼 있다 | 담당을 `itinerary-decomposition`의 호출 1회 안으로 정정(§6.3·§11.1) |
| 15 | 길이 계약 6종 중 `free_text`·`notice`는 편집기 블록이라 **생성 시점에 검사할 수 없는데** §19는 6종 전부 강제를 요구했다 | §17.1 표에 강제 시점 열 추가. **생성 4종 / 편집 6종**으로 분리 |
| 16 | §21의 잔여 결정 중 테마 키 목록·일차 구분 인식 범위는 이미 하위 문서에서 확정돼 있었고, §21의 인식 범위(4종)와 실제(6종)가 달랐다 | 확정 내용을 §9.4·§6.3 **본문으로 승격**하고 §21에서 제거. 인식 범위는 6종으로 확정 |

### 남은 항목

없다. 2.2 대조에서 발견된 16건을 전부 반영했다. 단 **하위 문서 13개가 아직 2.2 기준**이므로 아래 표대로 동기화해야 문서 세트가 정합해진다.

### 하위 문서 반영 필요

**`spec.md`가 단일 출처이므로 아래는 spec을 따라 수정해야 한다.**

**대상은 13개 파일이다.** 아래 표는 실제 파일을 대조해 확인한 목록이며, 개정 항목별로 묶었다.

| 개정 항목 | 문서 | 수정할 내용 |
|---|---|---|
| **1** 시작 조건 | `.claude/skills/product-orchestrator/SKILL.md` | 「선행 조건 표」 전체를 §14.5로 교체 |
| | `.claude/agents/web-builder-agent.md` | 「선행 조건」의 `status = brochure_ready`를 §14.5 5번 조건으로 교체 |
| | `.claude/skills/content-structuring/SKILL.md` | 같은 선행 조건 교체 |
| | `.claude/skills/intro-template-writer/SKILL.md` | 선행 조건의 `current_step = normalization_validated`를 `confirmed_data` 존재 + `axis_0 = pass`로 교체 |
| **2** `409` `reason` | `.claude/skills/product-orchestrator/SKILL.md` | 「응답 코드 규약」의 `409` 행을 §14.6의 `reason` 4종으로 분리 |
| | `.claude/agents/intake-agent.md` | 「응답 코드」 표의 `409`에 `reason`·`retry_from` 추가 |
| | `.claude/agents/validator-agent.md` | 같은 수정 |
| | `.claude/skills/fact-check/SKILL.md` | 「산출물」의 `409` 서술에 `reason`·`retry_from` 추가 |
| | `.claude/skills/consistency-check/SKILL.md` | 같은 수정 |
| | `workflow.md` | §10.4 응답 코드 규약을 §14.6으로 교체. §3·§4·§9의 흐름도와 조건 분기에 `retry_from` 반영 |
| **3** `form_input` 구조 | `.claude/skills/input-guard/SKILL.md` | `form_input` 예시를 §7.4 구조로 교체(평면 키 `가격_성인` → 점 표기 `가격.성인`). 사유 문장 규칙의 「내부 키」 예시도 함께 갱신 |
| **4** 여행기간 결합 | `.claude/skills/data-normalization/SKILL.md` | 「여행기간 결합 형식」을 §6.2.1 참조로 정리하고, 결합이 정규화 3종이 아니라 구조 변환 1종임을 명시 |
| **6** 조사 처리 | `.claude/skills/intro-content-fill/SKILL.md` | 값 필드에 조사를 붙인 예시(`객실타입` → `디럭스룸으로`) 삭제. 조사 표기는 서술 필드에만 쓴다는 규칙 추가 |
| | `workflow.md` | §7.1·§7.2의 조사 처리 예시를 같은 기준으로 수정 |
| **7** `gallery` 삭제 | `.claude/skills/content-structuring/SKILL.md` | 「이미지 슬롯 배치」에서 갤러리 블록 행 삭제. `data.image_slots`(복수) 필드를 §9.2 스키마에 맞춰 정리 |
| | `checklist.md` | F-02의 슬롯 상한에서 `gallery 6` 삭제 |
| **8** 이상 플래그 | `.claude/skills/abnormality-detection/SKILL.md` | 중복 기록 범위를 `(execution_id, attempt_no, step, type)`으로 교체. 입력에 `attempt_no` 추가 |
| | `.claude/agents/log-monitor-agent.md` | 같은 수정 |
| **9** 카운터 4종 | `.claude/skills/product-orchestrator/SKILL.md` | 카운터 3종 → 4종, 0차 공유 규정 삭제 |
| | `.claude/agents/intake-agent.md` | 0차 실패 시 `retry_counts.normalization` +1로 수정 |
| **11** 개인정보 | `.claude/skills/execution-log-collection/SKILL.md` | 「원본 보존」에 `category = application` 마스킹 예외 추가 |
| **13** `verdict` | `.claude/skills/execution-log-collection/SKILL.md` | `verdict` 저장값을 `pass`/`fail`/`-`로, 화면 표시만 한글로 |
| | `.claude/agents/log-monitor-agent.md` | 같은 수정 |
| **16** 확정 이관 | `.claude/skills/theme-design-token-match/SKILL.md` | 매핑표를 §9.4 참조로 정리(값은 동일) |
| | `.claude/skills/itinerary-decomposition/SKILL.md` | 일차 구분 인식 범위를 §6.3 참조로 정리(값은 동일) |
| **전체** | `checklist.md` | A 섹션에 시작 조건(§14.5)·`409 reason`(§14.6) 검증 항목 추가. S-03의 카운터 3종 → 4종, Q-08의 `verdict` 값, R-04의 `attempt_no`, J-09·H-11의 길이 계약 시점 반영 |

**대상은 16개 파일이다**(2.3 최종). `product-orchestrator` · `content-structuring` · `execution-log-collection` · `log-monitor-agent` · `intake-agent` · `workflow.md` · `checklist.md`는 두 개 이상의 항목에 걸쳐 있으므로 한 번에 수정한다.
